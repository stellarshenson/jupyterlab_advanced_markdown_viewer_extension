# Logs

| Log                                     | Tracks                                                                                                                                                                           |
| --------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `make-install.log`                      | Output of `make install` - nodeenv creation, dependency install, TypeScript build, labextension build and pip install                                                            |
| `make-test.log`                         | Output of `make test` - jest unit suite with coverage, then pytest for the server extension                                                                                      |
| `build-cycle.log`                       | One test-then-install cycle: `make test`, and `make install` only when the tests passed                                                                                          |
| `galata.log`                            | Output of the Playwright / Galata integration run in `ui-tests/`, including the test server's own log lines                                                                      |
| `jest.log`, `pytest.log`, `lint.log`    | The proving executor's unit, server and lint runs on the build named in `make-install.log`                                                                                       |
| `galata-proving-*.log`, `proving-*.log` | Narrowed Galata runs and the mutation attacks on the installed bundle - each mutation named, the target tests that went red, the control that stayed green, and the restored md5 |

- `round8-chain.log` - ACC-NOTES-139 chain: Galata proof against 0.6.59, round-8 mutations, graph refresh, build 0.6.60, lint, both Galata suites; ends with CHAIN-DONE
- `round8b-chain.log` - strip order ruling: Galata proof against 0.6.60, graph refresh, build 1.0.2, lint, both Galata suites; ends with CHAIN-DONE
- `screenshots-chain.log` - README screenshots (ui-tests/tests/screenshots.spec.ts), rewritten by every run of the spec; the shipped files are from the last run; ends with SHOTS-DONE
- `round9-chain.log` - round-8 fixes: mutations, graph refresh, version reset to 1.0.1, build 1.0.2 (second build of that number, the first carried the round-8 findings), lint, full jest to `jest-102.log`, both Galata suites to `galata-102-second-*.log`, screenshots rerun; ends with CHAIN-DONE
- `galata-102-second-rerun-138.log` - ACC-NOTES-138 rerun three times on the second 1.0.2 build after its one timeout during the low-memory event
- `round9-mutations-rerun.log` - the round-9 mutation runner rerun with the corrected first mutation, 4 of 4 caught
- `lint-102-second.log` - lint on the second 1.0.2 build
- `round10-chain.log` - round-9 fixes: mutations to `mutation-*.log` (one log per mutation, the glob shared by every round's runner), graph refresh, version reset to 1.0.1, build 1.0.2 (third build of that number), lint to `lint-102-third.log`, full jest to `jest-102-third.log`, both Galata suites to `galata-102-third-*.log`, screenshots rerun; ends with CHAIN-DONE
- `round11-chain.log` - round-10 fixes: mutations, graph refresh, version reset to 1.0.3 so the build lands on the published 1.0.4, lint to `lint-104.log`, full jest to `jest-104.log`, both Galata suites to `galata-104-*.log`, screenshots rerun; ends with CHAIN-DONE
- `galata-104-rerun-3.log` - the three cases that failed in the round-11 chain while the sibling extensions were being rebuilt in their own trees (ACC-NOTES-138 and two alerts-sibling cases), rerun on the settled lab: 3 passed
- `install-106-reinstall.log` - `make install` after the system refresh of 2026-09-10 put 1.0.5 back in site-packages; version reset to 1.0.5 so the build lands on 1.0.6 again
- `galata-106-proof-minimap-plus.log` - the rewritten ACC-NOTES-139 and ACC-NOTES-130 cases against the installed 1.0.6 that still showed the plus on the strip: 2 failed at the new toBeHidden assertions
- `round20-mutations.log` - two guards reverted, each spec passing unmutated first (`mutation-baseline-round20-*.log`): the plus put back on the bare rule that shows it in every state (`mutation-plus-shown-on-the-strip.log`), and the read applying the file's panel state while a state write is pending (`mutation-stored-state-read-over-a-pending-write.log`); 2 of 2 caught
- `round20-chain.log` - the minimap plus and the sibling gate: version reset to 1.0.5, build 1.0.6 to `build-106-round20.log`, lockfile churn restored, lint to `lint-106-round20.log`, full jest to `jest-106-round20.log`, both Galata suites to `galata-106-round20-*.log`, the sibling suite with CI=1 to `galata-106-siblings-ci.log`, screenshots rerun, graph refresh; ends with CHAIN-DONE
- `galata-106-rerun-138.log`, `galata-106-repeat-138.log`, `galata-106-trace-138.log` - ACC-NOTES-138 on the first round-20 build: 1 of 5 failed at its Hide-control focus assertion, then 10 of 10 and 30 of 30 passed (the last keeping traces on failure); with the chain's own failure, 2 of 46 (DEF-NOTES-71)
- `jest-def-notes-71-repro.log` - the DEF-NOTES-71 jest case on the unfixed controller: fails, the controller emitting expanded, hidden, expanded
- `round20b-chain.log` - DEF-NOTES-71 fixed in the controller: version reset to 1.0.5, build 1.0.6 to `build-106-round20b.log`, lockfile churn restored, lint to `lint-106-round20b.log`, full jest to `jest-106-round20b.log`, both Galata suites to `galata-106-round20b-*.log`, the sibling suite with CI=1 to `galata-106-siblings-ci-round20b.log`, ACC-NOTES-138 thirty times to `galata-106-repeat-138-round20b.log`, screenshots rerun, graph refresh; ends with CHAIN-DONE
- `galata-106-proof-def-notes-72.log` - the ACC-NOTES-139 case with its new Cancel step against the installed 1.0.6 that lacks the DEF-NOTES-72 fix: failed at `notes.spec.ts:625`, the file still holding the document marker after Cancel
- `jest-def-notes-72.log` - the two notes jest files with the DEF-NOTES-72 fix in the source: 260 passed
- `round21-mutations.log` - six DEF-NOTES-72 guards reverted, each spec passing unmutated first (`mutation-baseline-round21-*.log`), one log per mutation under `mutation-*.log`
- `round21-chain.log` - DEF-NOTES-72 fixed and the README images retaken at 2560x1440 in the Galaxa Dark Theme - Steel: version reset to 1.0.5, build 1.0.6 to `build-106-round21.log`, lockfile churn restored, lint to `lint-106-round21.log`, full jest to `jest-106-round21.log`, mutations, both Galata suites to `galata-106-round21-*.log`, screenshots rerun with the six image sizes listed in `screenshots-chain.log`, graph refresh; ends with CHAIN-DONE
- `galata-106-proof-acc-notes-146.log` - the ACC-NOTES-146 case against the installed 1.0.6 built before the note field font change: failed, the field's font family `monospace` where the panel's is `"DejaVu Sans", "Noto Sans SC Variable"`
- `round22-chain.log` - the note field in the panel's font family at a smaller size (ACC-NOTES-146): the proof above, version reset to 1.0.5, build 1.0.6 to `build-106-round22.log`, lockfile churn restored, lint to `lint-106-round22.log`, full jest to `jest-106-round22.log`, both Galata suites to `galata-106-round22-*.log`, graph refresh; ends with CHAIN-DONE
- `galata-106-rerun-118-forced-render.log` - the two cases that failed in the round-22 notes group, rerun on the round-22 build: ACC-NOTES-118 with its check changed to four filled lines without scrolling passed; the forced render case in siblings.spec.ts failed again with the preview moved by 1667 px (DEF-APPLY-37-1)
- `galata-106-repeat-forced-render.log` - the forced render case in siblings.spec.ts six times in isolation on the round-22 build, to measure how often it fails: 5 passed and 1 failed, the failure again with the preview moved by 1667 px
- `screenshots-two-below-top-bar.log` - the two README screenshots kept at the Star Colonel's request, retaken on the round-22 build below JupyterLab's top bar with dates in en-GB: 2 passed, animation-01.png and notes-01-panel.png both 2560x1440
- `galata-106-rerun-round23-events-forced-render.log` - the two cases that failed in the round-23 chain, three times each on the round-23 build: events.spec.ts 'applies two writes fifty milliseconds apart as one change' 3 passed (its round-23 failure, 2 applies for 1, came while another session's process held the load average above 20) and the forced render case in siblings.spec.ts 3 passed
- `galata-106-proof-def-cue-74.log` - the tab-cue case 'is a still red square with a tooltip, telling itself apart from the arriving circle', asserting the corrected held-change tooltip, against the installed round-23 build that still told the reader to save or revert: failed, the tooltip reading 'Save or revert the document to take the change.'
- `round24-chain.log` - DEF-CUE-74 fixed and the README rewritten from the marketing and UX reviews: the proof above, version reset to 1.0.5, build 1.0.6 to `build-106-round24.log`, lockfile churn restored, lint to `lint-106-round24.log`, full jest to `jest-106-round24.log`, both Galata suites to `galata-106-round24-*.log`, graph refresh; ends with CHAIN-DONE
- `round23-mutations.log` - the DEF-NOTES-73 fix reverted, the retried panel-state write given back the state its call captured, after the notes spec passed unmutated (`mutation-baseline-round23-notes.spec.log`); the mutated run in `mutation-retried-state-write-stores-the-captured-state.log`; 1 of 1 caught, the new case 'stores the newer state when an older state write is refused and retried after the newer one lands' failing
- `round23-chain.log` - DEF-NOTES-73 fixed and the round-2 register record written, on top of ACC-NOTES-146: version reset to 1.0.5, build 1.0.6 to `build-106-round23.log`, lockfile churn restored, lint to `lint-106-round23.log`, full jest to `jest-106-round23.log`, the mutation above, both Galata suites to `galata-106-round23-*.log`, graph refresh; ends with CHAIN-DONE
- `galata-106-proof-acc-notes-146-quarter.log` - the ACC-NOTES-146 case with its new size assertion, three quarters of `--jp-ui-font-size0`, against the installed round-24 build whose note field is at `--jp-ui-font-size0`: failed, the field at 10.8333 px where three quarters of `--jp-ui-font-size0` is 8.125 px
- `round25-chain.log` - the note field font a quarter smaller (ACC-NOTES-146): the proof above, version reset to 1.0.5, build 1.0.6 to `build-106-round25.log`, lockfile churn restored, lint to `lint-106-round25.log`, full jest to `jest-106-round25.log`, both Galata suites to `galata-106-round25-*.log`, graph refresh; ends with CHAIN-DONE
- `galata-106-proof-acc-notes-147.log` - the ACC-NOTES-147 case against the installed round-25 build, which still offers Add note on a row whose note field is open: failed, Add note count 1 where 0 is required
- `round26-mutations.log` - the panel spec passing unmutated first (`mutation-baseline-round26-notes-panel.spec.log`), then the row built without telling its controls that its note field is open (`mutation-add-note-offered-while-the-field-is-open.log`), which the spec must catch
- `round26-chain.log` - Add note left out of a row while its note field is open (ACC-NOTES-147): the proof and the mutation above, version reset to 1.0.5, build 1.0.6 to `build-106-round26.log`, lockfile churn restored, lint to `lint-106-round26.log`, full jest to `jest-106-round26.log`, both Galata suites to `galata-106-round26-*.log`, graph refresh; ends with CHAIN-DONE
- `galata-106-proof-def-cue-75.log` - the tab-cue case 'is a still red square with a tooltip, telling itself apart from the arriving circle', asserting the round-3 tooltip words 'choose Revert in the File Changed dialog', against the installed round-26 build whose tooltip still says 'Revert the document to take the change': failed, the tooltip reading 'A change on disk is held back by unsaved edits in this document. Revert the document to take the change; saving keeps your version and drops the change.'
- `round27-chain.log` - DEF-CUE-75 (the held-change tooltip names Revert in the File Changed dialog): the proof above, version reset to 1.0.5, build 1.0.6 to `build-106-round27.log`, lockfile churn restored, lint to `lint-106-round27.log`, full jest to `jest-106-round27.log`, both Galata suites to `galata-106-round27-*.log`, graph refresh; ends with CHAIN-DONE
- `galata-106-proof-acc-notes-148.log` - the ACC-NOTES-148 case against the installed round-27 build, whose closed rows still carry the triangle: failed, a triangle count of 1 on a closed row where 0 is required
- `galata-106-proof-acc-notes-146-ordinary.log` - the ACC-NOTES-146 case asserting three quarters of `--jp-ui-font-size1`, against the installed round-27 build whose note field is at three quarters of `--jp-ui-font-size0`: failed, the field at 8.12497 px where 9.75 px is required
- `round28-mutations.log` - the panel spec passing unmutated first (`mutation-baseline-round28-notes-panel.spec.log`), then the triangle built on a closed row as well (`mutation-triangle-on-a-closed-row.log`), which the spec must catch
- `round28-chain.log` - the triangle only on an open row (ACC-NOTES-148) and the note field at three quarters of the panel's ordinary text size (ACC-NOTES-146): both proofs and the mutation above, version reset to 1.0.5, build 1.0.6 to `build-106-round28.log`, lockfile churn restored, lint to `lint-106-round28.log`, full jest to `jest-106-round28.log`, both Galata suites to `galata-106-round28-*.log`, the README notes image retaken to `screenshots-round28-notes-panel.log`, graph refresh; ends with CHAIN-DONE
- `galata-106-proof-def-notes-76.log` - the DEF-NOTES-76 case against the installed round-28 build, where a click on a note leaves its row closed and unselected: failed, the row's controls not found after the click on its note
- `galata-106-proof-def-notes-77.log` - the DEF-NOTES-77 case against the installed round-28 build, where a double click on Add note lands its second click on a colour dot: failed, the row's swatch named 'pink' where 'yellow' is required
- `round29-mutations.log` - the panel spec passing unmutated first (`mutation-baseline-round29-notes-panel.spec.log`), then the note's listener moved to double click (`mutation-note-click-selects-nothing.log`), the text-selection test removed (`mutation-note-click-drops-a-text-selection.log`) and Add note put back before the colour dots (`mutation-add-note-built-before-the-dots.log`), each of which the spec must catch
- `round29-chain.log` - DEF-NOTES-76 fixed, a click on a note selecting its row, and DEF-NOTES-77 fixed, the colour dots staying in place when Add note leaves the row: both proofs and the mutations above, version reset to 1.0.5, build 1.0.6 to `build-106-round29.log`, lockfile churn restored, lint to `lint-106-round29.log`, full jest to `jest-106-round29.log`, both Galata suites to `galata-106-round29-*.log`, the README notes image retaken to `screenshots-round29-notes-panel.log`, graph refresh; ends with CHAIN-DONE
- `round30-mutations.log` - the panel spec passing unmutated first (`mutation-baseline-round30-notes-panel.spec.log`), then the click listener on a row's state line removed (`mutation-state-line-click-opens-nothing.log`), which the spec must catch
- `round30-chain.log` - DEF-NOTES-78 fixed, a click on a closed row's state line opening the row (review round 6's plan change): the mutation above, version reset to 1.0.5, build 1.0.6 to `build-106-round30.log`, lockfile churn restored, lint to `lint-106-round30.log`, full jest to `jest-106-round30.log`, both Galata suites to `galata-106-round30-*.log`, graph refresh; ends with CHAIN-DONE
- `galata-106-proof-def-cue-79.log`, `galata-106-proof-def-notes-80.log`, `galata-106-proof-acc-cue-149.log`, `galata-106-proof-def-notes-82.log`, `galata-106-proof-def-notes-83.log` - the five new Galata cases for the four known limitations against the installed 07654a2 build of 1.0.6: each failed, the red square still on the tab after Reload from Disk, no row opened from a click on its padding, an empty row description, a document marker written by a double click on the badge, and a note held twice after a double click on Save
- `galata-probe-badge-double-click.log` - the measurement of DEF-NOTES-82 on the installed build: 25 of 25 double clicks on the badge pressed the header control under it
- `round31-mutations.log` - the watcher and panel specs passing unmutated first (`mutation-baseline-round31-*.log`), then sixteen guards undone one at a time under `mutation-*.log`: four on the Reload from Disk path (`mutation-reload-*.log`), three on the row click (`mutation-padding-click-*.log`), five on the row description (`mutation-row-without-description.log`, `mutation-closed-description-on-an-open-row.log`, `mutation-description-*.log`, `mutation-descriptions-inside-the-list-body.log`) and four on the double-click guard (`mutation-double-click-*.log`, `mutation-single-click-ignored.log`, `mutation-key-press-ignored.log`); 16 of 16 caught
- `round31-chain.log` - the four known limitations fixed (DEF-CUE-79, DEF-NOTES-80, DEF-NOTES-81 with ACC-CUE-149, DEF-NOTES-82 and DEF-NOTES-83): the five proofs and the mutations above, version reset to 1.0.5, build 1.0.6 to `build-106-round31.log`, lockfile churn restored, lint to `lint-106-round31.log`, full jest to `jest-106-round31.log`, both Galata suites to `galata-106-round31-*.log`, graph refresh; ends with CHAIN-DONE
- `galata-106-proof-acc-notes-146-rows.log` - the rewritten ACC-NOTES-146 case, the note field measured against the note text shown in a row, against the installed round-31 build: failed, the field at 9.75 px where the note text is 13 px
- `round32-chain.log` - the note field at the size of the notes in the rows (ACC-NOTES-146, the Star Colonel's request): the proof above, version reset to 1.0.5, build 1.0.6 to `build-106-round32.log`, lockfile churn restored, lint to `lint-106-round32.log` (1 stylelint error on a comment placed inside the rule, moved above it and rerun clean to `lint-106-round32b.log`), full jest to `jest-106-round32.log`, both Galata suites to `galata-106-round32-*.log`, graph refresh; ends with CHAIN-DONE
- `galata-106-proof-def-notes-84.log`, `galata-106-proof-acc-notes-150.log`, `galata-106-proof-def-notes-80-closed-row.log` - the three cases of round 33 against the installed round-32 build: DEF-NOTES-84 failed with no note field opened (the mark was never written), ACC-NOTES-150 failed with a note entry on a closed row; the DEF-NOTES-80 case passed there because its closed-row assertions ran before the external write had reached the panel, which the case now waits for by opening the row first
- `round33-mutations.log` - the highlight and anchor specs and the panel spec passing unmutated first (`mutation-baseline-round33-*.log`), then the line break ignored (`mutation-line-break-ignored.log`), every element read as a break (`mutation-every-element-breaks-the-text.log`), a closed row showing every note (`mutation-closed-row-shows-every-note.log`) and its first note (`mutation-closed-row-shows-the-first-note.log`); 4 of 4 caught
- `round33-chain.log` - DEF-NOTES-84 (a hard line break in the captured text) and ACC-NOTES-150 (a closed row shows no note): the proofs and the mutations above, version reset to 1.0.5, build 1.0.6 to `build-106-round33.log`, lockfile churn restored, lint to `lint-106-round33.log`, full jest to `jest-106-round33.log`, both Galata suites to `galata-106-round33-*.log` (2 failed in the notes group: the DEF-NOTES-84 row listed the raw source `3. **Ostateczny rygor:**`, fixed in round 34, and the DEF-NOTES-76 case clicked a note on a closed row, rewritten for round 34), graph refresh; ends with CHAIN-DONE
- `round34-mutations.log` - the watcher spec and the notes spec passing unmutated first (`mutation-baseline-round34-*.log`), then the hold test reading the dirty flag alone (`mutation-stale-flag-holds-the-write.log`), the hold skipped for every dirty document with a change held (`mutation-every-dirty-document-takes-the-write.log`) and the passage listed as the raw source (`mutation-passage-listed-as-raw-source.log`)
- `round34-chain.log` - the round-32 review plan (DEF-CUE-85, the cursor over the controls row and the note form), the listed passage of DEF-NOTES-84 and the DEF-NOTES-76 case rewritten: the DEF-NOTES-84 proof against the round-33 build to `galata-106-proof-def-notes-84.log` (rewritten by this run), the mutations above, version reset to 1.0.5, build 1.0.6 to `build-106-round34.log`, lockfile churn restored, lint to `lint-106-round34.log`, full jest to `jest-106-round34.log`, both Galata suites to `galata-106-round34-*.log`, graph refresh; ends with CHAIN-DONE
- `galata-106-rerun-round34-forced-render.log`, `galata-106-rerun-round34-forced-render-2.log` - the forced-render sibling case (siblings.spec.ts 'runs every listener twice for one change without a sibling misbehaving'), which failed once in the round-34 notes group with the preview moved by 1667 px, rerun three times twice on the round-34 build: 3 failed, then 1 failed and 2 passed; the same intermittent failure as on the round-22 build (DEF-APPLY-37-1), nothing in this delta touching the scroll path
- `round35-mutations.log` - the watcher spec passing unmutated first (`mutation-baseline-round35-watcher.spec.log`), then the shadow clause removed from the hold test (`mutation-shadow-clause-removed.log`) and the shadow test inverted (`mutation-typed-text-taken-for-the-shadow.log`)
- `round35-chain.log` - shape A of DEF-CUE-85 and the held-change tooltip naming Reload from Disk: the tab-cue proof to `galata-106-proof-def-cue-75-reload.log` and the live-view proof to `galata-106-proof-def-cue-85-shape-a.log` against the round-34 build, the mutations above, version reset to 1.0.5, build 1.0.6 to `build-106-round35.log`, lockfile churn restored, lint to `lint-106-round35.log`, full jest to `jest-106-round35.log`, both Galata suites to `galata-106-round35-*.log`, the forced-render case three times to `galata-106-repeat-round35-forced-render.log`, graph refresh; ends with CHAIN-DONE
- `round36-shot.log` - the modern note field (ACC-NOTES-151): version reset to 1.0.5, build 1.0.6 to `build-106-round36.log`, lockfile churn restored, then the scratch spec `tmp/campaign/limitations/field-shot.spec.ts.txt` copied into `ui-tests/tests/` for the run and removed after, writing pictures of the open field, empty and while typing, in the Galaxa Steel dark theme and JupyterLab Light to `tmp/field-*.png` (`galata-106-round36-field-shot.log`, 2 passed)
- `galata-106-round36-acc-notes-151.log` - the Galata case 'ACC-NOTES-151 draws the note field as a rounded box with a brand-coloured focus ring, Cancel plain and Save filled at the right edge' twice on the round-36 build, after its first run read the border colour mid-transition (2 passed)
- `lint-106-round36.log` - lint check after the note field change
- `round37-mutations.log` - the anchor and notes specs passing unmutated first (`mutation-baseline-round37-*.spec.log`), then the br flush removed from tokeniseSource (`mutation-br-flush-removed.log`), every tag flushing (`mutation-every-tag-flushes.log`) and the listed passage read as a slice again (`mutation-listed-passage-read-as-a-slice.log`); each must fail its spec
- `round37-chain.log` - review round 2's plan (DEF-NOTES-86 a br written in the source, DEF-NOTES-87 the listed passage from the whole-source scan) with the note field of round 36 on one build: the DEF-NOTES-86 proof against the round-36 build to `galata-106-proof-def-notes-86.log`, the mutations above, version reset to 1.0.5, build 1.0.6 to `build-106-round37.log`, lockfile churn restored, lint to `lint-106-round37.log`, jest and pytest to `jest-106-round37.log`, Galata to `galata-106-round37-notes-siblings-settings.log` and `galata-106-round37-live-events-stream-cue.log`, the forced-render sibling case three times to `galata-106-repeat-round37-forced-render.log`, graph refresh
- `galata-106-repeat-round37-forced-render.log` - the forced-render sibling case three times on the round-37 build, 2 of 3, the rejected intermittent DEF-APPLY-37-1
- `round38-chain.log` - the note field refined at the Star Colonel's word of 2026-09-11 00:58 (ACC-NOTES-151: Save and Cancel in the Add note look, no focus ring): version reset to 1.0.5, build 1.0.6 to `build-106-round38.log`, lockfile churn restored, lint to `lint-106-round38.log`, jest and pytest to `jest-106-round38.log`, the notes Galata group to `galata-106-round38-notes.log`, the field pictures to `tmp/field-*.png` (`galata-106-round38-field-shot.log`), graph refresh
- `round39-chain.log` - the animation speed default raised to 50 characters per second (ACC-ANIM-143, ACC-ANIM-76) and the highlight duration default to 5 s (ACC-HILITE-21), both at the Star Colonel's word of 2026-09-11: the ACC-ANIM-143 proof against the round-38 build to `galata-106-proof-acc-anim-143-fifty.log`, version reset to 1.0.5, build 1.0.6 to `build-106-round39.log`, lockfile churn restored, lint to `lint-106-round39.log`, jest and pytest to `jest-106-round39.log`, the live-view, events, stream and tab-cue Galata group to `galata-106-round39-live-events-stream-cue.log`, graph refresh
- `round40-mutations.log` - the diff and controller specs passing unmutated first (`mutation-baseline-round40-*.spec.log`), then the line pass made always coarse (`mutation-line-pass-always-coarse.log`), the line pass skipped by diffWords (`mutation-line-pass-skipped-by-the-controller.log`) and changed lines no longer compared by word (`mutation-changed-lines-not-compared-by-word.log`); each must fail its spec
- `round40-chain.log` - DEF-HILITE-88 (a line pushed into a long document highlighted the whole document green): the Galata proof against the round-39 build to `galata-106-proof-def-hilite-88.log`, the mutations above, version reset to 1.0.5, build 1.0.6 to `build-106-round40.log`, lockfile churn restored, lint to `lint-106-round40.log`, jest and pytest to `jest-106-round40.log`, Galata to `galata-106-round40-notes-siblings-settings.log` and `galata-106-round40-live-events-stream-cue.log`, the forced-render sibling case three times to `galata-106-repeat-round40-forced-render.log`, graph refresh
- `galata-106-heading-shot.log`, `galata-106-heading-shot-marked.log` - the two-line h3 title of the Norlandia argument copy (`tmp/campaign/limitations/ostateczny.md`) pictured selected by the mouse with the extension live and off (`tmp/heading-live-selected.png`, `tmp/heading-off-selected.png`, byte-identical) and marked yellow (`tmp/heading-live-marked.png`), for DEF-NOTES-89; the scratch spec is `tmp/campaign/limitations/heading-shot.spec.ts.txt`, copied into `ui-tests/tests/` for the run and removed after
- `round41-chain.log` - DEF-NOTES-89 (headings in the preview take a line height of 1.25, so a selection over a wrapped heading covers no letters of the line above): the Galata proof against the round-40 build to `galata-106-proof-def-notes-89.log`, version reset to 1.0.5, build 1.0.6 to `build-106-round41.log`, lockfile churn restored, lint to `lint-106-round41.log`, jest and pytest to `jest-106-round41.log`, Galata to `galata-106-round41-notes-siblings-settings.log` and `galata-106-round41-live-events-stream-cue.log`, the heading pictured selected and marked again to `tmp/heading-*.png` (`galata-106-round41-heading-shot.log`), graph refresh
- `round41-chain-first-run.log` - the first run of the round-41 chain, stopped during its build after its proof failed for a wrong reason (the case read no line box); its install had already put the new stylesheet in the lab, so the second run rebuilds the old one for the proof first (`build-106-round41-prefix.log`)
- `round42-mutations.log` - the diff, controller and anchor specs passing unmutated first (`mutation-baseline-round42-*.spec.log`), then the line pass bounded by the token bound again (`mutation-line-pass-bounded-by-tokens.log`), the line pass skipped on the controller path in a form that compiles (`mutation-line-pass-skipped-by-the-controller.log`, replacing the round-40 run of the same name that a TypeScript error had scored) and the end-tag br spelling no longer a break (`mutation-end-tag-br-not-a-break.log`); each must fail its spec by a failing test, a compile error alone counts as missed
- `round42-chain.log` - review round 3's plan (MAX_LCS_LINES, the </br> spelling, the placeholder colour, the coarse-guard comment and the register trails): the ACC-NOTES-151 placeholder assertion against the round-41 build to `galata-106-proof-acc-notes-151-placeholder.log`, the mutations above, version reset to 1.0.5, build 1.0.6 to `build-106-round42.log`, lockfile churn restored, lint to `lint-106-round42.log`, jest and pytest to `jest-106-round42.log`, Galata to `galata-106-round42-notes-siblings-settings.log` and `galata-106-round42-live-events-stream-cue.log`, graph refresh
- `round43-mutations.log` - the diff spec passing unmutated first (`mutation-baseline-round43-diff.spec.log`), then the line bound without its headroom (`mutation-line-bound-without-headroom.log`) and the later side bounded at 1500 alone (`mutation-line-bound-on-the-earlier-side-alone.log`); each must fail the spec by a failing test
- `round43-chain.log` - review round 4's plan (headings at the font's own line height, the line bound 1600 with the 1500-line case, the ghost-placement comment): the mutations above, version reset to 1.0.5, build 1.0.6 to `build-106-round43.log`, lockfile churn restored, lint to `lint-106-round43.log`, jest and pytest to `jest-106-round43.log`, Galata to `galata-106-round43-notes-siblings-settings.log` and `galata-106-round43-live-events-stream-cue.log`, the heading pictured again to `tmp/heading-*.png` (`galata-106-round43-heading-shot.log`), graph refresh
- `round43-chain.log` - review round 4's plan verified: 2 of 2 mutations, build 1.0.6, lint, jest 615, pytest 29, Galata 115 and 48, heading pictures under line-height normal, graph refresh
- `round43-mutations.log`, `mutation-baseline-round43-diff.spec.log`, `mutation-line-bound-*.log` - the line bound without headroom and on the earlier side alone, each caught by the 1500-line case
- `build-106-round43.log`, `lint-106-round43.log`, `jest-106-round43.log`, `galata-106-round43-*.log` - the round-43 chain's build, lint, jest and Galata output
- `proof-def-apply-90-jest.log` - the DEF-APPLY-90 watcher case on the unfixed source: 1 failed, the file applied over the typed text at open
- `round44-chain.log` - review round 5's plan verified: the DEF-APPLY-90 Galata proof on the round-43 build (expected to fail), 2 of 2 mutations, build 1.0.6, lint, jest, pytest, Galata both groups, graph refresh
- `galata-106-proof-def-apply-90.log`, `round44-mutations.log`, `mutation-baseline-round44-watcher.spec.log`, `mutation-shadow-*.log`, `build-106-round44.log`, `lint-106-round44.log`, `jest-106-round44.log`, `galata-106-round44-*.log` - the round-44 chain's parts
- `diag-apply90.log`, `diag-apply90-dom.log` - a scratch Galata run on the round-44 build: after typing in the editor and opening the preview on a fresh page, the preview tab carries jp-AdvancedMd-tabBlocked and the typed text survives a write (the fix works in the lab; the round-44 case sat under a beforeEach that opened the preview first)
- `galata-106-round44-def-apply-90-repeat.log` - the misplaced round-44 case, 3 of 3 failed in isolation for that reason; `galata-106-round44-forced-render-repeat.log` - the siblings forced-render case (DEF-APPLY-37-1, the known intermittent) 3 of 3 passed in isolation after its one failure in the round-44 notes group
- `round45-chain.log` - the DEF-APPLY-90 Galata case in its own describe: proof against a build without the guard (`build-106-round45-prefix.log`, `galata-106-proof-def-apply-90.log`, expected to fail), then the guard restored and built (`build-106-round45.log`), lint, jest, pytest, both Galata groups (`lint-106-round45.log`, `jest-106-round45.log`, `galata-106-round45-*.log`), graph refresh; `galata-106-round45-forced-render-repeat.log` - the siblings forced-render case (DEF-APPLY-37-1) 3 of 3 passed in isolation after its one failure in the round-45 notes group
- `jest-notes-panel-acc-152.log`, `galata-108-proof-acc-notes-152.log` - ACC-NOTES-152 (the note field grows with its content): the panel spec with the two new cases, and the Galata case against the installed 1.0.8, which failed with the box at 96 px after seven lines
- `round46-chain.log` - ACC-NOTES-152: build 1.0.9 from the published 1.0.8 (`build-109-round46.log`), lint (`lint-109-round46.log`), jest and pytest (`jest-109-round46.log`), both Galata groups (`galata-109-round46-*.log`), graph refresh; ends with CHAIN-DONE
- `jest-acc-153-def-91.log` - the notes and notes-panel specs with the ACC-NOTES-153 and DEF-NOTES-91 cases
- `round47-chain.log` - ACC-NOTES-153 and DEF-NOTES-91: the two Galata cases against the installed round-46 build (`galata-109-proof-acc-153-def-91.log`, both expected to fail), build 1.0.9 again (`build-109-round47.log`), lint, jest and pytest, both Galata groups (`galata-109-round47-*.log`), graph refresh; ends with CHAIN-DONE
- `jest-acc-154.log` - the marks, anchor and notes specs with the ACC-NOTES-154 cases (the escaped single-line marker form, the table-row placement, the note rewrite on a table row)
- `round48-chain.log` - ACC-NOTES-154: the Galata table case against the installed round-47 build (`galata-109-proof-acc-154.log`, expected to fail), build 1.0.9 again (`build-109-round48.log`), lint, jest and pytest, both Galata groups (`galata-109-round48-*.log`), graph refresh; ends with CHAIN-DONE
- `galata-109-round48-forced-render-repeat-1.log` to `-3.log` - the forced-render sibling case (DEF-APPLY-37-1, the rejected intermittent) three times alone on the round-48 build after it failed once in the notes group: 2 of 3 passed, the failure again the scroll position after the second render
- `round49-chain.log` - the review fixes of a82457f (table detection inside a quote and after a text line, the leading pipe on a row without one, the row focused from a passage click, the hidden-tab measurement of the note field): the two new Galata table cases against the installed round-48 build (`galata-109-proof-round49.log`, expected to fail), build 1.0.9 again (`build-109-round49.log`), lint (`lint-109-round49.log`), jest and pytest (`jest-109-round49.log`), both Galata groups (`galata-109-round49-*.log`), graph refresh; ends with CHAIN-DONE
- `round50-chain.log` - ACC-NOTES-155 (a mark's status, open or closed; closed marks hidden until the panel shows them): the Galata case against the installed round-49 build (`galata-109-proof-acc-155.log`, expected to fail), build 1.0.9 again (`build-109-round50.log`), lint (`lint-109-round50.log`), jest and pytest (`jest-109-round50.log`), the notes group and the rest group (`galata-109-round50-*.log`), graph refresh; ends with CHAIN-DONE
- `round51-chain.log` - ACC-HILITE-156 (the removed-text ghost holds for its 500 ms rise, then deletes beside the typing) with the two Galata notes cases fixed on the test side after chain 50 (the header order with the Show closed control, the Close locator): the ACC-HILITE-156 Galata case against the installed round-50 build (`galata-109-proof-acc-156.log`, failed on the 750 ms hold as expected), the two notes cases on that build (`galata-109-check-notes-fixes.log`: ACC-NOTES-139 passed, ACC-NOTES-155 failed), then `make install` (`build-109-round51.log`); the chain stopped there and chain 52 replaced it
- `round52-chain.log` - review round 2 fixes (the table run ends where the quote depth changes, a draft held on a hidden closed row released, a hidden closed mark asked for shows the closed marks, the Show closed control out of the minimap strip, the closed wash and row colours), ACC-NOTES-157 (Copy mark ID on a row's context menu) and the ACC-HILITE-156 ghost hold already proven failing on the round-50 build (`galata-109-proof-acc-156.log`): the ACC-NOTES-155 and ACC-NOTES-157 Galata cases against the installed round-50 build (`galata-109-proof-round52.log`, expected to fail), build 1.0.9 again (`build-109-round52.log`), lint (`lint-109-round52.log`), jest and pytest (`jest-109-round52.log`), the notes group and the rest group (`galata-109-round52-*.log`), graph refresh; ends with CHAIN-DONE
- `jest-109-merge-dev.log` - the whole jest suite and lint run while the three-way merge was cut (2026-09-11), before chain 53: jest 660 of 660
- `round53-chain.log` - the three-way merge over unsaved edits (ACC-APPLY-10, 11, 13, 43 reopened, ACC-APPLY-105 narrowed to the no-shadow hold) with the review round 3 fixes (the note field keeps the browser menu, the leading pipe on a list-item table row): the four merge Galata cases against the installed round-52 build (`galata-109-proof-round53.log`, expected to fail), build 1.0.9 again (`build-109-round53.log`), lint (`lint-109-round53.log`), jest and pytest (`jest-109-round53.log`), the notes group and the rest group (`galata-109-round53-*.log`), graph refresh; ends with CHAIN-DONE; the rest group had 2 failures: the Reload from Disk over a conflict case (the Context reports no new revision after a merge, fixed in round 54) and the events intermittent of round 23 while jest ran beside the Galata run
- `jest-109-jitter-dev.log` - the whole jest suite, prettier and eslint run while the animation jitter was cut (2026-09-11): jest 657 with the schema spec failing on a missing type field, fixed before chain 54
- `round54-chain.log` - the conflict released when the document comes to hold the file's text (model.contentChanged) and the animation jitter (ACC-ANIM-158, speed default 75) on the merge build: the three proof cases against the installed round-53 build (`galata-109-proof-round54.log`, expected to fail), build 1.0.9 again (`build-109-round54.log`), lint (`lint-109-round54.log`), jest and pytest (`jest-109-round54.log`), the notes group and the rest group (`galata-109-round54-*.log`), the events intermittent three times alone (`galata-109-round54-events-repeat-1.log` to `-3.log`), graph refresh; ends with CHAIN-DONE; the notes group had 2 failures, the settings-editor jitter case (the evaluate handed querySelector an unescaped colon, test-side fix) and the forced-render intermittent
- `galata-109-round54-settings-jitter-rerun.log` - the ACC-ANIM-158 settings-editor case alone on the round-54 build after the selector fix: 1 passed
- `galata-109-round54-forced-render-repeat-1.log` to `-3.log` - the forced-render intermittent (siblings.spec.ts 'runs every listener twice for one change without a sibling misbehaving') three times alone on the round-54 build: 3 passed
- `round55-chain.log` - review round 4 fixes on the merge build (the conflict tooltip naming what a reload and a save drop, alignLines in src/diff.ts splitting a run past MAX_LCS_LINES at a unique line so a long document's write is two hunks and not one, the watcher fixture ordering contentChanged before the dirty flag as YFile does): the DEF-HILITE-88 Galata case against the installed round-54 build as a baseline (`galata-109-proof-round55.log`), build 1.0.9 again (`build-109-round55.log`), lint (`lint-109-round55.log`), jest and pytest (`jest-109-round55.log`), the notes group and the rest group (`galata-109-round55-*.log`), the forced-render and events intermittents three times alone each (`galata-109-round55-forced-render-repeat-*.log`, `galata-109-round55-events-repeat-*.log`), graph refresh; ends with CHAIN-DONE
- `round56-chain.log` - review round 7's confirmed MAJOR on the merge build: the drop test's unit raised from one word hunk to the run of the file's hunks on one line, so an agent's rewrite of a line the reader is editing is dropped whole instead of woven into their sentence word by word; the new whole-line Galata case against the installed round-55 build (`galata-109-proof-round56.log`, expected to fail on the per-hunk merge), build 1.0.9 again (`build-109-round56.log`), lint (`lint-109-round56.log`), jest and pytest (`jest-109-round56.log`), the notes group and the rest group (`galata-109-round56-*.log`), the forced-render and events intermittents three times alone each (`galata-109-round56-forced-render-repeat-*.log`, `galata-109-round56-events-repeat-*.log`), graph refresh; ends with CHAIN-DONE
- `round57-chain.log` - review round 8's plan: the Close control tooltip no longer promises to hide a mark the panel keeps listed (a jest case in notes-panel.spec, failing first) and the ACC-APPLY-10 and ACC-APPLY-11 statements re-worded to the line-run drop unit; no proof case, since no Galata case asserts that tooltip; build 1.0.9 again (`build-109-round57.log`), lint (`lint-109-round57.log`), jest and pytest (`jest-109-round57.log`), the notes group and the rest group (`galata-109-round57-*.log`), the forced-render and events intermittents three times alone each (`galata-109-round57-forced-render-repeat-*.log`, `galata-109-round57-events-repeat-*.log`), graph refresh; ends with CHAIN-DONE
- `jest-109-round57-rerun.log` - jest and pytest re-run after a scratch spec left in `src/__tests__` failed the round-57 chain's own jest step on an unused import: 672 of 672 and 29 of 29
- `round58-chain.log` - DEF-APPLY-95: a merge whose applied hunks would change the parity of the fence lines is refused whole, so a delimiter pair written around more than one line never lands its opening without its closing; two jest cases in watcher.spec are the failing-first proof and no Galata case asserts the fence policy; build 1.0.9 again (`build-109-round58.log`), lint (`lint-109-round58.log`), jest and pytest (`jest-109-round58.log`), the notes group and the rest group (`galata-109-round58-*.log`), the forced-render and events intermittents three times alone each (`galata-109-round58-forced-render-repeat-*.log`, `galata-109-round58-events-repeat-*.log`), graph refresh; ends with CHAIN-DONE
- `round59-chain.log` - review round 9: the DEF-APPLY-95 parity guard replaced by splitsPair, which refuses a merge whose dropped rewrite changes the count of a fence line, an opening comment or a closing one (DEF-APPLY-96), and the conflict tooltip's promise that any other part was applied deleted; four jest cases are the failing-first proof and no Galata case asserts either; build 1.0.9 again (`build-109-round59.log`), lint (`lint-109-round59.log`), jest and pytest (`jest-109-round59.log`), the notes group and the rest group (`galata-109-round59-*.log`), the forced-render and events intermittents three times alone each (`galata-109-round59-forced-render-repeat-*.log`, `galata-109-round59-events-repeat-*.log`), graph refresh; ends with CHAIN-DONE
- `round60-chain.log` - ACC-NOTES-159: a note entry the reader typed nothing into is cancelled when they click away from the notes panel or select another mark, the marking left as it is (NotesPanel._form focusout, NotesPanel.selectMark); the new Galata case against the installed 1.0.9 build (`galata-109-proof-acc-159.log`, expected to fail and failing with the field still open after the click away), build 1.0.10 (`build-109-round60.log`), lint (`lint-109-round60.log`), jest and pytest (`jest-109-round60.log`: 683 of 683 and 29 of 29), the notes group 125 of 125 and the rest group 51 of 51 with 2 skipped and 1 failure (`galata-109-round60-*.log`), the forced-render and events intermittents three times alone each (`galata-109-round60-forced-render-repeat-*.log`, `galata-109-round60-events-repeat-*.log`), graph refresh; ends with CHAIN-DONE; the rest group's failure was events.spec 'applies a write made by temporary file and rename', its first in ten rounds
- `round60-repeats.log` - the two cases that failed in the round-60 chain, each three times alone on the same build: the rename case 3 of 3 passed (`galata-109-round60-rename-repeat-1.log` to `-3.log`), so its failure under the parallel rest group is an intermittent; the forced-render case (the rejected intermittent DEF-APPLY-37-1, signature scroll diff 1672 against the 50 the case allows) 2 of 3 more passed (`galata-109-round60-forced-render-repeat-4.log` to `-6.log`), 3 of 6 over the round; the forced-render document carries no mark and opens no note entry, so nothing of the ACC-NOTES-159 delta runs in it
- `round61-chain.log` - review round 12's confirmed MAJOR fixed (the ACC-NOTES-159 focusout listener moved from the note form to the panel node), DEF-NOTES-97 closed (the entry carries whether it stands on a document note) and ACC-COPY-160 added (Copy Content: the rendered document on the clipboard as basic HTML): the new Galata spec against the installed round-60 build (`galata-109-proof-acc-160.log`, expected to fail and failing on the missing Copy Content entry), build 1.0.10 (`build-109-round61.log`), lint (`lint-109-round61.log`), jest and pytest (`jest-109-round61.log`: 696 of 696 and 29 of 29), the notes group 103 of 125 and the rest group 52 of 53 with 2 skipped (`galata-109-round61-*.log`), the two intermittents three times alone each, graph refresh; ends with CHAIN-DONE. The notes group's 22 failures are this build's own defects, both fixed for round 62: 21 of them the note entry closing the instant it opened, because a rebuild of the rows raises a focusout the browser reports as a focus leaving the panel and the listener read that as the reader leaving (jsdom raises no such event, so no jest case saw it), and 1 the Copy Content entry carrying no icon, which ACC-NOTES-120 requires of every entry this extension offers. The rest group's one failure was the ACC-COPY-160 case's own assertion, which spelled the link target without the trailing slash the renderer writes; the copied HTML itself was clean
- `round62-chain.log` - the round-61 defects answered (the ACC-NOTES-159 listener deciding one tick after the focusout from where the focus ended, and JupyterLab's copy icon on the Copy Content entry, which ACC-NOTES-120 requires): build 1.0.10 again (`build-109-round62.log`), lint (`lint-109-round62.log`, exit 1 on prettier's formatting of this file, corrected after), jest and pytest (`jest-109-round62.log`: 696 of 696 and 29 of 29), the notes group 123 of 125 and the rest group 53 of 53 with 2 skipped (`galata-109-round62-*.log`), both intermittents 3 of 3 alone, graph refresh; ends with CHAIN-DONE. ACC-COPY-160's Galata case passes here, which is Copy Content proven end to end. The notes group's two failures are the failing-first proof of the rule round 63 carries: a click on a part of the panel that takes no focus - a row's head in the ACC-NOTES-159 case, the controls strip in DEF-NOTES-77's double click - moves the focus to the page body, which a rule reading the focus alone cannot tell from the reader leaving the panel
- `round63-chain.log` - ACC-NOTES-159 rewritten on what the browser does: the reader's pointer landing outside the panel cancels an empty note entry, and so does the focus moving to something outside it, while a focus that lands nowhere is neither, since a click on a part of the panel that takes no focus and the panel's own rebuild both report that way (NotesPanel.handleEvent, onAfterAttach, onBeforeDetach); build 1.0.10 again (`build-109-round63.log`), lint 0 errors and 12 warnings (`lint-109-round63.log`), jest and pytest (`jest-109-round63.log`: 698 of 698 and 29 of 29), the notes group 125 of 125 and the rest group 53 of 53 with 2 skipped (`galata-109-round63-*.log`), the forced-render intermittent 2 of 3 and the events intermittent 3 of 3 alone, graph refresh; ends with CHAIN-DONE. The two Galata cases the round-62 rule failed, ACC-NOTES-159's own click-lands step and DEF-NOTES-77's double click, both pass here
- `round64-chain.log` - review round 13's plan applied: the copy drops what the preview hides (the mermaid source JupyterLab keeps beside a drawn diagram, a caption written for a screen reader, anything marked hidden), a table row of the text form ends after its last cell, and `removeEmptyDocument` keeps the marker of a closed document mark (DEF-NOTES-97's file-side half); four jest cases are the failing-first proof and no Galata case asserts any of the three, the suite's documents holding no diagram, no hidden block and no closed document mark; build 1.0.10 again (`build-109-round64.log`), lint 0 errors and 12 warnings (`lint-109-round64.log`), jest and pytest (`jest-109-round64.log`: 702 of 702 and 29 of 29), the notes group 125 of 125 and the rest group 53 of 53 with 2 skipped (`galata-109-round64-*.log`), both intermittents 3 of 3 alone, graph refresh; ends with CHAIN-DONE
- `round65-chain.log` - review round 14's plan applied, both changes in src/content.ts: the kept attributes lost `title`, which is where this extension writes a mark's notes, so a copy made for somebody else no longer carries the reader's own notes, and a row's cells are read from its elements, so the line breaks a renderer writes between cells stop turning the tab that parts them into a line break; two jest cases are the failing-first proof and no Galata case asserts either, the copy document holding no note on its mark and its table written on one line; build 1.0.10 again (`build-109-round65.log`), lint 0 errors and 12 warnings (`lint-109-round65.log`), jest and pytest (`jest-109-round65.log`: 704 of 704 and 29 of 29), the notes group 125 of 125 and the rest group 53 of 53 with 2 skipped (`galata-109-round65-*.log`), both intermittents 3 of 3 alone, graph refresh; ends with CHAIN-DONE
- `round66-chain.log` - review round 15's plan applied: src/content.ts drops `.jp-RenderedMermaid-Details > pre`, the parser's message a failed mermaid diagram folds into a details the reader has to open, so the copy carries the source they see and not the error they do not; the jest case for a failed diagram now uses the markup @jupyterlab/mermaid builds and failed first on it; the README calls the format basic HTML in both its lines and the ACC-COPY-160 records cite this campaign's own numbers; build 1.0.10 again (`build-109-round66.log`), lint 0 errors and 12 warnings (`lint-109-round66.log`), jest and pytest (`jest-109-round66.log`: 704 of 704 and 29 of 29), the notes group 125 of 125 and the rest group 53 of 53 with 2 skipped (`galata-109-round66-*.log`), both intermittents 3 of 3 alone, graph refresh; ends with CHAIN-DONE
- `round67-chain.log` - review round 16's plan applied: `ChangeAnimator.finish` with `LiveViewController.completeTyping` called by the copy command, so a change still being typed in is put at its end before Copy Content reads the view, and `NotesPanel.handleEvent` exempting a Lumino menu from the leave rule, which restores `Copy mark ID` on a row right-clicked while an empty entry stands; two jest cases are the failing-first proof, each shown red with its own fix neutered; build 1.0.10 again (`build-109-round67.log`), lint 0 errors and 12 warnings (`lint-109-round67.log`), jest and pytest (`jest-109-round67.log`: 706 of 706 and 29 of 29), the notes group 125 of 125 and the rest group 53 of 53 with 2 skipped (`galata-109-round67-*.log`), both intermittents 3 of 3 alone, graph refresh; ends with CHAIN-DONE
- `round68-chain.log` - review round 17's plan applied: `ChangeAnimator.finish` completes the added text alone, since round 16's version also ended the removal ghosts and pressing Copy Content mid-change took the struck text off the reader's screen, and a block div is no longer unwrapped, so two raw HTML lines an author wrote arrive on two lines; two jest cases are the failing-first proof; build 1.0.10 again (`build-109-round68.log`), lint 0 errors and 12 warnings (`lint-109-round68.log`), jest and pytest (`jest-109-round68.log`: 708 of 708 and 29 of 29), the notes group 125 of 125 and the rest group 53 of 53 with 2 skipped (`galata-109-round68-*.log`), both intermittents 3 of 3 alone, graph refresh; ends with CHAIN-DONE
- `round69-chain.log` - review round 18's plan applied, the round-17 div fix finished: a block div is a line of its own in the text form as it is a block in the HTML, and the ACC-COPY-160 mechanism record, which still described the unwrap rule round 17 abandoned, now describes the code and the animator handshake; the jest case for two raw HTML lines lost the whitespace it leaned on and failed first on it; build 1.0.10 again (`build-109-round69.log`), lint 0 errors and 12 warnings (`lint-109-round69.log`), jest and pytest (`jest-109-round69.log`: 708 of 708 and 29 of 29), the notes group 125 of 125 and the rest group 53 of 53 with 2 skipped (`galata-109-round69-*.log`), both intermittents 3 of 3 alone, graph refresh; ends with CHAIN-DONE. Round 18 before it was the first clean review round of this delta: four SHIP verdicts, nothing confirmed above MINOR
- `round70-chain.log` - the last plan of the loop, from a clean round 19 (four SHIP verdicts and zero confirmed findings, with round 18 clean before it, which closes the loop on two consecutive clean rounds): the copy keeps the width and height an image was given, since a mail client otherwise draws it at its full pixel size, and a doc line above `preview()` that the delta itself made false is gone; one jest case is the failing-first proof; build 1.0.10 again (`build-109-round70.log`), lint 0 errors and 12 warnings (`lint-109-round70.log`), jest and pytest (`jest-109-round70.log`: 709 of 709 and 29 of 29), the notes group 125 of 125 and the rest group 53 of 53 with 2 skipped (`galata-109-round70-*.log`), both intermittents 3 of 3 alone, graph refresh; ends with CHAIN-DONE
- `publish-1.0.9.log`, `publish-1.0.10.log` and `publish-1.0.11.log` - the three releases of this campaign through `make publish`, npm and PyPI in one run each; the version the Makefile's `increment_version` produced is in the file name
- `test-suite-extra.log` - jest 709 of 709 in 14 suites and pytest 32 of 32 while the ten Markdown siblings were made required dependencies of this package and `tests/test_packaging.py` was written; the three new pytest cases are the difference from 29
- `def-copy-99-test.log` and `def-copy-99-install.log` - DEF-COPY-99, Copy Content copying the whole document while a passage is selected: jest 711 of 711 in 14 suites and pytest 32 of 32, then `make install` for the build the browser judged it on
- `test-race-baseline.log` - jest 711 of 711 and pytest 32 of 32 taken as the baseline before the race sweep of `src/`, so a later count can be read against a known-green tree
- `galata-round71.log` - both groups on the round-71 build, the one before `selectedContent` nested its shell inside a clone of the rendered root: the notes group 125 of 125 and the rest group 59 of 59; ends with GALATA-DONE
- `build-round72.log` and `test-round72.log` - `make install` for the round-72 build and its jest 711 of 711 with pytest 32 of 32; this is the build where the selected-list case first passed
- `test-round73.log` - jest 712 of 712 and pytest 32 of 32 after review round 3's plan
- `round73-chain.log` - the round-73 chain produced no browser verdict: `make install` succeeded and the version was restored to 1.0.10, but both Galata groups refused to start with `http://localhost:8911/lab is already used`, because another Galata run still held the port; Galata runs one at a time on 8911, and the chain ends with CHAIN-DONE over two starts that never happened
- `galata-round73.log` - both groups re-run on the round-73 build once the port was free: the notes group 125 of 125 and the rest group 60 of 60; ends with GALATA-DONE
- `galata-round74-group2.log` - the rest group alone on the round-74 build, 58 of 58; ends with GALATA2-DONE
- `test-round74.log` - jest 716 of 716 and pytest 32 of 32 after review round 4's plan
- `round74-chain.log` - the round-74 chain: `make install`, the version restored to 1.0.10, lint 0 errors and 12 warnings, the notes group 125 of 125, and the rest group 1 failed with 57 passed; ends with CHAIN74-DONE. The failure is `tests/events.spec.ts:130` 'applies two writes fifty milliseconds apart as one change', which expected one apply and saw two. It is the race this campaign then removed rather than re-ran: the case separated its two writes by a 50 ms sleep that had to land inside the server's 0.1 s coalescing window, and a sleep names a floor and never a ceiling, so under load the two writes are two changes and the server is right. The timing claim moved to the layer that can set the window, `tests/test_routes.py::test_burst_writes_coalesce`, and the browser case now asserts only where the preview settles
- `racefix-chain.log` - both groups after that fix, on a build carrying it: the notes group 125 of 125 and the rest group 58 of 58; ends with RACEFIX-DONE
- `round5-chain.log` and `round5-build.log` - two background `make install` runs for review round 5's plan, both killed by the harness watchdog for low memory while `free -g` showed 476 GB available and the cgroup carried no limit, the watchdog reading 149 GiB of page cache as pressure; each file holds only its `== make install` line, and each killed run had bumped `package.json` to 1.0.11 before dying, restored with `git checkout -- package.json package-lock.json yarn.lock`. The build and both Galata groups were then run in the foreground and left no log of their own: jest 717 of 717 in 14 suites, pytest 32 of 32, the notes group 125 of 125 and the rest group 58 of 58
- `round7-test.log` - jest 717 of 717 in 14 suites and pytest 32 of 32 after review round 7's plan, which moved two register evidence lines and one code comment and no executable statement, so no build and no Galata run were needed
- `round8-test.log` - jest 717 of 717 in 14 suites and pytest 32 of 32 after review round 8's plan, which reverted two register clauses round 7 had added, replaced the ACC-EVENT-85 test hint that still prescribed the 50 ms race, and corrected one criterion id in a pytest docstring; no executable line moved, so no build and no Galata run were needed
- `round10-test.log` - jest 717 of 717 in 14 suites and pytest 32 of 32 after review round 10's plan, which deleted two false rank claims from ACC-COMPAT-162: that this extension's context-menu entries take no rank another extension uses (src/index.ts:484 registers at rank 10 and the SVG export sibling registers on .jp-RenderedHTMLCommon at ranks 10 and 11, recorded in the same change set at docs/defects.md:858), and an enumeration of its own ranks that omitted the per-mark rank 30 + index; register only, no executable line moved
- `round12-test.log`, `build-round12.log`, `galata-round12-group1.log` and `galata-round12-group2.log` - review round 12's MAJOR, the first executable change since round 5: a fenced code block dragged inside its own `code` element reached the clipboard as a bare `code` with no `pre`, and `code` outside `pre` carries no white-space rule in any browser's own stylesheet, so the reader's program arrived on one line. Proved failing first by a jest case that measured the copy as `<code>one = 1\ntwo = 2\n</code>`. `ROWS` became `FRAMED`, a map from the tag to the parent that carries its meaning, because the parent has to be named per tag: the review's own remedy looked for `closest('table, pre')` as a group, which a probe measured turning an inline code phrase inside a table cell into `<table><code>make install</code></table>`, a table the receiving parser foster-parents out. Both halves mutation-proved, one case each. jest 719 of 719 in 14 suites, pytest 32 of 32, lint 0 errors with 12 pre-existing warnings, the build restored from 1.0.11 to 1.0.10, the notes group 125 of 125 and the rest group 58 of 58
- `round13-test.log` - jest 719 of 719 in 14 suites and pytest 32 of 32 after review round 13, which changed no code: it filed DEF-COPY-105, the two measured residuals of round 12's framing lookup as one cause - a drag lying wholly inside the green span painted on added text loses the frame entirely, and a drag inside a fence carrying no line of its own is given one it does not need - with both proposed remedies measured and refused, one of them because it would put the ghost of deleted words on the clipboard
- `round14-test.log` - jest 719 of 719 in 14 suites, pytest 32 of 32 and lint 0 errors with 12 pre-existing warnings after review round 14, which moved no executable line: it deleted the two narrowing clauses round 13 had written into DEF-COPY-105, both measured over-broad against the framing map, which holds row tags and CODE only, so every span ancestor loses the frame and the permanent mark span has no window at all; it filed DEF-COPY-106, the fade timer unwrapping decorations with no regard for a live selection, so Copy Content at that instant answers with the whole document or a passage cut back to where the paint began; and it corrected two words of the Range-shim comment in content.spec.ts, since document.createRange does return setStart
- `round15-test.log` - jest 719 of 719 in 14 suites and pytest 32 of 32 after review round 15, which moved no code: it corrected two false particulars this loop had written into DEF-COPY-106 - HEAD does carry `src/content.ts`, 185 lines and mode M, the earlier probe having misread `grep -c` returning exit 1 on zero matches, and the only `replaceWith` in the tree is in `src/content.ts` while `src/highlight.ts` unwraps with `replaceChild` - and filed DEF-COPY-107, the copy ignoring the reader's selection when a change lands while the context menu is open, every particular read from the line cited in our own source and in the Lumino and rendermime bundles
- `round16-test.log` - jest 719 of 719 in 14 suites, pytest 32 of 32 and lint 0 errors with 12 pre-existing warnings after review round 16, the last round of the loop: it deleted six false words from the ACC-COMPAT-161 mechanism record, which claimed dev was the only optional-dependencies group while pyproject.toml declares dev at :46 and test at :50; no replacement enumeration was written, since one would stale again at the next group
- `defectfix-test.log`, `build-defectfix.log`, `galata-defectfix-group1.log` and `galata-defectfix-group2.log` - the four open COPY defects closed with three fixes, each proved failing first and mutation-proved. `selectedContent` now decides on what the copy would leave rather than on its words alone, so an image, a formula or a diagram selected by itself reads as a selection (DEF-COPY-104); it walks the frame lookup up through the ancestors the copy dissolves, so a fenced block dragged inside the green paint on added text keeps the `pre` that says its line breaks are the content (DEF-COPY-105); and `_restoreSelection` exempts an open context menu from its focus guard, so the restore that already runs on a render and on the settled signal reaches the reader's selection instead of refusing, which closes both the fade-unwrap and the open-menu losses (DEF-COPY-106 and 107). Each mutation fails exactly its own case. jest 722 of 722 in 14 suites, pytest 32 of 32, lint 0 errors with 12 pre-existing warnings, the build restored from 1.0.11 to 1.0.10, the notes group 125 of 125 and the rest group 58 of 58
- `round17-test.log` - jest 722 of 722 in 14 suites and pytest 32 of 32 after review round 17, which moved no code: ACC-COPY-160's third KNOWN LIMIT still advertised the behaviour the DEF-COPY-104 fix had just removed, so it now states the rule the code implements - a selection the copy leaves a picture in travels as that picture, and one it leaves neither words nor a picture in reads as no selection - rather than another list that would stale again
- `round18-test.log` - jest 722 of 722 in 14 suites and pytest 32 of 32 after review round 18, which moved no code: it reverted round 17's KNOWN LIMIT clause rather than wording it a third time, because measurement showed it false in two directions - a paragraph holding words and an inline image travels as the paragraph with text "see here", not as the picture, and the installed JupyterLab sets `enableAssistiveMml`, so a formula carries text rather than none; and it cut a false clause from the `selecting` helper's comment, since a real jsdom range obtained through `Document.prototype.createRange` does name a block-level common ancestor and does carry `cloneContents`
- `round19-20.log` - the adversarial review's terminal state, reached at round 20 rather than by a cap: the adjudicator returned STOP with the reason that the loop had begun generating its own work, so re-modelling is the answer rather than another round. Round 20's six findings were all MINOR and all immaterial, and its headline one - a selected mermaid diagram putting JupyterLab's hidden source on the clipboard - was refuted on reachability by a skeptic who measured every shape of reader selection over the real rendered DOM and found the source dropped in all of them, the leak needing a programmatic range no click or keystroke can build. Over twenty rounds no CRITICAL was ever confirmed. The STOP payload's 44-entry revert list is the whole closure log, the original feature work included, and was deliberately not applied
- `def102-103-test.log`, `def101-test.log`, `def102-test.log`, `build-alldefects.log`, `galata-alldefects-group1.log` and `galata-alldefects-group2.log` - the last three defects closed, leaving the register with none open. DEF-COPY-103: the copy reads the recorded selection from `NotesController` before `completeTyping` can disturb it and, when the browser has no usable selection, turns those offsets back into a range through the new `anchor.ts` `renderedToDomRange` and the new `content.ts` `contentOfRange`. DEF-APPLY-101: the unguarded cost was measured first as the review required, with a jest case holding a `contents.get` answer from before a save and resolving it after `saveState` reported completed - the reader's just-saved word was reverted and the watcher reported `applied`, reproducing the traced defect exactly; `src/watcher.ts` now counts completed saves and a read whose count moved applies nothing. DEF-COPY-102: `src/animate.ts` writes each frame by append or delete rather than by whole value, testing the relationship rather than assuming it, so a live range inside the typed node keeps its offsets; the failing-first case held a real range across a frame and saw its boundary dragged from 4 to 0. Each fix mutation-proved to fail its own case alone. jest 725 of 725 in 14 suites, pytest 32 of 32, lint 0 errors with 12 pre-existing warnings, the build restored from 1.0.11 to 1.0.10, the notes group 125 of 125 and the rest group 58 of 58
- `jest-108.log`, `lint-108.log`, `prettier-108.log`, `build-108.log`, `galata-108-single.log` and `galata-84-1-full.log` - DEF-NOTES-84-1, filed first as DEF-NOTES-108 and recorded as the regression of DEF-NOTES-84 once that parent was found: a heading of one or two words could not be marked, because the paragraph-mark link JupyterLab appends to every heading joined the heading's last word in the captured text. `jest-108.log` is the full jest run after `captureText` stopped reading that link, `lint-108.log` and `prettier-108.log` the lint and format checks, `build-108.log` the `make install` of 1.0.12 (the version files restored to 1.0.11 afterwards), `galata-108-single.log` the new Galata case alone on that build, and `galata-84-1-full.log` the full Galata suite on the same build
- `galata-109-before.log`, `jest-109.log`, `prettier-109.log`, `jest-84-1-mutation.log`, `build-84-1-mutation.log`, `galata-84-1-mutation.log`, `build-84-1-109.log` and `galata-84-1-109-full.log` - DEF-NOTES-109, found by round 1 of the adversarial review of DEF-NOTES-84-1: a triple-click on a heading or a paragraph marked the block below it as well, because `boundaryOffset` counted the text of the element a range ends before. `galata-109-before.log` is the new triple-click Galata case failing on build 1.0.12, which lacked the change, `jest-109.log` and `prettier-109.log` the full jest run and the formatting after the change. The three `mutation` logs prove the DEF-NOTES-84-1 tests catch their defect: the anchor-link check removed from `captureText`, four jest cases fail and the Galata case opens no note field on that build. `build-84-1-109.log` is the build of 1.0.13 with both fixes and `galata-84-1-109-full.log` the full Galata suite on it; the version files were restored to 1.0.11 after each build
- `review-84-1-round1.log` and `review-84-1-rounds2-3.log` - the adversarial review of the two fixes, the returned payloads of the workflow runs `wf_a3db6f6e-26e` and `wf_eee663ec-215` with the four lenses architect, ux-designer, bug-hunter and slop-hunter. Round 1 returned one planned change, the DEF-NOTES-109 end boundary, and rounds 2 and 3 were adjudicated clean, which ends the review as SHIP. Each file carries the round history, the refuted findings with their evidence and the one deferred finding: Add note on words the source match cannot find, such as a GitHub alert title alone, still writes nothing and shows no message
- `publish-1.0.12.log` - the `make publish` of 1.0.12, which carries the DEF-NOTES-84-1 and DEF-NOTES-109 fixes and the lint fix that cleared the 12 `@jupyter/eslint-plugin` warnings: the build, `jupyterlab_advanced_markdown_viewer_extension@1.0.12` published to npm, the wheel and sdist uploaded to PyPI, and the post-publish commit `7fdd485` of `package.json` and `package-lock.json`
- `jest-163.log`, `lint-163.log`, `mutation-163.log`, `jest-163-full.log`, `build-163.log`, `galata-163-single.log`, `galata-163-full.log`, `build-163-fix.log`, `galata-163-fix-full.log`, `build-163-final.log` and `galata-163-final-full.log` - ACC-COPY-163, Copy link address in the context menu of a link in the preview: the address the link opens with the `_xsrf` token removed, a heading link resolved against the document's own `lab/tree` address. `jest-163.log` the three new `link.spec.ts` cases and the two wiring cases, `mutation-163.log` the `_xsrf` removal taken out so the file-link case fails alone, `jest-163-full.log` the full run at 733 of 733, `build-163.log` the `make install` of 1.0.13, `galata-163-single.log` the new Galata case alone and `galata-163-full.log` the full suite at 186 passed 2 skipped. `build-163-fix.log` rebuilt after round 1 of the review moved the link lookup to `app.contextMenuHitTest`; `galata-163-fix-full.log` is that suite failing whole at 16 s because `make install` had pulled tornado 6.5.9, whose `StaticFileHandler` needs an argument jupyter_server 2.21.0 does not pass, so every static file answered 500; `build-163-final.log` the rebuild with the `heldLink` rename after round 2 and tornado pinned back to 6.5.8, `galata-163-final-full.log` the full suite on it at 185 passed 2 skipped, the one failure the forced-render repeat case of `siblings.spec.ts`, which has failed the same way in the round-48 to round-60 repeat logs above. The version files were restored to 1.0.12 after each build
- `jest-164-first.log`, `jest-164-second.log`, `jest-164-full.log`, `build-164.log`, `galata-164-notes.log`, `galata-169-single.log`, `review-164-round1.log`, `jest-164-review1.log`, `mutation-164-review1.log`, `build-164-review1.log`, `galata-164-review1.log`, `galata-164-review1-full.log`, `build-169-mutation.log`, `build-169-restored.log` and `mutation-169-growth.log` - the comment thread of a mark, ACC-NOTES-164 to 169, 171 and 172: the first entry is the comment and later entries are replies drawn indented, the row button reads Comment then Reply, every entry carries an edit icon and an x, a row being written offers no Close, Reopen or entry icons, Close and Reopen are eye icons, row buttons use the smaller font, and an opened field is scrolled into the panel. `jest-164-first.log` and `jest-164-second.log` are the first two jest runs of the two notes specs while the tests were being adjusted, at 11 and 3 failures, and `jest-164-full.log` the full run at 743 of 743. `build-164.log` is the `make install` of 1.0.13. `galata-164-notes.log` is the notes suite on it at 115 passed and 1 failed, the failure being the first ACC-NOTES-169 case before it measured boxes in the page and set a 1280 by 480 window, and `galata-169-single.log` that case passing after the change. `review-164-round1.log` is the round 1 payload of the adversarial review, workflow `wf_95d05aca-ca9` with the lenses architect, bug-hunter, ux-designer and slop-hunter: 19 findings, one MAJOR, and a plan of three changes. The changes grow the edit field before it is scrolled into view, stop an edit field from removing a bare document marker when it is left, and keep the reply indent on the edit field of a reply. `jest-164-review1.log` is jest at 744 of 744 after them, `mutation-164-review1.log` the two jest mutations each failing its own case, `build-164-review1.log` the rebuild of 1.0.13, `galata-164-review1.log` the notes suite at 117 of 117 and `galata-164-review1-full.log` the full suite at 190 passed and 2 skipped. `review-164-round2.log` is the confirm round, workflow `wf_36ac40da-87b`, ending as SHIP with two MINOR findings deferred: an edit field taller than the whole panel list opens with its top in view and Save below it, which ACC-NOTES-169 now states, and the two ACC-NOTES-169 Galata cases repeat their setup. The first confirm run lost the ux-designer and slop-hunter reviewers to a usage limit, and the resumed run re-ran those two alone. `build-169-mutation.log` and `mutation-169-growth.log` are the build without the grow call in `NotesPanel._render` and the long-entry Galata case failing on it, Save's bottom edge at 528 against the list's bottom edge at 451, with both ACC-NOTES-169 cases passing again on the restored build, and `build-169-restored.log` the rebuild with it restored. The version files were restored to 1.0.12 after each build
- `lint-164-final.log`, `prettier-1.0.13.log`, `lint-1.0.13.log` and `publish-1.0.13.log` - the release of 1.0.13, which carries Copy link address (ACC-COPY-163) and the comment thread of a mark (ACC-NOTES-164 to 169, 171 and 172). `lint-164-final.log` is the lint check on the finished campaign at 0 errors and 0 warnings, `prettier-1.0.13.log` the release's prettier pass, which changed no file, and `lint-1.0.13.log` the release lint check at exit 0. `publish-1.0.13.log` is the `make publish`: the build, `jupyterlab_advanced_markdown_viewer_extension@1.0.13` published to npm, the wheel and sdist uploaded to PyPI (https://pypi.org/project/jupyterlab-advanced-markdown-viewer-extension/1.0.13/), and the post-publish commit `2cc0455` of `package.json`, `package-lock.json` and `yarn.lock`
- `jest-173.log`, `lint-173.log`, `mutation-173.log`, `build-173.log`, `build-173-base.log`, `build-173b.log`, `build-173c.log`, `galata-173-notes.log`, `galata-173-full.log`, `galata-173-siblings.log`, `galata-173-base-siblings.log`, `galata-173b-siblings.log`, `galata-173b-notes.log`, `galata-173b-full.log`, `galata-173c-notes.log`, `galata-173c-full.log` and `review-173-round1.log` to `review-173-round3.log` - the row controls and the note stamp after 1.0.13: the colour swatch of a row is the button that rolls the other five colours down (ACC-NOTES-173), the header's Show closed control is Show hidden and a toggle with aria-pressed (ACC-NOTES-174), the row's buttons are readable again - text at the base interface size, icons 16 px in 24 px targets, the entry's edit icon and x floated so the note text keeps the full width (ACC-NOTES-165, reopened after the Star Colonel found them too small) - and a note stamp reads on a 24-hour clock in the language the lab is set to rather than the language the browser is set to (ACC-NOTES-175). `jest-173.log` is jest at 747 of 747, `lint-173.log` the lint check at 0 errors and 0 warnings, and `mutation-173.log` the nine mutations, each failing its own case alone: handleEvent not rolling the list up, a render not rebuilding the open list, the buttons back at the smaller size, the chosen colour not handing the focus back, a state change leaving the mark picked, a rebuild preferring the row over the swatch, a state change tearing the list out from under the reader, the stamp keeping the clock its locale would choose, and the language code not being made into a language tag. The three builds are the campaign as reviewed (`build-173.log`), the same tree with the round 1 fixes (`build-173b.log`) and the final tree (`build-173c.log`); `build-173-base.log` is a build of HEAD made to attribute one failure. `review-173-round1.log` to `review-173-round3.log` hold the adversarial review with the lenses architect, bug-hunter, ux-designer and slop-hunter (workflows `wf_0ddd5dee-bb2`, `wf_14e16891-e25`, `wf_874c0596-45f`): rounds 1 and 2 returned PLAN, four changes in all, and round 3 returned SHIP with six MINOR findings, none material, of which three were acted on anyway - the tag conversion pinned directly because with LANG unset the rendered value cannot tell a converted tag from a dropped one, a false clause in the screenshot suite corrected, and an assertion on a deleted class removed. On the final build the notes suite is 118 passed (`galata-173c-notes.log`) and the full suite 190 passed, 2 skipped and 1 failed (`galata-173c-full.log`). The siblings logs are one test, the forced render holding the reader's place: it failed inside the full suite on two builds and passed 3 of 3 alone on one of them and 2 of 2 on a build of HEAD, which is why it is filed as DEF-COMPAT-110 - a race, not a regression of this campaign. The version files were restored to 1.0.13 after each build
- `jest-172b.log`, `jest-172b-r1.log`, `jest-172b-r2.log`, `lint-172b.log`, `mutation-172b.log`, `build-172b.log`, `build-172c.log`, `galata-172b-notes.log`, `galata-172b-full.log`, `galata-172c-prefix-probe.log`, `galata-172c-notes.log`, `galata-172c-full.log` and `review-172b-round1.log` to `review-172b-round3.log` - the row controls after 1.0.14, from the Star Colonel's rulings of 2026-09-15: the eye shows whether the mark is visible (ACC-NOTES-172), replies are listed one after another without an indent (ACC-NOTES-171), the x deletes the last note only (ACC-NOTES-167), a closed row carries no word closed (ACC-NOTES-155), the edit icon and the x of a note are drawn only while that note is hovered or an icon holds the focus (ACC-NOTES-176), and the trash stays at the far right (ACC-NOTES-132). `jest-172b.log` is 748 of 748 on the tree as reviewed, `jest-172b-r1.log` and `jest-172b-r2.log` the same count after review rounds 1 and 2; `lint-172b.log` is stylelint, prettier and eslint clean; `mutation-172b.log` holds eleven mutations on the tree as reviewed, each caught - mutation 3, the reply indent restored, was caught by a jest stylesheet assertion that review round 1 removed, so on the final tree the indent is pinned by the Galata measurement alone. `build-172b.log` and `galata-172b-notes.log` (118 passed) are the tree before the review; `galata-172b-full.log` is a full suite stopped partway once review round 1 changed the stylesheet. `galata-172c-prefix-probe.log` is the thread case run alone against that earlier build, failing with Expected 26, Received 0 on the new measurement that the edit icon stands at one inset on every entry. `build-172c.log` is the final tree, on which the notes suite is 118 passed (`galata-172c-notes.log`) and the full suite 191 passed and 2 skipped (`galata-172c-full.log`). `review-172b-round1.log` to `review-172b-round3.log` hold the review with the lenses architect, bug-hunter, ux-designer and slop-hunter (workflows `wf_be6f93b2-437`, `wf_cbecf127-66b`, `wf_9850c2cb-694`): round 1 PLAN with three changes (the reserved slot for the x, test prose, the closed-row wording), round 2 PLAN with one comment, round 3 SHIP with no findings. The version files were restored to 1.0.14 after each build
- `jest-172d.log`, `mutation-172d.log`, `build-172d.log`, `galata-172d-notes.log`, `galata-172d-full.log` and `review-172d-round4.log` - the ruling that withdrew the last-note-only x and put the x back on every entry of a thread (ACC-NOTES-167). `jest-172d.log` is jest at 748 of 748, `mutation-172d.log` two mutations each caught (the x on the last entry alone, the x opening the edit), `build-172d.log` the `make install` of 1.0.15, on which the notes suite is 118 passed (`galata-172d-notes.log`) and the full suite 190 passed, 2 skipped and 1 failed, the failure the forced-render case filed as DEF-COMPAT-110 (`galata-172d-full.log`). `review-172d-round4.log` is the confirming round of the four-lens review over that delta, SHIP with no findings. The version files were restored to 1.0.14 after the build
- `jest-132e.log`, `jest-132e-r1.log`, `mutation-132e.log`, `mutation-132e-r1.log`, `build-132e.log`, `chain-132e-probe.log`, `galata-132e-probe.log`, `build-132f.log`, `chain-132f.log`, `galata-132f-notes.log`, `galata-132f-full.log`, `build-132g.log`, `chain-132g.log`, `galata-132g-notes.log`, `galata-132g-full.log`, `review-132e-round1.log` and `review-132e-round2.log` - the ruling that puts the eye and the trash at the right of a row's top line, removes the Collapse triangle and closes an open row from a click on its top line or Enter (ACC-NOTES-132, ACC-NOTES-148, ACC-CUE-149). `jest-132e.log` is jest at 748 of 748 on the change and `jest-132e-r1.log` at 749 of 749 after review round 1; `mutation-132e.log` holds seven mutations each caught (the top line selecting instead of closing, Enter selecting instead of closing, a press on the eye or trash reaching the top line, the two controls left under the thread, the trash before the eye, the old Collapse description, the eye offered while writing) and `mutation-132e-r1.log` the double-click guard removed, caught. `galata-132e-probe.log` is the changed cases on the first build with a temporary height probe, removed afterwards: the closed top line 18 px high, the open one 24 px with the passage 8 px lower, which led to the -3px pull-back; two cases failed there on test code. `build-132f.log` is the build with the pull-back, on which the notes suite is 117 passed and 1 failed (`galata-132f-notes.log`, the ACC-NOTES-155 case opening a row already open); `galata-132f-full.log` is a full suite stopped once review round 1 asked for a code change. `build-132g.log` is the final build; its first suite attempt found port 8911 held by the server of the stopped run, and `chain-132g.log` is the rerun: the notes suite 118 passed (`galata-132g-notes.log`) and the full suite 191 passed and 2 skipped (`galata-132g-full.log`). `review-132e-round1.log` and `review-132e-round2.log` hold the review with the lenses architect, bug-hunter, ux-designer and slop-hunter (workflows `wf_6b91c0f9-482`, `wf_ad149ddc-95d`): round 1 PLAN with three changes (a double click on a closed row opened and closed it, a stale `openRow` in the ACC-NOTES-155 case, a false comment in `_colour`), round 2 SHIP with no findings. The version files were restored to 1.0.14 after each build

# Logs

| Log                                     | Tracks                                                                                                                                                                           |
| --------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `make-install.log`                      | Output of `make install` - nodeenv creation, dependency install, TypeScript build, labextension build and pip install                                                            |
| `make-test.log`                         | Output of `make test` - jest unit suite with coverage, then pytest for the server extension                                                                                      |
| `build-cycle.log`                       | One test-then-install cycle: `make test`, and `make install` only when the tests passed                                                                                          |
| `galata.log`                            | Output of the Playwright / Galata integration run in `ui-tests/`, including the test server's own log lines                                                                      |
| `jest.log`, `pytest.log`, `lint.log`    | The proving executor's unit, server and lint runs on the build named in `make-install.log`                                                                                       |
| `galata-proving-*.log`, `proving-*.log` | Narrowed Galata runs and the mutation attacks on the installed bundle - each mutation named, the target tests that went red, the control that stayed green, and the restored md5 |

- `round8-chain.log` - ACC-NOTES-139 chain: Galata proof against 0.6.59, round-8 mutations, graph refresh, build 0.6.60, lint, both Galata suites; ends with CHAIN-DONE
- `round8b-chain.log` - strip order ruling: Galata proof against 0.6.60, graph refresh, build 1.0.2, lint, both Galata suites; ends with CHAIN-DONE
- `screenshots-chain.log` - README screenshots (ui-tests/tests/screenshots.spec.ts), rewritten by every run of the spec; the shipped files are from the last run; ends with SHOTS-DONE
- `round9-chain.log` - round-8 fixes: mutations, graph refresh, version reset to 1.0.1, build 1.0.2 (second build of that number, the first carried the round-8 findings), lint, full jest to `jest-102.log`, both Galata suites to `galata-102-second-*.log`, screenshots rerun; ends with CHAIN-DONE
- `galata-102-second-rerun-138.log` - ACC-NOTES-138 rerun three times on the second 1.0.2 build after its one timeout during the low-memory event
- `round9-mutations-rerun.log` - the round-9 mutation runner rerun with the corrected first mutation, 4 of 4 caught
- `lint-102-second.log` - lint on the second 1.0.2 build
- `round10-chain.log` - round-9 fixes: mutations to `mutation-*.log` (one log per mutation, the glob shared by every round's runner), graph refresh, version reset to 1.0.1, build 1.0.2 (third build of that number), lint to `lint-102-third.log`, full jest to `jest-102-third.log`, both Galata suites to `galata-102-third-*.log`, screenshots rerun; ends with CHAIN-DONE
- `round11-chain.log` - round-10 fixes: mutations, graph refresh, version reset to 1.0.3 so the build lands on the published 1.0.4, lint to `lint-104.log`, full jest to `jest-104.log`, both Galata suites to `galata-104-*.log`, screenshots rerun; ends with CHAIN-DONE
- `galata-104-rerun-3.log` - the three cases that failed in the round-11 chain while the sibling extensions were being rebuilt in their own trees (ACC-NOTES-138 and two alerts-sibling cases), rerun on the settled lab: 3 passed
- `install-106-reinstall.log` - `make install` after the system refresh of 2026-09-10 put 1.0.5 back in site-packages; version reset to 1.0.5 so the build lands on 1.0.6 again
- `galata-106-proof-minimap-plus.log` - the rewritten ACC-NOTES-139 and ACC-NOTES-130 cases against the installed 1.0.6 that still showed the plus on the strip: 2 failed at the new toBeHidden assertions
- `round20-mutations.log` - two guards reverted, each spec passing unmutated first (`mutation-baseline-round20-*.log`): the plus put back on the bare rule that shows it in every state (`mutation-plus-shown-on-the-strip.log`), and the read applying the file's panel state while a state write is pending (`mutation-stored-state-read-over-a-pending-write.log`); 2 of 2 caught
- `round20-chain.log` - the minimap plus and the sibling gate: version reset to 1.0.5, build 1.0.6 to `build-106-round20.log`, lockfile churn restored, lint to `lint-106-round20.log`, full jest to `jest-106-round20.log`, both Galata suites to `galata-106-round20-*.log`, the sibling suite with CI=1 to `galata-106-siblings-ci.log`, screenshots rerun, graph refresh; ends with CHAIN-DONE
- `galata-106-rerun-138.log`, `galata-106-repeat-138.log`, `galata-106-trace-138.log` - ACC-NOTES-138 on the first round-20 build: 1 of 5 failed at its Hide-control focus assertion, then 10 of 10 and 30 of 30 passed (the last keeping traces on failure); with the chain's own failure, 2 of 46 (DEF-NOTES-71)
- `jest-def-notes-71-repro.log` - the DEF-NOTES-71 jest case on the unfixed controller: fails, the controller emitting expanded, hidden, expanded
- `round20b-chain.log` - DEF-NOTES-71 fixed in the controller: version reset to 1.0.5, build 1.0.6 to `build-106-round20b.log`, lockfile churn restored, lint to `lint-106-round20b.log`, full jest to `jest-106-round20b.log`, both Galata suites to `galata-106-round20b-*.log`, the sibling suite with CI=1 to `galata-106-siblings-ci-round20b.log`, ACC-NOTES-138 thirty times to `galata-106-repeat-138-round20b.log`, screenshots rerun, graph refresh; ends with CHAIN-DONE
- `galata-106-proof-def-notes-72.log` - the ACC-NOTES-139 case with its new Cancel step against the installed 1.0.6 that lacks the DEF-NOTES-72 fix: failed at `notes.spec.ts:625`, the file still holding the document marker after Cancel
- `jest-def-notes-72.log` - the two notes jest files with the DEF-NOTES-72 fix in the source: 260 passed
- `round21-mutations.log` - six DEF-NOTES-72 guards reverted, each spec passing unmutated first (`mutation-baseline-round21-*.log`), one log per mutation under `mutation-*.log`
- `round21-chain.log` - DEF-NOTES-72 fixed and the README images retaken at 2560x1440 in the Galaxa Dark Theme - Steel: version reset to 1.0.5, build 1.0.6 to `build-106-round21.log`, lockfile churn restored, lint to `lint-106-round21.log`, full jest to `jest-106-round21.log`, mutations, both Galata suites to `galata-106-round21-*.log`, screenshots rerun with the six image sizes listed in `screenshots-chain.log`, graph refresh; ends with CHAIN-DONE
- `galata-106-proof-acc-notes-146.log` - the ACC-NOTES-146 case against the installed 1.0.6 built before the note field font change: failed, the field's font family `monospace` where the panel's is `"DejaVu Sans", "Noto Sans SC Variable"`
- `round22-chain.log` - the note field in the panel's font family at a smaller size (ACC-NOTES-146): the proof above, version reset to 1.0.5, build 1.0.6 to `build-106-round22.log`, lockfile churn restored, lint to `lint-106-round22.log`, full jest to `jest-106-round22.log`, both Galata suites to `galata-106-round22-*.log`, graph refresh; ends with CHAIN-DONE
- `galata-106-rerun-118-forced-render.log` - the two cases that failed in the round-22 notes group, rerun on the round-22 build: ACC-NOTES-118 with its check changed to four filled lines without scrolling passed; the forced render case in siblings.spec.ts failed again with the preview moved by 1667 px (DEF-APPLY-37-1)
- `galata-106-repeat-forced-render.log` - the forced render case in siblings.spec.ts six times in isolation on the round-22 build, to measure how often it fails: 5 passed and 1 failed, the failure again with the preview moved by 1667 px
- `screenshots-two-below-top-bar.log` - the two README screenshots kept at the Star Colonel's request, retaken on the round-22 build below JupyterLab's top bar with dates in en-GB: 2 passed, animation-01.png and notes-01-panel.png both 2560x1440
- `galata-106-rerun-round23-events-forced-render.log` - the two cases that failed in the round-23 chain, three times each on the round-23 build: events.spec.ts 'applies two writes fifty milliseconds apart as one change' 3 passed (its round-23 failure, 2 applies for 1, came while another session's process held the load average above 20) and the forced render case in siblings.spec.ts 3 passed
- `galata-106-proof-def-cue-74.log` - the tab-cue case 'is a still red square with a tooltip, telling itself apart from the arriving circle', asserting the corrected held-change tooltip, against the installed round-23 build that still told the reader to save or revert: failed, the tooltip reading 'Save or revert the document to take the change.'
- `round24-chain.log` - DEF-CUE-74 fixed and the README rewritten from the marketing and UX reviews: the proof above, version reset to 1.0.5, build 1.0.6 to `build-106-round24.log`, lockfile churn restored, lint to `lint-106-round24.log`, full jest to `jest-106-round24.log`, both Galata suites to `galata-106-round24-*.log`, graph refresh; ends with CHAIN-DONE
- `round23-mutations.log` - the DEF-NOTES-73 fix reverted, the retried panel-state write given back the state its call captured, after the notes spec passed unmutated (`mutation-baseline-round23-notes.spec.log`); the mutated run in `mutation-retried-state-write-stores-the-captured-state.log`; 1 of 1 caught, the new case 'stores the newer state when an older state write is refused and retried after the newer one lands' failing
- `round23-chain.log` - DEF-NOTES-73 fixed and the round-2 register record written, on top of ACC-NOTES-146: version reset to 1.0.5, build 1.0.6 to `build-106-round23.log`, lockfile churn restored, lint to `lint-106-round23.log`, full jest to `jest-106-round23.log`, the mutation above, both Galata suites to `galata-106-round23-*.log`, graph refresh; ends with CHAIN-DONE
- `galata-106-proof-acc-notes-146-quarter.log` - the ACC-NOTES-146 case with its new size assertion, three quarters of `--jp-ui-font-size0`, against the installed round-24 build whose note field is at `--jp-ui-font-size0`: failed, the field at 10.8333 px where three quarters of `--jp-ui-font-size0` is 8.125 px
- `round25-chain.log` - the note field font a quarter smaller (ACC-NOTES-146): the proof above, version reset to 1.0.5, build 1.0.6 to `build-106-round25.log`, lockfile churn restored, lint to `lint-106-round25.log`, full jest to `jest-106-round25.log`, both Galata suites to `galata-106-round25-*.log`, graph refresh; ends with CHAIN-DONE
- `galata-106-proof-acc-notes-147.log` - the ACC-NOTES-147 case against the installed round-25 build, which still offers Add note on a row whose note field is open: failed, Add note count 1 where 0 is required
- `round26-mutations.log` - the panel spec passing unmutated first (`mutation-baseline-round26-notes-panel.spec.log`), then the row built without telling its controls that its note field is open (`mutation-add-note-offered-while-the-field-is-open.log`), which the spec must catch
- `round26-chain.log` - Add note left out of a row while its note field is open (ACC-NOTES-147): the proof and the mutation above, version reset to 1.0.5, build 1.0.6 to `build-106-round26.log`, lockfile churn restored, lint to `lint-106-round26.log`, full jest to `jest-106-round26.log`, both Galata suites to `galata-106-round26-*.log`, graph refresh; ends with CHAIN-DONE
- `galata-106-proof-def-cue-75.log` - the tab-cue case 'is a still red square with a tooltip, telling itself apart from the arriving circle', asserting the round-3 tooltip words 'choose Revert in the File Changed dialog', against the installed round-26 build whose tooltip still says 'Revert the document to take the change': failed, the tooltip reading 'A change on disk is held back by unsaved edits in this document. Revert the document to take the change; saving keeps your version and drops the change.'
- `round27-chain.log` - DEF-CUE-75 (the held-change tooltip names Revert in the File Changed dialog): the proof above, version reset to 1.0.5, build 1.0.6 to `build-106-round27.log`, lockfile churn restored, lint to `lint-106-round27.log`, full jest to `jest-106-round27.log`, both Galata suites to `galata-106-round27-*.log`, graph refresh; ends with CHAIN-DONE
- `galata-106-proof-acc-notes-148.log` - the ACC-NOTES-148 case against the installed round-27 build, whose closed rows still carry the triangle: failed, a triangle count of 1 on a closed row where 0 is required
- `galata-106-proof-acc-notes-146-ordinary.log` - the ACC-NOTES-146 case asserting three quarters of `--jp-ui-font-size1`, against the installed round-27 build whose note field is at three quarters of `--jp-ui-font-size0`: failed, the field at 8.12497 px where 9.75 px is required
- `round28-mutations.log` - the panel spec passing unmutated first (`mutation-baseline-round28-notes-panel.spec.log`), then the triangle built on a closed row as well (`mutation-triangle-on-a-closed-row.log`), which the spec must catch
- `round28-chain.log` - the triangle only on an open row (ACC-NOTES-148) and the note field at three quarters of the panel's ordinary text size (ACC-NOTES-146): both proofs and the mutation above, version reset to 1.0.5, build 1.0.6 to `build-106-round28.log`, lockfile churn restored, lint to `lint-106-round28.log`, full jest to `jest-106-round28.log`, both Galata suites to `galata-106-round28-*.log`, the README notes image retaken to `screenshots-round28-notes-panel.log`, graph refresh; ends with CHAIN-DONE
- `galata-106-proof-def-notes-76.log` - the DEF-NOTES-76 case against the installed round-28 build, where a click on a note leaves its row closed and unselected: failed, the row's controls not found after the click on its note
- `galata-106-proof-def-notes-77.log` - the DEF-NOTES-77 case against the installed round-28 build, where a double click on Add note lands its second click on a colour dot: failed, the row's swatch named 'pink' where 'yellow' is required
- `round29-mutations.log` - the panel spec passing unmutated first (`mutation-baseline-round29-notes-panel.spec.log`), then the note's listener moved to double click (`mutation-note-click-selects-nothing.log`), the text-selection test removed (`mutation-note-click-drops-a-text-selection.log`) and Add note put back before the colour dots (`mutation-add-note-built-before-the-dots.log`), each of which the spec must catch
- `round29-chain.log` - DEF-NOTES-76 fixed, a click on a note selecting its row, and DEF-NOTES-77 fixed, the colour dots staying in place when Add note leaves the row: both proofs and the mutations above, version reset to 1.0.5, build 1.0.6 to `build-106-round29.log`, lockfile churn restored, lint to `lint-106-round29.log`, full jest to `jest-106-round29.log`, both Galata suites to `galata-106-round29-*.log`, the README notes image retaken to `screenshots-round29-notes-panel.log`, graph refresh; ends with CHAIN-DONE
- `round30-mutations.log` - the panel spec passing unmutated first (`mutation-baseline-round30-notes-panel.spec.log`), then the click listener on a row's state line removed (`mutation-state-line-click-opens-nothing.log`), which the spec must catch
- `round30-chain.log` - DEF-NOTES-78 fixed, a click on a closed row's state line opening the row (review round 6's plan change): the mutation above, version reset to 1.0.5, build 1.0.6 to `build-106-round30.log`, lockfile churn restored, lint to `lint-106-round30.log`, full jest to `jest-106-round30.log`, both Galata suites to `galata-106-round30-*.log`, graph refresh; ends with CHAIN-DONE
- `galata-106-proof-def-cue-79.log`, `galata-106-proof-def-notes-80.log`, `galata-106-proof-acc-cue-149.log`, `galata-106-proof-def-notes-82.log`, `galata-106-proof-def-notes-83.log` - the five new Galata cases for the four known limitations against the installed 07654a2 build of 1.0.6: each failed, the red square still on the tab after Reload from Disk, no row opened from a click on its padding, an empty row description, a document marker written by a double click on the badge, and a note held twice after a double click on Save
- `galata-probe-badge-double-click.log` - the measurement of DEF-NOTES-82 on the installed build: 25 of 25 double clicks on the badge pressed the header control under it
- `round31-mutations.log` - the watcher and panel specs passing unmutated first (`mutation-baseline-round31-*.log`), then sixteen guards undone one at a time under `mutation-*.log`: four on the Reload from Disk path (`mutation-reload-*.log`), three on the row click (`mutation-padding-click-*.log`), five on the row description (`mutation-row-without-description.log`, `mutation-closed-description-on-an-open-row.log`, `mutation-description-*.log`, `mutation-descriptions-inside-the-list-body.log`) and four on the double-click guard (`mutation-double-click-*.log`, `mutation-single-click-ignored.log`, `mutation-key-press-ignored.log`); 16 of 16 caught
- `round31-chain.log` - the four known limitations fixed (DEF-CUE-79, DEF-NOTES-80, DEF-NOTES-81 with ACC-CUE-149, DEF-NOTES-82 and DEF-NOTES-83): the five proofs and the mutations above, version reset to 1.0.5, build 1.0.6 to `build-106-round31.log`, lockfile churn restored, lint to `lint-106-round31.log`, full jest to `jest-106-round31.log`, both Galata suites to `galata-106-round31-*.log`, graph refresh; ends with CHAIN-DONE
- `galata-106-proof-acc-notes-146-rows.log` - the rewritten ACC-NOTES-146 case, the note field measured against the note text shown in a row, against the installed round-31 build: failed, the field at 9.75 px where the note text is 13 px
- `round32-chain.log` - the note field at the size of the notes in the rows (ACC-NOTES-146, the Star Colonel's request): the proof above, version reset to 1.0.5, build 1.0.6 to `build-106-round32.log`, lockfile churn restored, lint to `lint-106-round32.log` (1 stylelint error on a comment placed inside the rule, moved above it and rerun clean to `lint-106-round32b.log`), full jest to `jest-106-round32.log`, both Galata suites to `galata-106-round32-*.log`, graph refresh; ends with CHAIN-DONE
- `galata-106-proof-def-notes-84.log`, `galata-106-proof-acc-notes-150.log`, `galata-106-proof-def-notes-80-closed-row.log` - the three cases of round 33 against the installed round-32 build: DEF-NOTES-84 failed with no note field opened (the mark was never written), ACC-NOTES-150 failed with a note entry on a closed row; the DEF-NOTES-80 case passed there because its closed-row assertions ran before the external write had reached the panel, which the case now waits for by opening the row first
- `round33-mutations.log` - the highlight and anchor specs and the panel spec passing unmutated first (`mutation-baseline-round33-*.log`), then the line break ignored (`mutation-line-break-ignored.log`), every element read as a break (`mutation-every-element-breaks-the-text.log`), a closed row showing every note (`mutation-closed-row-shows-every-note.log`) and its first note (`mutation-closed-row-shows-the-first-note.log`); 4 of 4 caught
- `round33-chain.log` - DEF-NOTES-84 (a hard line break in the captured text) and ACC-NOTES-150 (a closed row shows no note): the proofs and the mutations above, version reset to 1.0.5, build 1.0.6 to `build-106-round33.log`, lockfile churn restored, lint to `lint-106-round33.log`, full jest to `jest-106-round33.log`, both Galata suites to `galata-106-round33-*.log` (2 failed in the notes group: the DEF-NOTES-84 row listed the raw source `3. **Ostateczny rygor:**`, fixed in round 34, and the DEF-NOTES-76 case clicked a note on a closed row, rewritten for round 34), graph refresh; ends with CHAIN-DONE
- `round34-mutations.log` - the watcher spec and the notes spec passing unmutated first (`mutation-baseline-round34-*.log`), then the hold test reading the dirty flag alone (`mutation-stale-flag-holds-the-write.log`), the hold skipped for every dirty document with a change held (`mutation-every-dirty-document-takes-the-write.log`) and the passage listed as the raw source (`mutation-passage-listed-as-raw-source.log`)
- `round34-chain.log` - the round-32 review plan (DEF-CUE-85, the cursor over the controls row and the note form), the listed passage of DEF-NOTES-84 and the DEF-NOTES-76 case rewritten: the DEF-NOTES-84 proof against the round-33 build to `galata-106-proof-def-notes-84.log` (rewritten by this run), the mutations above, version reset to 1.0.5, build 1.0.6 to `build-106-round34.log`, lockfile churn restored, lint to `lint-106-round34.log`, full jest to `jest-106-round34.log`, both Galata suites to `galata-106-round34-*.log`, graph refresh; ends with CHAIN-DONE
- `galata-106-rerun-round34-forced-render.log`, `galata-106-rerun-round34-forced-render-2.log` - the forced-render sibling case (siblings.spec.ts 'runs every listener twice for one change without a sibling misbehaving'), which failed once in the round-34 notes group with the preview moved by 1667 px, rerun three times twice on the round-34 build: 3 failed, then 1 failed and 2 passed; the same intermittent failure as on the round-22 build (DEF-APPLY-37-1), nothing in this delta touching the scroll path
- `round35-mutations.log` - the watcher spec passing unmutated first (`mutation-baseline-round35-watcher.spec.log`), then the shadow clause removed from the hold test (`mutation-shadow-clause-removed.log`) and the shadow test inverted (`mutation-typed-text-taken-for-the-shadow.log`)
- `round35-chain.log` - shape A of DEF-CUE-85 and the held-change tooltip naming Reload from Disk: the tab-cue proof to `galata-106-proof-def-cue-75-reload.log` and the live-view proof to `galata-106-proof-def-cue-85-shape-a.log` against the round-34 build, the mutations above, version reset to 1.0.5, build 1.0.6 to `build-106-round35.log`, lockfile churn restored, lint to `lint-106-round35.log`, full jest to `jest-106-round35.log`, both Galata suites to `galata-106-round35-*.log`, the forced-render case three times to `galata-106-repeat-round35-forced-render.log`, graph refresh; ends with CHAIN-DONE
- `round36-shot.log` - the modern note field (ACC-NOTES-151): version reset to 1.0.5, build 1.0.6 to `build-106-round36.log`, lockfile churn restored, then the scratch spec `tmp/campaign/limitations/field-shot.spec.ts.txt` copied into `ui-tests/tests/` for the run and removed after, writing pictures of the open field, empty and while typing, in the Galaxa Steel dark theme and JupyterLab Light to `tmp/field-*.png` (`galata-106-round36-field-shot.log`, 2 passed)
- `galata-106-round36-acc-notes-151.log` - the Galata case 'ACC-NOTES-151 draws the note field as a rounded box with a brand-coloured focus ring, Cancel plain and Save filled at the right edge' twice on the round-36 build, after its first run read the border colour mid-transition (2 passed)
- `lint-106-round36.log` - lint check after the note field change
- `round37-mutations.log` - the anchor and notes specs passing unmutated first (`mutation-baseline-round37-*.spec.log`), then the br flush removed from tokeniseSource (`mutation-br-flush-removed.log`), every tag flushing (`mutation-every-tag-flushes.log`) and the listed passage read as a slice again (`mutation-listed-passage-read-as-a-slice.log`); each must fail its spec
- `round37-chain.log` - review round 2's plan (DEF-NOTES-86 a br written in the source, DEF-NOTES-87 the listed passage from the whole-source scan) with the note field of round 36 on one build: the DEF-NOTES-86 proof against the round-36 build to `galata-106-proof-def-notes-86.log`, the mutations above, version reset to 1.0.5, build 1.0.6 to `build-106-round37.log`, lockfile churn restored, lint to `lint-106-round37.log`, jest and pytest to `jest-106-round37.log`, Galata to `galata-106-round37-notes-siblings-settings.log` and `galata-106-round37-live-events-stream-cue.log`, the forced-render sibling case three times to `galata-106-repeat-round37-forced-render.log`, graph refresh
- `galata-106-repeat-round37-forced-render.log` - the forced-render sibling case three times on the round-37 build, 2 of 3, the rejected intermittent DEF-APPLY-37-1
- `round38-chain.log` - the note field refined at the Star Colonel's word of 2026-09-11 00:58 (ACC-NOTES-151: Save and Cancel in the Add note look, no focus ring): version reset to 1.0.5, build 1.0.6 to `build-106-round38.log`, lockfile churn restored, lint to `lint-106-round38.log`, jest and pytest to `jest-106-round38.log`, the notes Galata group to `galata-106-round38-notes.log`, the field pictures to `tmp/field-*.png` (`galata-106-round38-field-shot.log`), graph refresh
- `round39-chain.log` - the animation speed default raised to 50 characters per second (ACC-ANIM-143, ACC-ANIM-76) and the highlight duration default to 5 s (ACC-HILITE-21), both at the Star Colonel's word of 2026-09-11: the ACC-ANIM-143 proof against the round-38 build to `galata-106-proof-acc-anim-143-fifty.log`, version reset to 1.0.5, build 1.0.6 to `build-106-round39.log`, lockfile churn restored, lint to `lint-106-round39.log`, jest and pytest to `jest-106-round39.log`, the live-view, events, stream and tab-cue Galata group to `galata-106-round39-live-events-stream-cue.log`, graph refresh
- `round40-mutations.log` - the diff and controller specs passing unmutated first (`mutation-baseline-round40-*.spec.log`), then the line pass made always coarse (`mutation-line-pass-always-coarse.log`), the line pass skipped by diffWords (`mutation-line-pass-skipped-by-the-controller.log`) and changed lines no longer compared by word (`mutation-changed-lines-not-compared-by-word.log`); each must fail its spec
- `round40-chain.log` - DEF-HILITE-88 (a line pushed into a long document highlighted the whole document green): the Galata proof against the round-39 build to `galata-106-proof-def-hilite-88.log`, the mutations above, version reset to 1.0.5, build 1.0.6 to `build-106-round40.log`, lockfile churn restored, lint to `lint-106-round40.log`, jest and pytest to `jest-106-round40.log`, Galata to `galata-106-round40-notes-siblings-settings.log` and `galata-106-round40-live-events-stream-cue.log`, the forced-render sibling case three times to `galata-106-repeat-round40-forced-render.log`, graph refresh
- `galata-106-heading-shot.log`, `galata-106-heading-shot-marked.log` - the two-line h3 title of the Norlandia argument copy (`tmp/campaign/limitations/ostateczny.md`) pictured selected by the mouse with the extension live and off (`tmp/heading-live-selected.png`, `tmp/heading-off-selected.png`, byte-identical) and marked yellow (`tmp/heading-live-marked.png`), for DEF-NOTES-89; the scratch spec is `tmp/campaign/limitations/heading-shot.spec.ts.txt`, copied into `ui-tests/tests/` for the run and removed after
- `round41-chain.log` - DEF-NOTES-89 (headings in the preview take a line height of 1.25, so a selection over a wrapped heading covers no letters of the line above): the Galata proof against the round-40 build to `galata-106-proof-def-notes-89.log`, version reset to 1.0.5, build 1.0.6 to `build-106-round41.log`, lockfile churn restored, lint to `lint-106-round41.log`, jest and pytest to `jest-106-round41.log`, Galata to `galata-106-round41-notes-siblings-settings.log` and `galata-106-round41-live-events-stream-cue.log`, the heading pictured selected and marked again to `tmp/heading-*.png` (`galata-106-round41-heading-shot.log`), graph refresh
- `round41-chain-first-run.log` - the first run of the round-41 chain, stopped during its build after its proof failed for a wrong reason (the case read no line box); its install had already put the new stylesheet in the lab, so the second run rebuilds the old one for the proof first (`build-106-round41-prefix.log`)
- `round42-mutations.log` - the diff, controller and anchor specs passing unmutated first (`mutation-baseline-round42-*.spec.log`), then the line pass bounded by the token bound again (`mutation-line-pass-bounded-by-tokens.log`), the line pass skipped on the controller path in a form that compiles (`mutation-line-pass-skipped-by-the-controller.log`, replacing the round-40 run of the same name that a TypeScript error had scored) and the end-tag br spelling no longer a break (`mutation-end-tag-br-not-a-break.log`); each must fail its spec by a failing test, a compile error alone counts as missed
- `round42-chain.log` - review round 3's plan (MAX_LCS_LINES, the </br> spelling, the placeholder colour, the coarse-guard comment and the register trails): the ACC-NOTES-151 placeholder assertion against the round-41 build to `galata-106-proof-acc-notes-151-placeholder.log`, the mutations above, version reset to 1.0.5, build 1.0.6 to `build-106-round42.log`, lockfile churn restored, lint to `lint-106-round42.log`, jest and pytest to `jest-106-round42.log`, Galata to `galata-106-round42-notes-siblings-settings.log` and `galata-106-round42-live-events-stream-cue.log`, graph refresh
- `round43-mutations.log` - the diff spec passing unmutated first (`mutation-baseline-round43-diff.spec.log`), then the line bound without its headroom (`mutation-line-bound-without-headroom.log`) and the later side bounded at 1500 alone (`mutation-line-bound-on-the-earlier-side-alone.log`); each must fail the spec by a failing test
- `round43-chain.log` - review round 4's plan (headings at the font's own line height, the line bound 1600 with the 1500-line case, the ghost-placement comment): the mutations above, version reset to 1.0.5, build 1.0.6 to `build-106-round43.log`, lockfile churn restored, lint to `lint-106-round43.log`, jest and pytest to `jest-106-round43.log`, Galata to `galata-106-round43-notes-siblings-settings.log` and `galata-106-round43-live-events-stream-cue.log`, the heading pictured again to `tmp/heading-*.png` (`galata-106-round43-heading-shot.log`), graph refresh
- `round43-chain.log` - review round 4's plan verified: 2 of 2 mutations, build 1.0.6, lint, jest 615, pytest 29, Galata 115 and 48, heading pictures under line-height normal, graph refresh
- `round43-mutations.log`, `mutation-baseline-round43-diff.spec.log`, `mutation-line-bound-*.log` - the line bound without headroom and on the earlier side alone, each caught by the 1500-line case
- `build-106-round43.log`, `lint-106-round43.log`, `jest-106-round43.log`, `galata-106-round43-*.log` - the round-43 chain's build, lint, jest and Galata output
- `proof-def-apply-90-jest.log` - the DEF-APPLY-90 watcher case on the unfixed source: 1 failed, the file applied over the typed text at open
- `round44-chain.log` - review round 5's plan verified: the DEF-APPLY-90 Galata proof on the round-43 build (expected to fail), 2 of 2 mutations, build 1.0.6, lint, jest, pytest, Galata both groups, graph refresh
- `galata-106-proof-def-apply-90.log`, `round44-mutations.log`, `mutation-baseline-round44-watcher.spec.log`, `mutation-shadow-*.log`, `build-106-round44.log`, `lint-106-round44.log`, `jest-106-round44.log`, `galata-106-round44-*.log` - the round-44 chain's parts
- `diag-apply90.log`, `diag-apply90-dom.log` - a scratch Galata run on the round-44 build: after typing in the editor and opening the preview on a fresh page, the preview tab carries jp-AdvancedMd-tabBlocked and the typed text survives a write (the fix works in the lab; the round-44 case sat under a beforeEach that opened the preview first)
- `galata-106-round44-def-apply-90-repeat.log` - the misplaced round-44 case, 3 of 3 failed in isolation for that reason; `galata-106-round44-forced-render-repeat.log` - the siblings forced-render case (DEF-APPLY-37-1, the known intermittent) 3 of 3 passed in isolation after its one failure in the round-44 notes group
- `round45-chain.log` - the DEF-APPLY-90 Galata case in its own describe: proof against a build without the guard (`build-106-round45-prefix.log`, `galata-106-proof-def-apply-90.log`, expected to fail), then the guard restored and built (`build-106-round45.log`), lint, jest, pytest, both Galata groups (`lint-106-round45.log`, `jest-106-round45.log`, `galata-106-round45-*.log`), graph refresh; `galata-106-round45-forced-render-repeat.log` - the siblings forced-render case (DEF-APPLY-37-1) 3 of 3 passed in isolation after its one failure in the round-45 notes group

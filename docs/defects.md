# Defects - jupyterlab_advanced_markdown_viewer_extension

Observed wrong behaviour of the extension and the trail of what was tried against it.

## Authors

- `@kj` Konrad Jelen

## Change highlighting `HILITE`

Defects in the rendered-view decorations

- [x] `DEF-HILITE-1` **Added block wraps the whitespace between blocks** - MAJOR; when an external change appends a block, the newline text node markdown-it emits between blocks is marked as added and wrapped in a span, which becomes a new direct child of .jp-RenderedMarkdown and breaks the block count edit-at-content relies on
  - evidence: unit test 'never wraps the whitespace between blocks when a block was added' in `src/__tests__/highlight.spec.ts` passes (36/36) and Galata 'adds no direct child to the render root' passes, logs/galata.log 2026-09-05, 10/10 on version 0.1.3
  - repro: open a preview, rewrite the file with one extra paragraph, count root.children before and after: one more than the block count
  - test-tags: UNIT, E2E
  - root-cause: 2026-09-04T22:49:49Z @kj decorate() wrapped every span overlapping an added range, including text nodes whose parent is the render root
  - log: 2026-09-04T22:49:49Z @kj added
  - log: 2026-09-04T22:55:27Z @kj closed
- [x] `DEF-HILITE-6` **Word replacement ghost is glued to its replacement** - MINOR; a replaced word renders as the struck old word immediately followed by the new word with no separator, reading as one fused token
  - evidence: unit tests 'adds a gap after a ghost whose replacement starts with a word' and 'adds no gap when the ghost ends at punctuation' in `src/__tests__/highlight.spec.ts`; Galata 'separates a struck word from its replacement' asserts the ::after content holds a non-breaking space; Galata 15/15 on the installed build v0.6.7 (logs/galata.log 2026-09-05), unit suite 62/62 via make test, adversarial review rounds 9 and 10 adjudicated clean (SHIP)
  - repro: rewrite 'mentions apples.' to 'mentions oranges.': the preview shows 'apples.oranges.' with the first struck
  - test-tags: E2E
  - root-cause: 2026-09-05T07:39:51Z @kj confirmed; resolved with a generated gap after a ghost whose text ends in a word and whose following text starts with one: class jp-AdvancedMd-gap, a non-breaking space in an inline-block ::after so the strike does not cross it; no property is set on the ghost's own box
  - root-cause: 2026-09-04T23:22:21Z @kj the ghost is inserted at the removal offset and the replacement starts at the same offset; no box model property may be set on decorations; adversarial review round 1 finding F10
  - log: 2026-09-04T23:22:21Z @kj added
  - log: 2026-09-05T08:34:56Z @kj closed
- [x] `DEF-HILITE-7` **Changed SVG text labels vanish for the fade window** - MINOR; a decoration span inserted inside an SVG text element is not an SVG element, so a changed diagram label is unpainted until undecorate restores it
  - evidence: unit tests 'reads HTML inside a foreignObject but never SVG text' and 'leaves a changed SVG label untouched' in `src/__tests__/highlight.spec.ts`; Galata 15/15 on the installed build v0.6.7 (logs/galata.log 2026-09-05), unit suite 62/62 via make test, adversarial review rounds 9 and 10 adjudicated clean (SHIP)
  - repro: render a mermaid diagram, change a node label on disk: the label disappears for fadeDuration plus 300 ms and then returns
  - test-tags: UNIT
  - root-cause: 2026-09-05T07:39:51Z @kj confirmed; resolved in captureText by reading past text nodes whose parent is in the SVG namespace, so a diagram label is never wrapped; HTML inside a foreignObject is still read and decorated
  - root-cause: 2026-09-04T23:22:21Z @kj captureText accepts text nodes inside SVG and decorate wraps them in HTML spans; adversarial review round 1 finding F17
  - log: 2026-09-04T23:22:21Z @kj added
  - log: 2026-09-05T08:34:56Z @kj closed
- [x] `DEF-HILITE-8` **Fade timer outlives decorations cleared by a settings change** - MINOR; turning highlight or the extension off during a fade removes the decorations but leaves the fade timer pending, so the next external change within that window is treated as held: its decorations skip the fade-in and the diff baseline stays put until the orphaned timer fires
  - evidence: unit test 'a settings change during a fade ends the fade and the next change fades in' in `src/__tests__/controller.spec.ts`; Galata 15/15 on the installed build v0.6.7 (logs/galata.log 2026-09-05), unit suite 62/62 via make test, adversarial review rounds 9 and 10 adjudicated clean (SHIP)
  - repro: apply a change, within 4 s set highlight=false then true, apply another change: the new spans carry --jp-AdvancedMd-fade-in: 0ms
  - test-tags: UNIT
  - root-cause: 2026-09-05T07:39:51Z @kj confirmed; resolved by clearing the fade timer inside _clearDecorations and by advancing the diff baseline to the text on screen whenever a fade ends early (_endFade), so the next change is diffed alone and fades in
  - root-cause: 2026-09-04T23:59:23Z @kj _clearDecorations does not clear _fadeTimer; only dispose and the fade callback do; adversarial review round 5 finding 2, adjudicated immaterial; remedy is moving the timer clear into _clearDecorations
  - log: 2026-09-04T23:59:23Z @kj added
  - log: 2026-09-05T08:34:56Z @kj closed
- [x] `DEF-HILITE-9` **Held write skips the fade-in in regions never tinted before** - MINOR; while a fade is pending every re-created decoration gets a zero fade-in, including spans over text in a block that carried no highlight before, so that text appears at full tint instantly instead of over 300 ms
  - evidence: unit tests 'cuts a held run where the fresh part begins' in `src/__tests__/highlight.spec.ts` and 'fades in a region never tinted before during a held fade' in `src/__tests__/controller.spec.ts`; Galata 15/15 on the installed build v0.6.7 (logs/galata.log 2026-09-05), unit suite 62/62 via make test, adversarial review rounds 9 and 10 adjudicated clean (SHIP)
  - repro: apply a change to paragraph one, within the fade apply a change to paragraph two: paragraph two's span has --jp-AdvancedMd-fade-in: 0ms
  - test-tags: UNIT
  - root-cause: 2026-09-05T07:39:51Z @kj confirmed; resolved by a per-span decision: during a fade the controller also diffs the previous render against this one and passes those fresh ranges to decorate, which cuts an added run where the fresh part begins and gives only the part an earlier render already tinted a zero fade-in; a ghost is held when the fresh diff has no removal at its offset
  - root-cause: 2026-09-04T23:59:23Z @kj the hold decision is per render, not per span; a per-span decision needs the previous decoration's snapshot offsets; adversarial review round 4 and 5 finding 1, adjudicated immaterial and deferred as not earned
  - log: 2026-09-04T23:59:23Z @kj added
  - log: 2026-09-05T08:34:56Z @kj closed
- [x] `DEF-HILITE-10` **Held ghost re-fades when the same position is replaced twice within one fade** - MINOR; isFreshRemoval matches a removal to the fresh diff by offset only, so a ghost already on screen at that offset is treated as new when the word there is replaced again inside the fade window and dips from transparent for 300 ms; adversarial review round 6 finding 4, deferred by the adjudicator as cosmetic
  - evidence: unit tests 'holds a ghost already on screen when the word at its offset is replaced again' and 'fades in a ghost of text this render removed' in `src/__tests__/highlight.spec.ts`, 132/132 pass; Galata 18/18 on the installed build (logs/galata.log 2026-09-05)
  - root-cause: 2026-09-05T15:00:12Z @kj resolved: isFreshRemoval matches a fresh removal by offset and text, so a ghost still on screen at an offset where a different word was removed again is held instead of fading in a second time; with the animation on, ghosts carry explicit fresh flags from the animator and this rule serves the speed 0 path
  - repro: rewrite 'mentions apples.' to 'mentions oranges.', then within the fade to 'mentions pears.': the apples ghost fades in a second time
  - test-tags: UNIT
  - log: 2026-09-05T08:02:48Z @kj added
  - log: 2026-09-05T15:00:12Z @kj closed
- [x] `DEF-HILITE-11` **Ghost bound misses the coarse diff branch when only the added side passes the token bound** - MAJOR; decorate skipped a removal only when the removal's own token count passed MAX_LCS_TOKENS, but the diff goes coarse when either side of the changed middle passes it, so an early edit plus a 500-word appended section in one write produced a document-sized ghost inside the first block and a layout shift at fade end; adversarial review round 8 finding 4, reproduced under node against lib/diff.js
  - evidence: unit test 'shows no ghost when only the added side of a replacement passes the bound' in `src/__tests__/highlight.spec.ts` fails before the fix and passes after; review round 9 and 10 confirms traced to the change and planned no edit; Galata 15/15 on the installed build v0.6.7 (logs/galata.log 2026-09-05), unit suite 62/62 via make test, adversarial review rounds 9 and 10 adjudicated clean (SHIP)
  - repro: before 'a ' + 'old '.repeat(400), after 'a ' + 'new '.repeat(600): removal 799 tokens and addition 1199 tokens at offset 2; the old body appears struck inside the first block
  - test-tags: UNIT
  - root-cause: 2026-09-05T08:21:07Z @kj the skip condition tested one side of a two-sided rule; resolved by also skipping a removal when an added range starting at its offset passes the bound, so the guard evaluates the diff's own coarse rule on the diff's own outputs
  - log: 2026-09-05T08:21:07Z @kj added
  - log: 2026-09-05T08:34:56Z @kj closed
- [x] `DEF-HILITE-12` **Added-side ghost bound hides a short readable ghost when the same write appends a large section** - MINOR; the two-sided ghost bound from DEF-HILITE-11 skips a removal whenever the added range at its offset passes MAX_LCS_TOKENS, so a replaced closing sentence followed by an appended section of more than 1000 tokens loses its strike while the green wash and the tab cue still fire; adversarial review round 10, adjudicated as a product choice (a ghost-size line between 7 and 799 tokens, or a bound in the diff itself) and deferred to the Star Colonel
  - evidence: unit test 'shows a short ghost when only the addition beside it passes the bound' in `src/__tests__/highlight.spec.ts` (7-token removal beside a 1206-token addition) and the DEF-HILITE-11 test still hiding the 799-token removal, 132/132 pass; Galata 18/18 on the installed build (logs/galata.log 2026-09-05)
  - repro: before a 200-word paragraph then 'The old closing sentence here.', after the same paragraph then 'The new closing sentence here.' then 600 appended words: decorate produces 0 removed spans where the 7-token ghost was shown before the DEF-HILITE-11 fix
  - test-tags: UNIT
  - root-cause: 2026-09-05T15:00:12Z @kj resolved by a ghost-size line: MAX_GHOST_TOKENS (50) in src/highlight.ts; a removal in the coarse branch is hidden only when it is longer than that, so a short removal keeps its ghost whatever the size of the addition beside it while the lumped middle of a rewritten document (DEF-HILITE-11) stays hidden
  - root-cause: 2026-09-05T08:35:14Z @kj the coarse diff branch lumps the whole changed middle into one removal and one addition at the same offset, so the size of the addition says nothing about whether the removal is a readable ghost; reverting the clause re-opens DEF-HILITE-11 (799-token ghost), so a separate ghost-size threshold or a diff-side bound is needed
  - log: 2026-09-05T08:35:14Z @kj added
  - log: 2026-09-05T15:00:12Z @kj closed
- [x] `DEF-HILITE-14` **Emptied document shows no removal ghost** - MINOR; MINOR; when an external change leaves the rendered view with no text at all, the words that went are not struck out - the reader sees an empty page with no record of what was there
  - evidence: 'shows no ghost when the change left the render with no block', 'puts the ghost of all the text in the block that survived it' and 'passes over a heading when only headings and a block survived' in `src/__tests__/highlight.spec.ts`; removing the deferral fails the last two; the deliberate half is recorded as ACC-HILITE-110
  - repro: open a preview of a file with text, write an empty file over it, watch the rendered view: no struck-out text appears
  - test-tags: UNIT
  - root-cause: 2026-09-06T22:05:27Z @kj a ghost is anchored inside a block the renderer produced; when the change leaves a block but no text the ghost is now deferred to the first surviving block that is not a heading, and when no block survives none is shown, because the only remaining place would be a new direct child of the render root whose count the edit-at-content sibling matches against the Markdown block count
  - root-cause: 2026-09-06T19:24:17Z @kj a ghost is anchored inside a block the renderer produced; an emptied render has no text nodes and so no blocks, so spanAt and ghostAnchor in src/highlight.ts have nowhere to place it
  - log: 2026-09-06T19:24:17Z @kj added
  - log: 2026-09-06T22:05:27Z @kj closed
- [x] `DEF-HILITE-15` **Removal ghost inside a fenced code block carries text from after it** - MINOR; MINOR; when a line inside a fenced code block changes, the struck-out ghost can include words from the paragraph following the block, so for the length of the fade the code reads as nonsense; the text is correct once the highlight goes
  - evidence: 'breaks between two blocks the renderer left nothing between' and 'keeps a ghost inside a fenced block clear of the paragraph after it' in `src/__tests__/highlight.spec.ts`; neutralising the block break fails three tests; jest 185 of 185 on build 0.6.19
  - repro: open a preview of a file with a fenced python block followed by a paragraph, rewrite 'second = 2' to 'second = 33' on disk, read the block during the fade: the ghost reads '2After' and the block shows 'second = 2After33'
  - test-tags: E2E
  - root-cause: 2026-09-06T22:05:27Z @kj captureText concatenated the text of every block with nothing between, so where a fenced block's last word abutted the next paragraph's first word the two became one token and the diff paired a match after the fence with a removal inside it; the captured text now carries a block break between two blocks whose own text abuts, and the break belongs to no span so nothing is decorated over it
  - root-cause: 2026-09-06T20:24:58Z @kj the word diff runs over the rendered text of the whole document with no regard for block boundaries, so a match found after the fence is paired with a removal inside it
  - log: 2026-09-06T20:24:58Z @kj added
  - log: 2026-09-06T22:05:27Z @kj closed
- [x] `DEF-HILITE-21` **Painting the marks rescans the whole document once per mark** - MINOR; MINOR; every mark repeats the same two full scans of the document and of the rendered view, so a long document with many marks spends most of a paint doing the same work again; measured at 240 ms for twenty marks on a 126 KB document under the test harness, against a project bound of 100 ms for applying a change
  - evidence: unit test 'scans the source and the render once for a paint of five marks' pinning one source scan and two render captures against five and six before, and the browser test 'DEF-HILITE-21 paints every passage from the one scan of the source and the render' over a 157 KB document of 1200 paragraphs carrying 20 marks; capping the paint loop at three marks turns it red with the three-mark control green
  - repro: open a 126 KB document carrying twenty marks and time one paint
  - test-tags: UNIT
  - root-cause: 2026-09-07T03:39:11Z @kj the two scans were inside the per-mark loop and are now computed once per paint; the second clause of the original reading is accepted rather than fixed - the guard that skips the rewrite still runs after the scanning, so an unchanged document still pays one source scan and two render captures per paint, measured at about 38 ms on a 126 KB document, inside the project's own 100 ms bound
  - root-cause: 2026-09-07T02:47:38Z @kj the source tokens and the rendered text snapshot are computed inside the per-mark loop although both are the same for every mark, and the guard that skips the DOM rewrite is evaluated after the scanning rather than before it
  - log: 2026-09-07T02:47:38Z @kj added
  - log: 2026-09-07T03:39:11Z @kj closed
- [x] `DEF-HILITE-31` **Reduced motion removes the removal ghost's opacity fade and brings back the layout jump** - MEDIUM; Under prefers-reduced-motion the decoration rule sets animation none on every decoration, which also removes the ghost's opacity fade; the ghost is taken out at full opacity and the following text reflows visibly, the jump ACC-HILITE-21 forbids; colour and opacity ramps are not motion and should run
  - evidence: Galata 'the highlights under reduced motion > keep their colour ramps, the ghost fading out before it is taken out' in ui-tests/tests/live-view.spec.ts fails with the decoration reduced-motion rule put back into the installed CSS bundle (animationName none) while the tab-marker reduced-motion test passes; green on build 0.6.31
  - repro: emulate reduced motion, rewrite the file removing a word, read the computed animation-name on the removed span
  - test-tags: FUNCTIONAL
  - root-cause: 2026-09-07T09:01:37Z @kj The reduced-motion rule at style/base.css:107-111 sets animation none on .jp-AdvancedMd-decoration, which by source order also removes the removal ghost's opacity fade
  - log: 2026-09-07T09:01:37Z @kj added
  - log: 2026-09-07T10:04:33Z @kj closed
  - log: 2026-09-07T10:04:33Z @kj fixed in style/base.css: the decoration reduced-motion block deleted; the tab-marker and mark-flash blocks stay, and the typing animation stays off under reduced motion through the controller
- [x] `DEF-HILITE-88` **A line pushed into a long document highlights the whole document green** - MAJOR; in a document of more than about five hundred words, a write that pushes a line in near the top and changes anything further down leaves the whole text between the two edits highlighted green and landed at once, as if everything had changed; the removed text is not shown; reported by the Star Colonel on 2026-09-11 on an agent's write that pushed the content down by a line
  - evidence: diffWords in src/diff.ts aligns a middle past the token bound line by line (lines, lineOps) under a line bound of its own, MAX_LCS_LINES = 1600 (review round 3: the line pass had reused the 1000-token bound as its line bound, so a document past a thousand lines, this repository's own 1272-line criteria register among them, still fell to the whole-document tint; review round 4: the bound sits a hundred lines above the 1500 lines the extension promises, so a document of that size gaining a line stays inside it), and compares only the lines that differ word by word; past 1600 lines, or where no line survives, the middle is one replacement as before; jest 'keeps two small edits apart in a long document, a line pushed in at the top and a word changed at the end (DEF-HILITE-88)', 'keeps two small edits apart past a thousand lines as well, the line pass having a bound of its own (DEF-HILITE-88)' and 'keeps two small edits apart in a document of exactly 1500 lines that gains a line (DEF-HILITE-88)' in `src/__tests__/diff.spec.ts`, the first failing on the old diff with the whole document as one insertion; the rewritten controller cases 'types two small changes 600 words apart and tints nothing between them', 'shows a dropped first paragraph as a ghost and types the changed last word of a long document' and 'shows two dropped paragraphs far apart as ghosts and tints nothing between them' in `src/__tests__/controller.spec.ts` pin the corrected behaviour, while 'types a small write made while an earlier coarse write still stands' keeps the coarse path with a rewrite in which no line survives; mutations line-pass-always-coarse and changed-lines-not-compared-by-word caught (logs/round40-mutations.log), the round-40 run of line-pass-skipped-by-the-controller was scored on a TypeScript unused-symbol error with no test run and was rewritten in round 42 to compile, where it and line-pass-bounded-by-tokens are caught by failing tests (logs/round42-mutations.log), line-bound-without-headroom and line-bound-on-the-earlier-side-alone caught by the 1500-line case (logs/round43-mutations.log); Galata 'DEF-HILITE-88 tints the two edits alone, a line pushed in at the top and a word changed at the end' in ui-tests/tests/live-view.spec.ts failed on the round-39 build with 121 tinted runs against the 2 expected (logs/galata-106-proof-def-hilite-88.log) and is green on the round-40, round-42 and round-43 builds of 1.0.6, round 43 with jest 615 of 615, pytest 29 of 29, lint 0 errors, Galata 115 of 115 and 48 of 48 with 2 skipped (logs/round43-chain.log)
  - repro: open a preview of a 120-paragraph file, then rewrite it with one line added at the top and one word changed in the last paragraph: every paragraph is tinted green and nothing is typed
  - test-tags: UNIT, E2E
  - root-cause: 2026-09-10T23:28:27Z @kj diffWords in src/diff.ts trims the common prefix and suffix by token and aligns the remaining middle by a longest-common-subsequence pass bounded at 1000 tokens, whitespace tokens counted; two edits far apart put the whole document between them into the middle, and past the bound the middle is reported as one deletion and one insertion of everything, which the controller lands at once with the insertion tinted and the deletion hidden
  - log: 2026-09-10T23:28:27Z @kj added
  - log: 2026-09-10T23:46:16Z @kj closed
  - log: 2026-09-11T07:48:43Z @kj edited evidence (replaced)
  - log: 2026-09-11T07:48:56Z @kj edited evidence (replaced)
  - log: 2026-09-11T08:28:46Z @kj edited evidence (replaced)

## Settings `CONFIG`

Defects in how settings are applied

- [x] `DEF-CONFIG-2` **Disabling the extension does not stop the file watcher** - MAJOR; with enabled=false the poll keeps running and external content is still applied to the preview; only decorations and the tab cue were gated
  - evidence: FileWatcher.enabled starts or stops the poll; Galata 'the extension turned off > does not update the preview at all' passes, logs/galata.log 2026-09-05, 10/10 on version 0.1.3
  - repro: set enabled=false in settings, open a preview, rewrite the file, wait two poll intervals: the preview shows the new text
  - test-tags: E2E
  - root-cause: 2026-09-04T22:49:50Z @kj the enabled setting was read by the controller only; FileWatcher had no switch and started its poll unconditionally in context.ready
  - log: 2026-09-04T22:49:50Z @kj added
  - log: 2026-09-04T22:55:27Z @kj closed
- [x] `DEF-CONFIG-23` **Live updates reach the document through two doors the enabled switch does not cover** - MEDIUM; MEDIUM; with live updates turned off, a change on disk can still replace the text in the preview: once through the refresh the notes feature calls before every write, and once when a document holding a blocked change becomes clean; neither path tests the switch, and because the highlight and the tab marker are disabled the replacement is silent
  - evidence: four unit tests in `src/__tests__/watcher.spec.ts` covering the refresh door, the clean-document door, a read in flight and the switch round trip, each turning red when its half of the fix is mutated away; and two browser tests, 'DEF-CONFIG-23 reads nothing on the refresh a marker write asks for' in ui-tests/tests/notes.spec.ts and 'DEF-CONFIG-23 lets the held change go, and reports it again once the switch is back on' in ui-tests/tests/settings.spec.ts; green on build 0.6.24
  - related: DEF-CONFIG-2
  - repro: turn live updates off, let an external change arrive while the document is dirty, then make the document clean: the text is replaced with no highlight and no marker
  - test-tags: UNIT
  - root-cause: 2026-09-07T03:39:11Z @kj the switch was tested at the entrances - registration, the event path, then refresh - so every later entrance was a new door; it is now tested in the one function that reads the file and writes the document, at entry and again after the read because a read is a round trip, and turning the switch off lets a held change go rather than leaving it waiting behind the switch
  - root-cause: 2026-09-07T02:47:54Z @kj the switch was placed on the watcher's registration and its event path; refresh and the model-state handler are two later entrances that do not consult it, and turning the switch off leaves a held change pending
  - log: 2026-09-07T02:47:54Z @kj added
  - log: 2026-09-07T03:39:11Z @kj closed

## Applying external content `APPLY`

Defects in how disk content reaches the open document

- [x] `DEF-APPLY-3` **Scroll restore never runs its late pass** - MAJOR; scroll events dispatch asynchronously, so the controller's own restore is counted as a reader scroll and the 150 ms late restore always returns early; with a URL hash and the TOC fix installed every external write scrolls the reader back to the anchor
  - evidence: unit tests 'overrides a scroll no input preceded during the restore window', 'keeps the reader at the top' and 'stands down when the reader scrolls' in `src/__tests__/controller.spec.ts`; Galata 'the reader position > holds through a rewrite while an anchor scroll fires' (stand-in trigger through the TOC fix's setFragment, position drift under 50 px); Galata 15/15 on the installed build v0.6.7 (logs/galata.log 2026-09-05), unit suite 62/62 via make test, adversarial review rounds 9 and 10 adjudicated clean (SHIP)
  - repro: open a preview with a hash in the URL and the TOC fix installed, scroll away, rewrite the file: the view smooth-scrolls back to the anchor on every write
  - test-tags: E2E
  - root-cause: 2026-09-05T07:54:27Z @kj E2E note: in the Galata lab the page URL carries the reset query, which JupyterLab rewrites within seconds and the hash goes with it, so the TOC fix's render-time anchor scroll never fires there (measured: hash gone 2.5 s after replaceState, scrollTop flat through 900 ms after render); the fix's own scrollToFragment does scroll when called (setFragment: 2367 to 32 over 850 ms), so the Galata test stands the trigger in - the fix's patched setFragment called 100 ms after the same rendered signal - and asserts the reader's position holds
  - root-cause: 2026-09-05T07:39:51Z @kj confirmed: the scroll event of the controller's own restore arrives after the flag around the assignment is reset; resolved by recognising the restore by where it lands (the assigned scrollTop) and, while a restore is in flight, by counting only a scroll within 500 ms of a wheel, pointer or key event as the reader's; a scroll no input preceded is another extension's and the late pass overrides it, so the TOC fix sibling needs no change
  - root-cause: 2026-09-04T23:22:20Z @kj _restoring is toggled synchronously around the scrollTop assignment while the scroll event arrives later with the flag already false; adversarial review round 1 finding F15
  - log: 2026-09-04T23:22:20Z @kj added
  - log: 2026-09-05T08:34:56Z @kj closed
- [x] `DEF-APPLY-4` **First save after an applied change raises the File Changed dialog** - MAJOR; the watcher applies disk content to the model but the Context's private contents model keeps the old hash and last_modified, so the next save from the editor compares against disk and raises the conflict dialog; Revert discards the edit
  - evidence: unit tests 'the Context still exposes _updateContentsModel' and 'records the applied revision on the context' in `src/__tests__/watcher.spec.ts`; Galata 'a document open in the editor as well > saves after an applied change without a dialog' failed on v0.1.6 and passes on v0.6.7; Galata 15/15 on the installed build v0.6.7 (logs/galata.log 2026-09-05), unit suite 62/62 via make test, adversarial review rounds 9 and 10 adjudicated clean (SHIP)
  - repro: open a file in the preview and the editor, rewrite it on disk, wait for the apply, type in the editor and save: the File Changed dialog appears
  - test-tags: E2E
  - root-cause: 2026-09-05T07:39:51Z @kj confirmed: Context._maybeSave compares the recorded hash before writing, so a save after apply would raise the dialog itself and option one is not viable; the collaborative drive reaches the same private updater through a save event the Context accepts only for collaborative models; resolved by calling Context._updateContentsModel with the applied revision after every apply, guarded by a unit test that fails when a JupyterLab release drops the method, and by a warning at runtime; a save over unsaved edits never reaches the apply path so the editor keeps its dialog
  - root-cause: 2026-09-04T23:22:20Z @kj Context._contentsModel is private and refreshed only by load and save; no public way to record the applied revision; the three options are recorded on ACC-APPLY-9 and the choice is the Star Colonel's; adversarial review round 1 findings F1 and F16
  - log: 2026-09-04T23:22:20Z @kj added
  - log: 2026-09-05T08:34:56Z @kj closed
- [x] `DEF-APPLY-22` **A mark written while the document has unsaved edits saves them unasked** - MEDIUM; MEDIUM; the preview and the editor share one document, so typing in the editor leaves unsaved edits; marking a passage in the preview then writes the whole document to disk without being asked, which is the opposite of the rule that a document holding unsaved edits is left alone, and with a change already on disk it can raise the File Changed dialog that ACC-NOTES-100 says never appears
  - evidence: unit test 'leaves a document holding unsaved edits unsaved' in `src/__tests__/notes.spec.ts` and the two-widget browser test 'DEF-APPLY-22 writes the mark into the document and puts nothing on disk' in ui-tests/tests/notes.spec.ts, which types in the editor without saving, marks from the preview, and asserts the file on disk is untouched three seconds later with no dialog open; making the write save unconditionally turns it red while the clean-document test stays green
  - related: ACC-NOTES-100
  - repro: open a Markdown file in the editor and its preview, type in the editor without saving, mark a passage in the preview: the file on disk carries the typed text
  - test-tags: UNIT
  - root-cause: 2026-09-07T02:47:54Z @kj the marker write calls save on the shared context with no test of whether the model was already dirty
  - log: 2026-09-07T02:47:54Z @kj added
  - log: 2026-09-07T03:39:11Z @kj closed
- [x] `DEF-APPLY-27` **A CRLF file gains one carriage return per line on every save after the preview opens** - CRITICAL; Open a file with CRLF line endings in the preview: the watcher applies the raw disk text as an external change at once, the tab shows the updated marker, and every later save (a mark, a note, a panel state change) writes one more CR per line, so the file grows CR CR LF then CR CR CR LF; reproduced by the round 1 sceptic on 0.6.28
  - evidence: unit tests 'applies nothing when a CRLF file holds what the document holds as LF' and 'applies a CRLF write as LF text' in `src/__tests__/watcher.spec.ts` fail on the unchanged _read and again when the regex is made a no-op; Galata 'DEF-APPLY-27 keeps one carriage return per line through two marks' in ui-tests/tests/notes.spec.ts fails with the normalisation mutated in the installed bundle; green on build 0.6.31 (jest 427, Galata 99)
  - repro: open a CRLF file in the preview, mark a passage twice, count the CR CR LF sequences in the file
  - test-tags: UNIT, FUNCTIONAL
  - root-cause: 2026-09-07T09:01:36Z @kj FileWatcher._read compares and applies the raw disk text while the Context holds a CRLF file as LF and re-expands on save, so a CRLF file is applied as an external change on open and every save adds one CR per line
  - log: 2026-09-07T09:01:36Z @kj added
  - log: 2026-09-07T10:04:32Z @kj closed
  - log: 2026-09-07T10:04:32Z @kj fixed in FileWatcher._read: the disk text is normalised to LF before the shadow and model comparisons and _apply, while _record(full) keeps the raw revision and hash; consequence kept: a document loaded as LF whose file is later rewritten as CRLF is applied and saved as LF, the Context's own line-ending rule
- [x] `DEF-APPLY-37` **Scroll guard records a sibling's late smooth scroll as the reader's own** - MEDIUM; LiveViewController._restoreScroll makes one late pass at 150 ms that writes nothing when root.scrollTop still equals the target; a sibling's smooth scroll to a heading that starts after that pass runs with _lateScrollTimer null, so _onScroll records every position of the foreign scroll as the reader's own and the viewer's next render restores the reader to the top; Galata siblings.spec.ts 'runs every listener twice for one change without a sibling misbehaving' fails with Received 1691 in 1 of 113 full runs, 1 of 3 and 2 of 4 isolated repeats on 0.6.37; ACC-COMPAT-108 was closed on this test being green on 0.6.19
  - evidence: SUPERSEDED - the 0.6.38 fix described here was reverted on 0.6.39 and the live state is DEF-APPLY-37-1 (rejected, wontfix). Nothing named below exists in the tree any more: LATE_SCROLL_RESTORE_PASSES, jest controller.spec 'DEF-APPLY-37 keeps the guard up for a foreign smooth scroll that starts after the late pass' and Galata siblings.spec 'DEF-APPLY-37 overrides a sibling scroll that starts after the first late pass' were all removed with the revert. What was proven at the time: the late pass re-armed itself up to 3 more times at 150 ms while root.scrollTop still equalled the target, holding the guard about 600 ms after a render; Galata siblings.spec 'runs every listener twice for one change without a sibling misbehaving' 7 of 7 including --repeat-each=6 where 2 of 4 failed on 0.6.37; the new deterministic Galata test 3 of 3, failing 3 of 3 on a bundle with the pass count at 0 while the alerts control passed. Round 5 of the review confirmed the revert is complete in src, lib and the installed bundle
  - test-tags: UNIT, FUNCTIONAL
  - repro: cd ui-tests && JUPYTER_TEST_PORT=8911 jlpm playwright test -g 'runs every listener twice' --repeat-each=4: 2 of 4 fail with the preview at scrollTop 0 after the reader scrolled to 1691
  - root-cause: 2026-09-07T19:14:01Z @kj the late pass ends the guard when nothing has moved yet, so a foreign scroll that begins after it and that no input preceded is treated as the reader's
  - log: 2026-09-07T19:14:01Z @kj added
  - log: 2026-09-07T19:14:13Z @kj edited test-tags (added)
  - log: 2026-09-07T19:43:27Z @kj closed
  - log: 2026-09-07T21:13:49Z @kj regressed as DEF-APPLY-37-1
  - log: 2026-09-07T21:57:14Z @kj edited evidence (replaced)
  - log: 2026-09-07T21:57:14Z @kj round 5: all three lenses raised the closure citing a mechanism and two tests absent from the tree; the evidence line now records the reversion and points at DEF-APPLY-37-1
- [-] `DEF-APPLY-37-1` **Scroll guard records a sibling's late smooth scroll as the reader's own** - MEDIUM; LiveViewController._restoreScroll makes one late pass at 150 ms that writes nothing when root.scrollTop still equals the target; a sibling's smooth scroll to a heading that starts after that pass runs with _lateScrollTimer null, so _onScroll records every position of the foreign scroll as the reader's own and the viewer's next render restores the reader to the top; Galata siblings.spec.ts 'runs every listener twice for one change without a sibling misbehaving' fails with Received 1691 in 1 of 113 full runs, 1 of 3 and 2 of 4 isolated repeats on 0.6.37; ACC-COMPAT-108 was closed on this test being green on 0.6.19
  - test-tags: UNIT, FUNCTIONAL
  - repro: run the Galata siblings.spec test 'runs every listener twice for one change without a sibling misbehaving' with --repeat-each=6; it failed 2 of 4 isolated repeats and 1 of 113 full runs on 0.6.37 with the reader restored to the top
  - root-cause: 2026-09-07T21:13:57Z @kj The single late pass at 150 ms writes nothing when root.scrollTop still equals the target, so a sibling's smooth scroll that begins after it runs with _lateScrollTimer null and _onScroll records every frame of the foreign scroll as the reader's own. Every remedy tried so far widens the window in which a scroll with no preceding input on the widget node is treated as foreign, and that set contains the reader's own actions - a table-of-contents click and a scrollbar drag both scroll the preview without an input event on the widget node.
  - log: 2026-09-07T21:13:49Z @kj regression of DEF-APPLY-37: the fix was reverted on 0.6.38: round 4 of the review found, on three independent lenses, that the re-armed late pass discards the reader's own scroll whenever no pointer, wheel or key event on the widget node preceded it - a table-of-contents click lands in the left sidebar, a scrollbar thumb drag emits only one pointerdown - and under a stream (one render per ~400 ms against a 600 ms window) the guard never comes down
  - log: 2026-09-07T21:14:02Z @kj edited severity and repro (added) and test-tags (added)
  - log: 2026-09-07T21:14:09Z @kj rejected: wontfix on 0.6.38: the defect is real but every remedy tried costs the reader more than the defect does. The flake is a sibling's smooth scroll landing after the single late pass, seen 1 time in 113 full Galata runs; the remedy is to hold the guard longer, and the guard's rule is that a scroll no input on the widget node preceded is foreign. The reader's own table-of-contents click and scrollbar drag both satisfy that rule, so a longer guard undoes them - three of four review lenses raised it as MAJOR independently, and the adjudicator had deferred the fix on the same reasoning before it was made. Closing the gap needs the guard to tell a sibling's scroll from the reader's by something other than a preceding input event, which is a different design and not scheduled
  - log: 2026-09-10T21:51:24Z @kj on the round-34 build of 1.0.6 the forced-render case failed once in the chain's notes group and then 4 of 6 times in two reruns of three (logs/galata-106-rerun-round34-forced-render*.log), each with the preview moved by 1667 px; the delta since 07654a2 touches no scroll path (src/controller.ts changed only in a tooltip string), so it stays the known intermittent case; scored again on the round-35 build
  - log: 2026-09-10T22:07:34Z @kj round-35 build 1.0.6: the case passed in both full Galata runs (113 of 113, 46 of 46) and in 2 of 3 isolated repeats, the third at Received 1667 (logs/galata-106-repeat-round35-forced-render.log); the intermittent stands as rejected
  - log: 2026-09-10T22:52:41Z @kj round-37 build 1.0.6: the case passed in both full Galata runs (115 of 115, 46 of 46) and in 2 of 3 isolated repeats (logs/galata-106-repeat-round37-forced-render.log); the intermittent stands as rejected
  - log: 2026-09-10T23:46:16Z @kj round-40 build 1.0.6: the case passed in both full Galata runs (115 of 115, 47 of 47) and in 3 of 3 isolated repeats (logs/galata-106-repeat-round40-forced-render.log)
- [x] `DEF-APPLY-90` **A preview opened over a document with unsaved edits has them overwritten by the file's text** - CRITICAL; the reader types in the editor, then opens the Markdown Preview of the same file: the read that closes the registration gap applies the file's text over the typed text at once, with no marker, no dialog and nothing saved; the same happens to the next external write while the document stays dirty; found by review round 5 (bug-hunter) on the round-43 build of 1.0.6, reproduced by the reviewer with the FileWatcher class in a scratch jest run
  - evidence: FileWatcher in src/watcher.ts sets its shadow from the model when the context is ready only while the model is clean; over a document the reader has already typed in the shadow stays empty, so the read that closes the registration gap and every later read hold the file's text back through the hold test of DEF-CUE-85 (the typed text differs from the empty shadow and from the held revision) instead of applying it over the typed text; jest 'holds the first write over text typed before the watcher was built (DEF-APPLY-90)' in `src/__tests__/watcher.spec.ts` failed on the unfixed source with the file applied at open, events ['applied'] against ['blocked:dirty'] (logs/proof-def-apply-90-jest.log), and passes with the guard; mutations shadow-set-from-a-dirty-model and shadow-guard-inverted caught by failing tests (logs/round44-mutations.log); Galata 'DEF-APPLY-90 holds the first write over text typed before the preview was opened' in ui-tests/tests/live-view.spec.ts in a describe of its own with no preview open before the typing, failed on a build of the same tree without the guard (logs/build-106-round45-prefix.log) with the preview opening on the file's text and the typed text gone, before any write (logs/galata-106-proof-def-apply-90.log), and is green on the round-45 build with jest 616 of 616, pytest 29 of 29, lint 0 errors, Galata 114 of 115 in the notes group (the one failure the siblings forced-render case of DEF-APPLY-37-1, rejected wontfix, passing 3 of 3 in isolation, logs/galata-106-round45-forced-render-repeat.log) and 49 of 49 with 2 skipped (logs/round45-chain.log; the round-44 chain on the same source, logs/round44-chain.log, ran the case under a beforeEach that opened the preview before the typing, which tests the ordinary hold, and the case was moved); residue stated and accepted: a preview opened over typed text shows the held-change square and the unsaved-edits tooltip from the moment it opens, since the document differs from the file and the watcher cannot tell whose difference it is; while the typed text stays unsaved every later write is held as well, the same jest case expecting a second blocked event with the document unchanged; the marker clears on a save from this session (_onSaveState), on Reload Markdown File from Disk while the file is not being rewritten at that moment (the equal branch of _read), or once the document is clean again (_onModelState); review round 6 (ux-designer) found the earlier wording of this line stating that the next write landing clears the marker, which the test contradicts, and the wording was corrected
  - related: DEF-CUE-85, ACC-APPLY-105
  - repro: open live.md in the editor, type UNSAVED WORK, open the Markdown Preview of live.md: the preview never shows UNSAVED WORK, or shows it and loses it within a second; upload a rewritten file: it is shown live and the typed text is gone
  - test-tags: UNIT, FUNCTIONAL
  - root-cause: 2026-09-11T08:55:28Z @kj FileWatcher sets its shadow, the text last loaded, applied or saved, from the model when the context is ready (src/watcher.ts:117); a watcher built over a document the reader has already typed in records the typed text as the shadow, and the hold test of DEF-CUE-85 (model.dirty and the text differing from the held revision and from the shadow) then reads the typed text as nothing unsaved and lets the write through
  - log: 2026-09-11T08:55:28Z @kj added
  - log: 2026-09-11T09:33:12Z @kj closed: review round 5 (bug-hunter) found the typed text overwritten at open; fixed by one guard on the shadow's initial value
  - log: 2026-09-11T09:58:24Z @kj edited evidence (replaced)

## Tab title cue `CUE`

Defects in the document tab marker and animation

- [x] `DEF-CUE-5` **Blocked cue clears on the first interaction and is never re-established** - MAJOR; a wheel tick, click or key on the preview strips the blocked marker together with the updated marker, and the watcher emits blocked once per external write because it records the new hash before the dirty check, so the stale preview shows no cue until the next write
  - evidence: unit tests 'reports a blocked change once and applies it once the document is clean' and 'lets a blocked change go when a save overwrote it' in `src/__tests__/watcher.spec.ts` and the 'tab cue' describe in `src/__tests__/controller.spec.ts`; Galata 'keeps the blocked marker through attention and drops it on Overwrite' and 'drops the blocked marker on Revert' failed on v0.1.6 and pass on v0.6.7; Galata 15/15 on the installed build v0.6.7 (logs/galata.log 2026-09-05), unit suite 62/62 via make test, adversarial review rounds 9 and 10 adjudicated clean (SHIP)
  - repro: open the file in the editor and type, rewrite the file on disk, see the red dot, scroll the preview one tick: the dot is gone and the preview stays stale
  - test-tags: E2E
  - root-cause: 2026-09-05T07:39:51Z @kj confirmed, two causes: the controller cleared every marker on attention, and the watcher recorded the new hash before the dirty check so the change was found once; resolved by keeping the blocked change in the watcher until the document is clean (applied then) or took it by a save or reload (a new unblocked signal then), reporting it once, and by clearing only the updated marker on attention
  - root-cause: 2026-09-04T23:22:20Z @kj _onAttention calls _setTabState(null) without checking for the blocked class; adversarial review round 1 finding F9
  - log: 2026-09-04T23:22:20Z @kj added
  - log: 2026-09-05T08:34:56Z @kj closed
- [x] `DEF-CUE-13` **A file gone from disk and a change held over unsaved edits show the same tab marker** - MINOR; the watcher reports blocked('missing') when the file is gone and blocked('dirty') when a change is held over unsaved edits, and the controller gives both the same jp-AdvancedMd-tabBlocked class, so the two states are indistinguishable on the tab; a reader cannot tell a file someone deleted from a change waiting on their own unsaved edits, and the two need opposite actions
  - evidence: the missing-marker tests in `src/__tests__/cue.spec.ts`, which assert its own class and that its rule declares no animation at all, with the other two markers asserted to declare one as a positive control; mapping both blocked reasons back to one class fails three of the cue tests; the Galata test 'stand still and keep their shapes' in ui-tests/tests/tab-cue.spec.ts reads all three markers under reduced motion
  - repro: open a preview, delete the file on disk, note the marker; revert, type an unsaved edit and write the file externally, note the same marker
  - test-tags: E2E
  - root-cause: 2026-09-06T22:05:51Z @kj the controller mapped both blocked reasons to one tab class; a file gone from disk now carries its own class and glyph, a still cross, so the three states differ by shape and by three amounts of movement - continuous turn, occasional nudge, none
  - root-cause: 2026-09-06T18:15:00Z @kj src/controller.ts _onBlocked maps both reasons of the BlockedReason union to one class; style/base.css therefore has one blocked marker to style
  - log: 2026-09-06T18:15:00Z @kj added
  - log: 2026-09-06T22:05:51Z @kj closed
- [x] `DEF-CUE-19` **The missing-file marker occasionally does not appear under load** - MINOR; MINOR; one full-suite run in three left the tab without the marker for a deleted file until the test gave up, and the same test passed on its own immediately afterwards and on the next full run
  - evidence: the test now waits for the checkpoint before deleting the file: two failures in ten under forty busy processes before the change, each with a failed checkpoint request in the server log, and ten of ten after; the fixed test still fails when the missing-marker class is renamed in the installed bundle, so it did not stop discriminating
  - repro: run the whole browser suite repeatedly and watch ui-tests/tests/tab-cue.spec.ts 'stand still and keep their shapes'; it waited out twenty seconds for the missing marker once in three runs
  - test-tags: E2E
  - root-cause: 2026-09-07T01:31:42Z @kj not a product defect: the test deleted the file while JupyterLab was still writing the checkpoint it takes of a document it opens, that request failed on the vanished file, and the platform closed the document with a load error, so no tab survived to carry the marker
  - root-cause: 2026-09-07T00:38:59Z @kj not established; the shape is a deletion event the watcher did not report while the machine was busy, so the state is only reached by the slower fallback
  - log: 2026-09-07T00:38:59Z @kj added
  - log: 2026-09-07T01:31:42Z @kj closed
- [x] `DEF-CUE-30` **The turning half circle, the marker of the commonest state, has no tooltip** - MEDIUM; The held square and the missing cross carry a caption naming the state in words; the updated marker carries none, so the commonest marker reads as a loading glyph to a first-time reader
  - evidence: unit test 'names the change in words for as long as it stands, then gives the caption back' in `src/__tests__/cue.spec.ts` fails with the map entry removed and with the guard removed; Galata tab-cue 'is a half-filled circle in the tab own text colour, with a tooltip' fails with the words reworded or the guard removed in the installed bundle, and siblings 'keeps the colour it gave the tab while the marker shows' fails with the composition removed; green on build 0.6.31 (Galata 99)
  - repro: rewrite an open file externally, hover the tab, read the tooltip
  - test-tags: UNIT, FUNCTIONAL
  - root-cause: 2026-09-07T10:04:33Z @kj the caption map lacked the updated state, and a caption set on an applied change was overwritten about 10 ms later by the document manager, which rewrites the caption on the file-changed signal once it has listed the checkpoints; the earlier held and missing captions also replaced the document's own caption, which the colourful-tab sibling reads for its Path line
  - root-cause: 2026-09-07T09:01:37Z @kj The tab caption map names the held and missing states only, so the updated state has no tooltip
  - log: 2026-09-07T09:01:37Z @kj added
  - log: 2026-09-07T10:04:33Z @kj closed
  - log: 2026-09-07T10:04:33Z @kj fixed in three parts in src/controller.ts: the TAB_CAPTIONS entry for the updated state; a title.changed guard that puts the state's words back over the fresh caption the document manager writes after an applied change; the caption composed as the state's words, a blank line, then the document's own caption, so siblings reading the Path line keep working
- [x] `DEF-CUE-68` **The tab marker does not turn while changes arrive** - MAJOR; The half-filled circle on the document tab stands still on the Star Colonel's 1.0.5 install. It must turn a quarter step at a time, faster while writes keep coming (ACC-CUE-71). Likely cause: the stylesheet removes every tab animation when the browser reports prefers-reduced-motion, and Windows reports that whenever its show-animations switch is off (ACC-CUE-72). If the typing animation is also missing on the same machine, that is the cause. Second candidate: the active class not reaching the tab node. When fixed, the marker turns regardless of the operating system preference; only the extension's own settings switch its motion off.
  - evidence: cause confirmed by a read-only diagnosis: the reduced-motion block in style/base.css set animation none on all three tab markers, last in the file at equal specificity, and the shorthand also reset the faster turn; src/controller.ts silenced the typing on the same preference. Both are gone with the helper, the listener and its field. jest 'silences no marker for the operating system reduced-motion preference' (cue.spec), 'types the change in all the same' and 'keeps typing when the operating system turns reduced motion on' (controller.spec); Galata 'DEF-CUE-68 keep turning and keep their shapes' (tab-cue.spec.ts) and 'DEF-CUE-68 types the added text in rather than landing it at once' (live-view.spec.ts), both under emulated reduced motion; green on the 1.0.6 build with jest 562 of 562, pytest 29 of 29, lint 0 errors, and Galata 147 of 147 in two runs, the last case passing on a re-run after the machine was busy
  - related: ACC-CUE-71
  - related: ACC-CUE-72
  - repro: On 1.0.5 open a Markdown preview and rewrite its file from outside JupyterLab once a second for five seconds. The tab marker appears and does not turn.
  - test-tags: E2E
  - log: 2026-09-08T20:41:08Z @kj added
  - log: 2026-09-08T20:48:30Z @kj edited title and text and repro (replaced)
  - log: 2026-09-08T22:31:32Z @kj closed
- [x] `DEF-CUE-74` **The held-change tooltip tells the reader to save to take the change, and a save drops it** - MEDIUM; the tooltip of a tab whose change is held back by unsaved edits reads 'Save or revert the document to take the change', but a save from the session overwrites the file on disk, so the held change is dropped and the marker cleared, as ACC-APPLY-105 states; a reader who saves as the tooltip says loses the change that came from disk
  - evidence: tab-cue.spec.ts 'is a still red square with a tooltip, telling itself apart from the arriving circle' failed on the build before the fix with the tooltip 'Save or revert the document to take the change.' (logs/galata-106-proof-def-cue-74.log) and passed on the round-24 build of 1.0.6 (logs/galata-106-round24-live-events-stream-cue.log, 44 passed, 2 skipped); jest 589 of 589; lint 0 errors
  - related: ACC-CUE-30, ACC-APPLY-105
  - repro: open a Markdown file in the preview and in an editor, type in the editor without saving, rewrite the file from another process, hover the red square on the tab and read the tooltip, then save: the file holds the typed text and the external change is gone
  - test-tags: E2E
  - root-cause: 2026-09-10T11:39:10Z @kj TAB_CAPTIONS in src/controller.ts names a save as a way to take the held change, while FileWatcher._onSaveState releases the held change when a save completes, as ACC-APPLY-105 and the ACC-CUE-30 mechanism record
  - log: 2026-09-10T11:39:10Z @kj added
  - log: 2026-09-10T11:55:00Z @kj closed
- [x] `DEF-CUE-75` **The held-change tooltip's word Revert matches the File menu's Revert to Checkpoint, which writes the checkpoint over the change** - MEDIUM; the tab tooltip of a change held back by unsaved edits said 'Revert the document to take the change'; in JupyterLab 4.6.3 the only File menu entry with that word, Revert Markdown File to Checkpoint, restores the checkpoint made when the file was opened or last saved over the file on disk, so a reader who follows the tooltip loses the agent's change; Reload from Disk takes the change but leaves the document dirty and the red square on the tab; the route that takes the change and clears the marker is a save, then Revert in the File Changed dialog
  - evidence: src/controller.ts TAB_CAPTIONS[TAB_BLOCKED_CLASS] reads 'A change on disk is held back by unsaved edits in this document. To take the change, choose Reload Markdown File from Disk in the File menu; a save keeps your version and drops the change.' and the README bullet Unsaved edits are kept names the same route; the word Revert is gone from the tooltip; Galata tab-cue 'is a still red square with a tooltip, telling itself apart from the arriving circle' asserting 'choose Reload Markdown File from Disk' and no 'Revert' failed on the round-34 build with the old dialog wording (logs/galata-106-proof-def-cue-75-reload.log) and passes on the round-35 build of 1.0.6 (logs/round35-chain.log, Galata 46 of 46)
  - related: ACC-CUE-30, ACC-APPLY-105, DEF-CUE-74
  - repro: open a Markdown file in the preview and in an editor, type without saving, let a process rewrite the file; the tab shows the red square whose tooltip says to revert; choose File, Revert Markdown File to Checkpoint and confirm: the file on disk holds the checkpoint text and the process's change is gone
  - test-tags: E2E
  - root-cause: 2026-09-10T12:31:33Z @kj TAB_CAPTIONS in src/controller.ts named 'Revert the document' without naming a JupyterLab control, and the File menu's only entry with that word runs restoreCheckpoint and then revert, which copies the checkpoint over the file on disk; Context._revert, behind Reload from Disk, sets no dirty flag to false and emits no save state, so FileWatcher does not release the held change
  - log: 2026-09-10T12:31:33Z @kj added
  - log: 2026-09-10T12:50:10Z @kj closed
  - log: 2026-09-10T21:49:48Z @kj at the Star Colonel's word of 2026-09-10 the held-change tooltip now names Reload Markdown File from Disk in the File menu as the way to take the change and a save as the way to keep the reader's version; it names no dialog and no Revert, since Reload from Disk takes the change and clears the marker (DEF-CUE-79, DEF-CUE-85); README updated to match
  - log: 2026-09-10T22:07:43Z @kj edited evidence (replaced)
- [x] `DEF-CUE-79` **Reload from Disk takes a held change into the document but keeps the red square and the dirty flag, and holds the next write again** - MEDIUM; with unsaved edits in a Markdown document open in the preview, a change another process writes to the file is held back and the tab shows the red square; File, Reload Markdown File from Disk, confirmed with Reload in JupyterLab's dialog, loads the file into the document, yet the square stays, the document stays marked dirty, and the next write by the process is held back again instead of shown; measured on 1.0.6 in JupyterLab 4.6.3: the square was on the tab at every 2 s sample for 16 s after the reload and model.dirty was true
  - evidence: FileWatcher listens for context.fileChanged and re-checks a held change (src/watcher.ts _onFileChanged); when the file matches the document text _read releases the change, records the contents model and sets model.dirty false; Galata 'takes the change on Reload from Disk, drops the blocked marker and follows the next write' failed on the installed 07654a2 build with the marker still on the tab (logs/galata-106-proof-def-cue-79.log) and passes on the round-31 build 1.0.6 (logs/galata-106-round31-live-events-stream-cue.log, 45 passed); jest 'lets a held change go when a reload brought it into the document', 'keeps holding the change when the reader typed between the reload and the read', 'reads nothing when the Context records a revision and nothing is held', 'starts no second read from its own record or clean flag when it lets a held change go'; 4 mutations caught: reload-listener-removed, reload-leaves-the-document-dirty, reload-records-before-it-releases, reload-reads-with-nothing-held (logs/round31-mutations.log); jest 602 of 602 (logs/jest-106-round31.log)
  - related: ACC-CUE-30
  - related: DEF-CUE-75
  - repro: open a Markdown file in the preview and in an editor, type without saving, let a process rewrite the file until the tab shows the red square; choose File, Reload Markdown File from Disk and confirm Reload: the preview shows the process's text but the red square and the dirty mark stay, and a second write by the process does not appear
  - test-tags: UNIT, E2E
  - root-cause: 2026-09-10T16:25:48Z @kj Context._revert in @jupyterlab/docregistry 4.6.3 (lib/context.js:583-641) loads the file with model.fromString and records the revision with _updateContentsModel, which emits fileChanged, but sets no dirty flag to false (only _save does, context.js:553) and emits no save state, and the file does not move, so the change channel reports nothing; FileWatcher listened only to saveState, the channel, pathChanged and the dirty flag going down (src/watcher.ts:108-125), so no read ran, and the branch that lets a held change go when the document holds the disk text (src/watcher.ts:365-370) left the dirty flag set
  - log: 2026-09-10T16:25:48Z @kj added
  - log: 2026-09-10T20:41:44Z @kj closed
- [x] `DEF-CUE-85` **A write that lands while a reload is read is held behind the reloaded revision's stale dirty flag** - MEDIUM; with a change held back over unsaved edits, Reload from Disk loads the held revision into the document and, as JupyterLab's Context._revert clears no dirty flag, leaves the flag set with nothing unsaved behind it; a write that lands on disk before the read the reload started resolves is then held again (shape B), and after a reload with nothing held the next write is held behind the same stale flag (shape A); nothing releases either until the reader saves or reloads once more; found by the bug-hunter lens of the round-32 review as two shapes of DEF-CUE-79
  - evidence: the hold test in FileWatcher._read holds a write only while the document is dirty and its text differs from both the held revision and the text last loaded, applied or saved (src/watcher.ts); jest 'applies a write that landed while the reload was being read, the held revision being what the document holds' (shape B) and 'applies the next write after a reload that loaded the file over typed text with nothing held' (shape A) in `src/__tests__/watcher.spec.ts`, with 'keeps holding the change when the reader typed between the reload and the read' guarding the hold; mutations stale-flag-holds-the-write and every-dirty-document-takes-the-write (logs/round34-mutations.log), shadow-clause-removed and typed-text-taken-for-the-shadow (logs/round35-mutations.log) each caught; Galata 'takes the next write after Reload from Disk over typed text with nothing held (DEF-CUE-85)' in ui-tests/tests/live-view.spec.ts failed on the round-34 build at A third paragraph appeared (logs/galata-106-proof-def-cue-85-shape-a.log) and is green on the round-35 build of 1.0.6 with jest 607 of 607, pytest 29 of 29, Galata 113 of 113 and 46 of 46 (logs/round35-chain.log); limit of shape B, found by review round 6 (bug-hunter) by code trace and recorded here without a code change: where Context._revert loads a revision that is neither the held one nor the shadow, the agent having written again between the held write and the reload's own read, the hold test has no escape, so the document stays dirty over a file revision the reader never typed and every later write is held until a second Reload from Disk with the file quiet, which recovers through the equal branch; the obvious remedy, setting the shadow from the model in _onFileChanged, is unsafe and not taken, since Context._updateContentsModel in @jupyterlab/docregistry 4.6.3 emits fileChanged from the rename path and the collaborative-save path as well, where the model holds the reader's typed text, and would record typed text as a file revision and reopen the DEF-APPLY-90 class of silent overwrite
  - related: DEF-CUE-79,ACC-CUE-30
  - repro: shape A: type in the editor, choose File, Reload Markdown File from Disk and confirm, then let the process write the file: the red square shows and the write is not shown; shape B: hold a change over unsaved edits, reload, and have the process write again before the watcher's read returns: the same
  - test-tags: UNIT, E2E
  - root-cause: 2026-09-10T21:49:48Z @kj the hold test in FileWatcher._read (src/watcher.ts) reads the dirty flag alone, and after a reload the flag says nothing about unsaved work: the document holds the revision the watcher was holding back (shape B) or the text the watcher last loaded, applied or saw saved (shape A), and neither holds anything the reader typed
  - log: 2026-09-10T21:20:07Z @kj added
  - log: 2026-09-10T21:20:07Z @kj fix in the tree: the hold test is model.dirty and the document text differing from the held revision; jest 'applies a write that landed while the reload was being read, the held revision being what the document holds' passes; round-34 chain running
  - log: 2026-09-10T21:49:48Z @kj edited text and repro (replaced)
  - log: 2026-09-10T21:49:48Z @kj shape A fixed at the Star Colonel's word: the hold test also compares the document text with the watcher's shadow, the text it last loaded, applied or saw saved; the seven watcher cases that faked unsaved edits by flipping the flag now type; jest 'applies the next write after a reload that loaded the file over typed text with nothing held' passes
  - log: 2026-09-10T22:07:34Z @kj closed
  - log: 2026-09-11T09:58:24Z @kj edited evidence (replaced)

## Comments `NOTES`

Marks, note lines and the notes panel

- [x] `DEF-NOTES-16` **Panel hides itself when the last mark is taken away** - MEDIUM; MEDIUM; when an external rewrite removes the markers of the only mark in a document, the panel closes, so the unanchored mark the reader should still see is off screen
  - evidence: unit 'stays open when a rewrite takes the only mark away' and 'opens when the first mark arrives in a document that had none' in `src/__tests__/notes.spec.ts`, and Galata 'ACC-NOTES-59 lists a mark whose markers a rewrite removed' in ui-tests/tests/notes.spec.ts, now written as the single-mark case; reverting the fix fails the first unit test and the browser test while both controls pass, and the naive first-read-only fix fails the second
  - repro: mark one passage, then write the file externally without the markers: the panel closes and the remembered mark is invisible
  - test-tags: UNIT
  - root-cause: 2026-09-07T00:38:47Z @kj NotesController._read applies the opening-state rule on every content change rather than only on the first read, so a document with no settings marker flips to hidden as soon as its mark count reaches zero
  - log: 2026-09-07T00:38:47Z @kj added
  - log: 2026-09-07T01:31:42Z @kj closed
- [x] `DEF-NOTES-17` **A marker that splits a word paints the passage short** - MINOR; MINOR; a closing marker written by hand or by an agent between a word and its punctuation makes the painted highlight stop early, covering only the first words of the passage
  - evidence: two cases in `src/__tests__/anchor.spec.ts` written failing first; restoring containment fails exactly those two and passes the other 46, and widening the filter by a thousand characters either side fails six, so the boundary is pinned in both directions
  - repro: hand-write a mark around 'apples and pears' inside the sentence 'apples and pears.', open the preview: the highlight covers only 'apples and'
  - test-tags: UNIT
  - root-cause: 2026-09-07T01:31:42Z @kj measured: the source scanner emitted one token for the whole word including the punctuation the marker split, so its end lay past the passage, and the passage kept only tokens wholly inside it and dropped that word; the two-word fallback did not shorten the highlight, contrary to the first reading; the passage now selects its source tokens by overlap, so a word a marker splits belongs to the passage whole
  - root-cause: 2026-09-07T00:38:47Z @kj the source token is 'pears' while the rendered word is 'pears.', so locate fails on the full sequence and falls back to matching the first two words; the extension's own writer snaps to whole words, so only markers written by hand or by an agent reach this
  - log: 2026-09-07T00:38:47Z @kj added
  - log: 2026-09-07T01:31:42Z @kj closed
- [x] `DEF-NOTES-18` **A note line can carry an opaque user id instead of a name** - MINOR; MINOR; where the lab reports a named identity whose username is an opaque identifier rather than a login name, the note line opens with that identifier, which tells a later reader nothing about who wrote it
  - evidence: three cases in `src/__tests__/notes.spec.ts` written failing first; the rule is pinned in both directions - a predicate that never fires fails all three, one that also fires on a plain login name fails 'writes the lab identity when the setting is empty', and dropping the two name candidates fails the fallback pair
  - repro: open a lab whose identity carries a generated username and a display name, add a note, read the line: it opens with the generated username
  - test-tags: UNIT
  - root-cause: 2026-09-07T00:38:59Z @kj the author is taken from the identity username without asking whether it reads as a handle; ACC-NOTES-104 asks for the hub login name, which a generated identifier is not
  - log: 2026-09-07T00:38:59Z @kj added
  - log: 2026-09-07T01:31:42Z @kj closed
- [x] `DEF-NOTES-28` **Add note and the passage click open no note entry while the panel is minimap or hidden** - MAJOR; With the panel in the minimap or hidden state, the Add note command and a click on a marked passage select the mark and ask for a note entry, but those states render no rows, so the entry never appears and the reader gets nothing; reproduced by the round 1 sceptic
  - evidence: unit tests 'asks for the expanded state when the entry is opened from the minimap' and 'while hidden' in `src/__tests__/notes-panel.spec.ts` and 'expands a minimap panel when the note entry is asked for' in `src/__tests__/wiring.spec.ts` fail with the setState ask removed; Galata 'DEF-NOTES-28 opens the note entry from the marked passage' fails with the ask removed from the installed bundle; green on build 0.6.31
  - repro: open a document whose settings marker says panel=minimap, click a marked passage, look for the note textarea
  - test-tags: UNIT, FUNCTIONAL
  - root-cause: 2026-09-07T09:01:36Z @kj NotesPanel.selectMark opens a note entry without leaving the minimap or hidden state, whose render builds no rows, so the entry the reader asked for never appears
  - log: 2026-09-07T09:01:36Z @kj added
  - log: 2026-09-07T10:04:33Z @kj closed
  - log: 2026-09-07T10:04:33Z @kj fixed in NotesPanel.selectMark: with openNote and a state other than expanded it asks the handler for the expanded state first, so both routes to the entry are covered by one line
- [x] `DEF-NOTES-29` **The note caret jumps to the end of the draft on every external write** - MAJOR; The panel is rebuilt on every applied write; the textarea is a new node carrying the draft text and the focus but not the caret, so a reader typing a note mid-sentence while the agent writes finds every further keystroke landing at the end; reproduced by the round 1 sceptic (the list scroll position is kept, only the caret moves)
  - evidence: unit test 'keeps the caret where it was through a change of the document' in `src/__tests__/notes-panel.spec.ts` fails with setSelectionRange removed (caret 11 instead of 5); Galata 'DEF-NOTES-29 keeps the caret where it was through an external write' fails with the call removed from the installed bundle; green on build 0.6.31
  - repro: open a note entry, type a sentence, put the caret in its middle, rewrite the file externally, type one character
  - test-tags: UNIT, FUNCTIONAL
  - root-cause: 2026-09-07T09:01:36Z @kj NotesPanel._render recreates the note textarea on every setMarks and carries only its text and focus, so the caret moves to the end of the draft on every external write
  - log: 2026-09-07T09:01:36Z @kj added
  - log: 2026-09-07T10:04:33Z @kj closed
  - log: 2026-09-07T10:04:33Z @kj fixed in NotesPanel._render: the caret of a focused textarea in the body is kept and restored on the new textarea after focus; no scroll handling, the list body node persists
- [x] `DEF-NOTES-32` **Panel buttons are announced by their glyph instead of their title** - MEDIUM; button() sets a title but no aria-label, so assistive technology announces the glyph (multiplication x) for close, expand, collapse, the colour dots, Save, Cancel and Remove
  - evidence: unit test 'names every button for assistive technology by its title' in `src/__tests__/notes-panel.spec.ts` (ten buttons) fails with the aria-label line removed; green on build 0.6.31
  - repro: inspect any panel button: title set, aria-label absent
  - test-tags: UNIT
  - root-cause: 2026-09-07T09:01:37Z @kj button() in src/notes-panel.ts names the panel buttons by their glyph text and sets no aria-label
  - log: 2026-09-07T09:01:37Z @kj added
  - log: 2026-09-07T10:04:33Z @kj closed
  - log: 2026-09-07T10:04:33Z @kj fixed in button() in src/notes-panel.ts: aria-label set to the title
  - log: 2026-09-07T18:16:38Z @kj confirm A on 0.6.37: two titles reworded so the visible label is a prefix of the accessible name (WCAG 2.5.3) - 'Add note to this mark', 'Cancel this note'; notes-panel.spec 'names every button for assistive technology by its title' asserts the prefix for every worded button
- [x] `DEF-NOTES-33` **A mark saved during a write stream faster than one write per 200 ms raises File Changed** - MAJOR; Marking while the agent streams writes: at 40-100 ms spacing 1 to 2 marks in 10 raise the File Changed dialog; Overwrite discards the agent's lines since the mark started, Cancel holds the preview until the document is saved or reverted, Revert keeps the agent's text; one line was lost with no dialog at 100 ms; 0 of 18 at 200 ms and 0 of 6 at 1 s; reproduced by the round 1 sceptic. ACC-NOTES-100 is proven for writes spaced 200 ms or more apart and open below that
  - evidence: Galata ui-tests/tests/stream.spec.ts 'DEF-NOTES-33 writes a mark through the route and not through a save' (one POST /write 200, no PUT, no dialog, clean tab) and 'DEF-NOTES-33 ten marks under a 50 ms stream raise no dialog and reach the file' - 0 dialogs and 10 of 10 marks in 8 of 8 runs at 50 ms spacing on 0.6.36 (the earlier record: 1-2 dialogs in 10 at 200 ms); Galata 110 of 110, jest 446, pytest 29; four bundle mutations of _write each caught by exactly their tests with a control green (logs/proving-round3-mutations.log); residual loss inside the server's write window recorded in the log above and in README
  - related: ACC-NOTES-100 - the guarantee this defect bounds
  - repro: stream writes every 50 ms to an open file, mark a passage ten times, count File Changed dialogs and lost lines
  - test-tags: FUNCTIONAL
  - root-cause: 2026-09-07T13:59:51Z @kj A mark was saved through Context.save, whose check-then-PUT reads the file's revision, compares it with the model's and writes in two separate requests; an external write landing between them raised File Changed, and a write landing before the comparison was overwritten by the PUT with no dialog
  - root-cause: 2026-09-07T09:01:37Z @kj The mark's save goes through the Context, whose check-then-PUT leaves a window in which a write lands between the hash check and the PUT; a stat before save only narrows it
  - log: 2026-09-07T09:01:37Z @kj added
  - log: 2026-09-07T09:21:51Z @kj round 2 design: no client-side edit closes the Context's check-then-PUT windows; a server write route removes the dialog but not the loss; the bound at 200 ms spacing is accepted for now and the route design is in docs/defects/DEF-NOTES-33-atomic-write-route.md, decision for the Star Colonel
  - log: 2026-09-07T10:18:39Z @kj Star Colonel's decision 2026-09-07: fix it - the server compare-and-write route from docs/defects/DEF-NOTES-33-atomic-write-route.md is being built, server half first, client half after the round 2 executors leave src/notes.ts
  - log: 2026-09-07T10:28:03Z @kj server half built: WriteHandler on POST .../write with @authorized write on contents; FileWatchRegistry.swap takes a per-path lock, compares the file's hash (the contents manager's own algorithm over the raw bytes, the value the client holds as contentsModel.hash) with expected, answers 409 with the current content on a mismatch and otherwise writes in place keeping the inode so an appender holding the file open keeps writing to it; five pytest cases, 29 passing, the lock and the comparison each broken and watched failing
  - log: 2026-09-07T12:50:17Z @kj round 2 proof on 0.6.34 with DEF-NOTES-34 fixed: the reader's gesture now reaches every mark and the 200 ms bound still does not hold - stream.spec.ts at 200 ms spacing gave 2 dialogs and 1 lost line in 10 marks in run 1 and 1 dialog and 0 lost in run 2; README's '0 of 18 at 200 ms' is not reproducible; server half of the route is in the tree (routes.py WriteHandler, watch.py swap, 5 pytest cases), client half next
  - log: 2026-09-07T13:59:51Z @kj client half built (round 3, build 0.6.36): src/notes.ts _write takes its edits as a function of the current source and, for a clean document with the route present, POSTs the new text to <base>/jupyterlab-advanced-markdown-viewer-extension/write with expected = context.contentsModel.hash; 200 applies the same edits through the MARK_ORIGIN transaction, marks the model clean and refreshes with no Context.save; 409 refreshes and retries, the third refusal takes the Context save path so a lab with live updates off meets File Changed as before (DEF-CONFIG-23); an HTML-bodied 404 is remembered as route absent for the session; a dirty model is unchanged (transaction only). fetchAPI in src/request.ts, serverSettings from src/index.ts. Residual, in the server's read-to-write window of watch.py swap (a few milliseconds): at 50 ms write spacing 1-2 of 120 appended lines were lost in 3 runs of 8, 0 in the other 5, each traced to a line appended inside that window; the design document names this as the loss a lock-free external writer cannot be protected from without cooperating
  - log: 2026-09-07T13:59:51Z @kj closed
  - log: 2026-09-07T21:18:32Z @kj round 4, declined and recorded rather than fixed: seen from the reader's side, the residual loss makes NotesController._write answer true for a mark that reached neither the document nor the file, because the guard reads only that the route answered 200 and the document has since moved, and the panel clears the reader's draft on that answer. Closing it means the write route reporting what the file holds after the write rather than that the write returned, which is server work outside this defect's fix; the loss itself is bounded by the server's own write window and named in the README
  - log: 2026-09-07T21:57:37Z @kj round 5 overturns the round 4 decline recorded above: the bug hunter reproduced this symptom with no server residual involved at all - the guard alone produced it, and the remedy was a client-side one-line change, not the server work the decline claimed. Filed and fixed as DEF-NOTES-39. The decline's premise was wrong; the loss inside the server's own write window that this defect names is a separate and still-real residual
- [x] `DEF-NOTES-34` **Every applied write collapses the reader's selection and detaches the context-menu hit node** - MAJOR; Each render replaces the rendered nodes, so the selection lives 530-1010 ms at one write a second and a context-menu gesture slower than about half a second after a write marks nothing, silently, because marking() finds its attachment through the stale hit-test node; under a stream of writes a passage can never be marked; reproduced by the round 1 sceptic
  - evidence: Galata notes.spec.ts 5 DEF-NOTES-34 tests pass on 0.6.34 (mark after a write between menu and choice; selection kept through a write after it and a longer write before it; caret left alone while the preview holds a selection; palette item marks after the focus move); 5 bundle mutations each fail exactly their target with a control green (logs/proving-selection-mutations*.log); jest notes.spec and wiring.spec 440 passing; under the 200 ms stream of stream.spec.ts all 8-9 menu marks now land
  - related: ACC-NOTES-100 - the guarantee this defect bounds; ACC-NOTES-114 - keyboard marking needs the same selection carry
  - repro: stream writes once a second, select a passage, right-click, wait one second, choose Mark: nothing is marked
  - test-tags: FUNCTIONAL
  - root-cause: 2026-09-07T12:50:17Z @kj Each applied write re-renders the preview, replacing every rendered node; the browser selection lived on the old nodes and collapsed, and the context-menu hit node the commands resolved their preview through was the detached old node, so marking() found nothing
  - root-cause: 2026-09-07T09:01:37Z @kj The selection is not carried across renders and marking() resolves its attachment from the contextMenuHitTest node that the render detached, and mark() holds a live Range across its own refresh
  - log: 2026-09-07T09:01:37Z @kj added
  - log: 2026-09-07T12:50:17Z @kj fix on 0.6.34: the selection is recorded on selectionchange as offsets into the captured text (src/notes.ts _selection), carried across renders and settles through the word diff (mapOffsets(diffWords)) and restored while the page or the viewer holds the focus; selectionToSource split into selectionOffsets and renderedToSource (src/anchor.ts); the commands resolve their preview from the hit test with a tracker.currentWidget fallback (src/index.ts target())
  - log: 2026-09-07T12:50:17Z @kj closed
  - log: 2026-09-07T13:59:51Z @kj round 3 proof: get selection() reads the window on the ask as well as on the selectionchange event, because Chromium delivers the event a frame or more after the selection moved and a context menu opened at once asked before it had (ACC-NOTES-45 flaky 6 of 10 on 0.6.35 with the re-run removed, 16 of 16 on 0.6.36 with it restored; bundle mutation dropping the re-run fails 3 of 8 repeats)
- [x] `DEF-NOTES-35` **A note typed into the panel is discarded without a word when the mark's markers vanished before Save** - MEDIUM; addNote and _rewrite cannot say whether the write found the markers; Save clears the draft regardless, so a note written while the agent removed the markers is lost and the row only shows unanchored; confirmed by reading src/notes-panel.ts:465-472 and src/notes.ts:836
  - evidence: Galata notes.spec.ts 'DEF-NOTES-35 keeps a note whose mark vanished before Save' passes on 0.6.34 with a 1.5 s wait for the save round trip (the first version asserted before the answer and passed on the unfixed path; repaired by the proving run); 2 bundle mutations caught (_rewrite answering true for a lost mark; panel clearing the draft regardless); jest notes.spec 'addNote says whether the markers were found' and notes-panel.spec 'keeps the draft when the note could not be written'
  - repro: open a note entry, remove the mark's markers externally, type a note, press Save
  - test-tags: UNIT
  - root-cause: 2026-09-07T12:50:17Z @kj _rewrite resolved void whether or not the opening marker was found in the source, so addNote could not report a lost mark and the panel's Save handler cleared the draft unconditionally
  - root-cause: 2026-09-07T09:01:37Z @kj addNote and _rewrite in src/notes.ts return nothing about whether the markers were found, so the panel's Save handler clears the draft unconditionally
  - log: 2026-09-07T09:01:37Z @kj added
  - log: 2026-09-07T12:50:17Z @kj fix on 0.6.34: _rewrite and addNote resolve boolean (false when disposed, when no mark of that id with an opening marker is in the source, or when its type is not note); INotesPanelHandlers.addNote returns Promise<boolean>; the Save handler keeps the entry until the answer and clears the draft only on true. Named, not fixed: a second Save click while the first write is in flight writes the note twice (window: one save round trip)
  - log: 2026-09-07T12:50:17Z @kj closed
  - log: 2026-09-08T21:45:54Z @kj the ruling is unreachable from 1.0.6: under ACC-NOTES-144 a mark whose markers a rewrite removed is gone from the panel, so the row that held the draft goes with it and there is no row left to keep the note in front of the reader; the fix that closed this defect stands in the code, and its Galata case was rewritten under ACC-NOTES-144 to assert the row, the entry and the draft all leave together
- [x] `DEF-NOTES-36` **Route write applied twice when the document moved before the 200** - MAJOR; NotesController._write computes its edits against the source captured before the POST and transacts them after the await without checking that the document still holds that source; when the watcher's read-back of the written file (100 ms coalesce plus one GET) completes before a delayed 200, or the reader types during the POST, the stale transact doubles the markers, marks the model clean while it differs from disk, and the next mark writes the garbled text to disk; reproduced by the architect on the installed 0.6.37 with a Playwright probe delaying the /write response by 120, 400 and 900 ms (modelEqualsFile false, interleaved markers, dirty false), found independently by the bug hunter at MEDIUM
  - evidence: fixed on 0.6.38 by a guard in NotesController._write (if landed and this._source differs from the captured source: refresh and return, no transact and no dirty = false); jest notes.spec 'leaves the document alone when the watcher applied the written file before the 200' and 'keeps the reader's edits when they typed while the route wrote', both written failing first (transact in the order, two marker pairs / dirty false); Galata notes.spec 'DEF-NOTES-36 applies the route's edits once when the watcher read the file back first' (route answer delayed 400 ms) green in the full suite of 113; installed-bundle mutation removing the guard fails the pin on toHaveLength while ACC-NOTES-100 stays green, dirty = false inside the guard fails the typing test, the guard reduced to if (landed) fails the two plain-200 route tests, the refusal limit 3 to 4 as negative control is caught by nothing; browser probe at STALL 400 gives one marker pair and model equal to file, at STALL 900 with typing keeps dirty true. Named consequence: when the reader typed during the POST the mark is on disk and not in the document until the reader's next save answers the File Changed dialog - the never-overwrite-unsaved-edits outcome
  - test-tags: UNIT, FUNCTIONAL
  - repro: scratch jupyter server over a scratch root, open the preview, select a word, page.route on /write delaying only the response by 400 ms, right-click and mark: the model holds two marker pairs and differs from the file
  - root-cause: 2026-09-07T19:14:01Z @kj _write transacts edits computed before the route POST after the await with no check that the document still holds the source they were computed against; the watcher's own read of the written file or the reader's typing moves the document first
  - log: 2026-09-07T19:14:01Z @kj added
  - log: 2026-09-07T19:14:13Z @kj edited test-tags (added)
  - log: 2026-09-07T19:43:27Z @kj closed
  - log: 2026-09-07T21:17:23Z @kj regressed as DEF-NOTES-36-1
- [x] `DEF-NOTES-36-1` **Guard path returns before the mark re-read, so Add note never opens the note box** - MEDIUM; The DEF-NOTES-36 guard in NotesController._write returns true after _refresh() without calling _flush(), which the write path below it calls for the stated reason that the caller is handed an id and a command acts on it straight away. _onContentChanged defers the mark re-read one animation frame, so on the guard path NotesController.marks is still the pre-write list when mark() resolves. COMMANDS.addNote in src/index.ts receives the new id and calls panel.selectMark(id, true), which returns at its first line because the panel's items do not hold the mark yet: on the delayed-200 case the guard exists to serve, marking a passage from the command writes the mark and never opens the note box
  - evidence: fixed by calling this._flush() on the guard path before it returns, the same re-read the write path below performs; jest notes.spec 'leaves the document alone when the watcher applied the written file before the 200' now asserts controller.marks holds the returned id when mark() resolves; the assertion fails with the flush taken back out (1 of 448) and passes with it, jest 448 of 448 and pytest 29 of 29
  - test-tags: UNIT
  - repro: mark a passage with the /write route answering 200 after the watcher has already brought the written file in; mark() resolves with an id the notes panel cannot select
  - log: 2026-09-07T21:17:23Z @kj regression of DEF-NOTES-36: the guard shipped in 0.6.38 without the mark re-read the write path does, found on three review lenses in round 4
  - log: 2026-09-07T21:17:31Z @kj edited title and text and severity and repro (added) and test-tags (added)
  - log: 2026-09-07T21:17:45Z @kj closed
  - log: 2026-09-07T21:58:42Z @kj round 5: the closure above proves the flush for the branch where the watcher applied the written file, and its title claims the symptom in general. The other branch - the document moved elsewhere - kept the same symptom until DEF-NOTES-39 closed it on 0.6.39 by computing the answer instead of assuming it
- [x] `DEF-NOTES-38` **Reduced motion removes the only signal locating a chosen mark** - MEDIUM; The mark flash is a background-colour ramp - @keyframes jp-AdvancedMd-mark-flash changes background-color alone, 900 ms per iteration, two iterations, so 1.11 flashes per second and 2 in any one-second window against WCAG 2.3.1's limit of 3 - but a prefers-reduced-motion rule set animation: none on it. DEF-HILITE-31 had already settled the opposite reading for this project, keeping the removal ghost's opacity fade under the same preference on the ground that colour and opacity ramps are not motion, and that reading was not applied here. With the operating system set to reduce motion, choosing a row in the notes panel scrolled the passage to the centre of the view with nothing marking which passage it was
  - evidence: fixed on 0.6.38 by removing the @media (prefers-reduced-motion: reduce) block over .jp-AdvancedMd-markFlash from style/base.css; nothing else in that block changed, and the tab markers keep their own reduced-motion rule (ACC-CUE-72), which covers marks that really do move; jest notes-panel.spec 'keeps the flash under reduced motion, and it ramps nothing but a colour' asserts both that no reduced-motion rule names the flash class and that the keyframes declare background-color alone; the test fails with the rule put back (1 of 448) and passes without it, jest 448 of 448 and pytest 29 of 29
  - repro: emulate prefers-reduced-motion: reduce, choose a row in the notes panel: the view scrolls and no flash locates the passage
  - test-tags: UNIT
  - root-cause: 2026-09-07T21:17:45Z @kj The reduced-motion block was written with the tab markers, which do move, and covered the flash by proximity rather than by the rule the project applies to colour ramps
  - log: 2026-09-07T21:17:45Z @kj added
  - log: 2026-09-07T21:17:52Z @kj closed
  - log: 2026-09-07T21:58:30Z @kj edited text
  - log: 2026-09-07T21:58:30Z @kj round 5, two lenses: the rate was recorded as 0.55 Hz, which counts the whole two-iteration run as one flash. One 900 ms iteration is one flash, so the rate is 1.11 Hz and the margin against the WCAG 2.3.1 limit is 2.7x, not 5.4x. The conclusion is unchanged; the figure is corrected because it is the standing licence for the flash's envelope and a contributor halving the duration against a doubled margin would land near the threshold. WCAG 2.2.2 does not apply: 1.8 s is inside its five-second trigger and the flash is reader-initiated
- [x] `DEF-NOTES-39` **Write route answers success for a mark the document never took, and the panel throws the reader's note away** - MAJOR; NotesController._write returns true from the guard that fires when the route answered 200 and the document has since moved. Two different states reach it: the watcher brought in the file the route wrote, or the document moved to something else - the reader typed in the editor, or a second external write landed after the server wrote and before the answer came back. On the second the mark is on disk and not in the document, and true is a lie. NotesPanel's Save button clears this._entry only on saved === true, so the reader types a note, presses Save, the box closes as though written, and the note exists in no document: their next save either overwrites disk and destroys it or raises File Changed, where Overwrite loses the note and Revert loses their typing. Reproduced by the round 5 bug hunter and ux lenses independently, with probes on a copy of the tree: addNote returned true, the open document held zero notes, dirty true, saves 0
  - evidence: fixed on 0.6.39: the guard in NotesController._write returns this._source === applyEdits(source, changes) instead of true, so the caller is told whether the document actually holds what was written; applyEdits is the same helper that builds the content the route posts, so the two halves are compared in the same space. mark() then returns null and addNote() false on the branch where the document moved elsewhere, and NotesPanel keeps the draft. Named cost: if the watcher applies the written file AND one further external change inside the same window the answer is false although the mark landed, so the note box does not open on a mark the reader can see - a false negative in place of a silent loss. jest notes.spec 'keeps the reader's edits when they typed while the route wrote' now asserts mark() returned null, and a new test 'answers no when a second external write landed inside the route write' covers the other way in; the mutation restoring return true fails both (2 of 451) while 'leaves the document alone when the watcher applied the written file before the 200' stays green, so the guard still answers yes on the branch that did write
  - repro: mark a passage, open its row, type a note, and have an external write land on the file inside the /write POST round trip: the note box closes and the document holds no note
  - test-tags: UNIT
  - root-cause: 2026-09-07T21:57:25Z @kj The guard was written for the watcher-read-back state alone and answered for the state it was written for rather than for what it could observe; from 0.6.39 the answer is computed, not assumed
  - log: 2026-09-07T21:57:25Z @kj added
  - log: 2026-09-07T21:57:37Z @kj closed
- [x] `DEF-NOTES-40` **The mark flash is invisible in the dark theme, where it is the only signal locating a chosen mark** - MAJOR; The flash keyframe sets background-color on the mark span itself, so during the flash the mark's own colour is replaced rather than covered and the flash composites over the page, not over the mark. Over the dark layout colour the mid grey rgb(128 128 128 / 55%) lands at relative luminance 0.0763 while the default yellow mark sits at 0.0773: a difference of 0.0010, a tenth of one percent of maximum luminance. Blue 3.9%, pink 3.5%, orange 3.9%; the same flash over the light theme is 15.6% to 38.3%. Peripheral vision is luminance-driven, so an isoluminant hue swap is undetectable outside the point already being looked at - which is exactly where the flash is not needed, because the panel has just jumped the view to a passage the reader has not located. A dark-theme reader with many marks in a long document chooses a row, the view jumps, and nothing tells them which passage they chose
  - evidence: fixed on 0.6.39 by theming the flash grey through --jp-AdvancedMd-mark-flash-bg, the same :root / body[data-jp-theme-light='false'] pair every other colour in the stylesheet uses: rgb(128 128 128 / 55%) over light, unchanged, and rgb(220 220 220 / 45%) over dark. Dark now gives a minimum luminance step of 7.4 percent against any of the four mark colours, up from 0.1, with white content text at 5.22:1 which is above AA; light is unchanged at 15.6 to 38.3 percent and 10.7:1. jest notes-panel.spec 'flashes light enough to be seen away from where the eye already is' composites the flash over the page rather than over the mark, which is what the keyframe does, and asserts a step above 0.05 for every colour on both themes plus text contrast above 4.5:1. Two mutations caught: the dark value put back to the mid grey, and the theming removed altogether. Recorded: an earlier version of this test composited the flash over the mark, passed the mid-grey mutation, and was rewritten - the escape is why the model is spelled out in the test's own comment
  - repro: in the dark theme, with two yellow marks on screen, choose one of their rows in the notes panel: the scroll happens and no flash is perceptible
  - test-tags: UNIT
  - root-cause: 2026-09-07T21:57:48Z @kj The grey was chosen against the light theme and left unthemed, while every other colour in the stylesheet carries a dark counterpart; the reasoning also assumed the flash layers over the mark, where the keyframe in fact replaces the mark's own background-color
  - log: 2026-09-07T21:57:48Z @kj added
  - log: 2026-09-07T21:58:05Z @kj closed
- [x] `DEF-NOTES-41` **A passage flashed for an earlier row keeps the class and flashes again unasked** - MINOR; NotesPanel._reveal clears the single pending timeout but never takes the flash class off the passages the previous selection put it on, and only the newly selected passages get a removal scheduled. A passage chosen and then left inside the flash window keeps the class indefinitely. The class is inert until the animation restarts, which Lumino's display:none tab hide does on return: the reader picks row A then row B while scanning the list, switches tab and comes back, and passage A flashes with no action on A. Adding a class an element already carries also restarts no animation, so the same omission made the neighbouring test's name - 'restarts a flash rather than letting the first one end it early' - describe something the code did not do
  - evidence: fixed on 0.6.39: _reveal takes the flash class off every element in the render root before putting it on the passages just chosen, so one set is always tracked and the animation genuinely restarts on the passage the reader picked. jest notes-panel.spec 'takes the flash off the passage it left when the reader picks another' asserts the first passage is clear and the second lit while the window is still open, and the mutation removing the clearing loop fails it (1 of 451); the neighbouring test 'restarts a flash rather than letting the first one end it early' now describes what the code does
  - repro: select a mark's row, then another within 1.8 s, then hide and show the tab: the first passage flashes
  - test-tags: UNIT
  - root-cause: 2026-09-07T21:58:05Z @kj One timeout is held for the whole panel while the class is put on a set of elements that changes with every selection, so the removal and the addition were tracking different sets
  - log: 2026-09-07T21:58:05Z @kj added
  - log: 2026-09-07T21:58:17Z @kj closed
- [x] `DEF-NOTES-42` **The guard path skipped the render, so the note box opened over a passage with no mark on it** - MEDIUM; The DEF-NOTES-36 guard in NotesController._write returned above the this._widget.content.update() that every other path runs, and the comment beside that call states the viewer otherwise waits out its own render timeout before the mark appears. The guard is the path taken when a watcher change is in flight, which is precisely when a reader marks a passage during a stream. The reader selects a phrase, chooses Add note, the panel expands and focuses an empty textarea, and the passage in the preview carries no highlight at all until the viewer's timeout runs - they are asked to write a note about a mark they cannot see
  - evidence: fixed on 0.6.39 by moving the existing this._widget.content.update() above the guard's return, so both paths ask for the render at the same point. jest notes.spec 'leaves the document alone when the watcher applied the written file before the 200' now asserts content.update was called, and the mutation taking the call back out fails it (1 of 451). Not fixed and out of this defect: _reveal still runs against the render as it stands, so a mark whose span is not in the DOM yet is neither scrolled to nor flashed - the render is asynchronous on every path, which is pre-existing and why no Galata test asserts the flash
  - repro: mark a passage with the /write route answering 200 after the watcher has already brought the written file in, then look at the preview: no highlight until the viewer's own render timeout
  - test-tags: UNIT
  - root-cause: 2026-09-07T21:58:17Z @kj The guard was added as an early return and inherited none of the work the path below it does after the write
  - log: 2026-09-07T21:58:17Z @kj added
  - log: 2026-09-07T21:58:30Z @kj closed
- [x] `DEF-NOTES-44` **A render the viewer still needs is dropped when the document moved before a marker write** - MAJOR; the render-drop hook remembers what a marker write leaves in the document and drops the viewer's render of it, taking the render on screen for the render of the document the write changed; when a change was still waiting on the viewer's render timeout, typed beside the preview or brought in by the refresh sibling's revert, a mark, a note or a panel toggle written inside that second makes the hook drop the render that would have shown the change, and the preview holds stale text until the next change
  - evidence: NotesController._renderedSource gate: a write is remembered only when the render on screen is the render of the document it changed; jest 'lets the render through when the document moved before the write' and 'drops the render of a second mark written inside the window' (notes.spec), 464/464 via make test; mutations 'rendered-gate-never-refuses' and 'written-source-not-taken-as-rendered' each failed exactly those (logs/mutation-_.log); Galata ACC-NOTES-117 and the full suite 121/121 on 0.6.46 (logs/galata-0646-_.log)
  - repro: with editor and preview side by side type a word, and within one second click the notes panel button on the preview toolbar: the preview keeps the old text; jest 'lets the render through when the document moved before the write' on the code before the fix
  - test-tags: UNIT, E2E
  - log: 2026-09-08T10:49:06Z @kj added
  - log: 2026-09-08T11:38:21Z @kj closed
  - log: 2026-09-08T14:47:49Z @kj the notes panel button on the preview toolbar named in the repro was removed by ACC-NOTES-126; the toggle is now the panel entry of the context menu over the rendered view, or the palette; the repro text stays as the record of what was run
  - log: 2026-09-08T20:07:22Z @kj edited evidence (replaced)
- [x] `DEF-NOTES-45` **The note entry is unthemed in the dark theme** - MAJOR; the enlarged note textarea sets only width, box sizing and resize, so in the dark theme it is the browser's default field, a white box with black text in the default monospace font, the brightest object in the panel beside buttons that carry the panel's colours
  - evidence: style/base.css .jp-AdvancedMd-notesForm textarea now carries color: inherit, background var(--jp-layout-color0) and the panel's border; ACC-NOTES-118 Galata box assertions unchanged and green on 0.6.46 (logs/galata-0646-notes-siblings-settings.log)
  - repro: switch to the dark theme, open a mark's row, press Add note: the box is white with black text
  - test-tags: E2E
  - log: 2026-09-08T10:49:06Z @kj added
  - log: 2026-09-08T11:38:21Z @kj closed
- [x] `DEF-NOTES-46` **Add note scrolls the passage the reader is already at** - MAJOR; opening the note entry from a click on a painted mark or from Add note over a selection runs the panel's reveal, which scrolls the passage to the centre of the view and flashes it although the reader is looking at it; the view jumps under them as the entry opens
  - evidence: NotesPanel.selectMark reveals only when the entry is not being opened from the passage; jest 'leaves the reader at the passage when the entry is opened from it', mutation 'reveal-kept-on-open-note' failed exactly it (logs/mutation-reveal-kept-on-open-note.log); Galata 'ACC-NOTES-122 neither renders nor scrolls the preview from Add note through Save' green on 0.6.46
  - repro: scroll a long document to a passage, select it, choose Add note: the preview scrolls so the passage sits at the centre
  - test-tags: UNIT, E2E
  - log: 2026-09-08T10:49:06Z @kj added
  - log: 2026-09-08T11:38:21Z @kj closed
- [x] `DEF-NOTES-47` **Save and Cancel drop the keyboard focus to the page body** - MEDIUM; the panel rebuilds its whole body on every change, which destroys the control the reader had focused; only a textarea's focus was carried over, so after Save, Cancel, a colour dot, a toggle or Remove the focus falls to the page body and a keyboard reader restarts the tab order from the top of the document
  - evidence: NotesPanel._render records the row of the focused control before the rebuild and focuses the row built in its place; jest 'keeps the focus in the row after Cancel destroyed the pressed button' and 'keeps the focus in the row after Save' (notes-panel.spec), 462/462 via make test (logs/test-round2-fixes.log); mutation 'focus-not-returned-to-the-row' failed exactly those two (logs/mutation-focus-not-returned-to-the-row.log)
  - repro: open a row, Tab to Add note, press it, type, Tab to Cancel, press it: document.activeElement is body
  - test-tags: UNIT
  - log: 2026-09-08T10:49:06Z @kj added
  - log: 2026-09-08T10:52:00Z @kj closed
- [x] `DEF-NOTES-48` **A note continuing on a second line reads as a second note in the tooltip** - MINOR; the tooltip of a marked passage joins its notes with a line break and puts the author before each; a note whose text holds a line break shows its second line as a line of its own, without an author, as if it were another note
  - evidence: tooltip() runs the lines of a note together with a space; jest 'puts the notes on the painted span as its tooltip, one line each with its author' asserts a two-line note is one line with its author, 462/462 via make test; mutation 'tooltip-keeps-the-newlines-of-a-note' failed exactly that test (logs/mutation-tooltip-keeps-the-newlines-of-a-note.log)
  - repro: add a note of two lines to a mark and hover the passage: the tooltip shows three lines, the third with no author
  - test-tags: UNIT
  - log: 2026-09-08T10:49:07Z @kj added
  - log: 2026-09-08T10:52:00Z @kj closed
- [-] `DEF-NOTES-49` **Two marks inside one second can render the preview once in full** - MINOR; the write remembers the source it will leave before its server route call; when the viewer's render timeout for the first mark runs while the second mark's route call is in flight, the remembered source is the second mark's future text, the document still holds the first mark's text, and the hook takes that for a change and lets the render through: one full render with its image reload, which is what the preview did for every mark before the render-drop hook
  - repro: mark two passages within one second with the server route slow enough that the second call is in flight when the first mark's render timeout runs; the preview renders once in full
  - test-tags: UNIT
  - log: 2026-09-08T10:49:07Z @kj added
  - log: 2026-09-08T10:52:00Z @kj rejected: wontfix: the cost is one full render in a race of the server route's latency against the viewer's one-second render timeout, which is what every mark cost before the render-drop hook; answering it needs a second remembered source in the hook for a coincidence that harms no reader, and the adjudication of round 2 (tmp/campaign/siblings/round2/adjudication.md) ruled it non-material
- [x] `DEF-NOTES-50` **A bright unthemed ring frames the rendered Markdown after its tab comes back to the front** - MAJOR; in the dark theme, after the reader switches to another tab and back, a bright ring sometimes frames the rendered Markdown, panel open or hidden; the ring follows no theme colour and shows on some switches and not on others
  - evidence: style/base.css rule .jp-MarkdownViewer:focus { outline: none }, the rule JupyterLab applies to .jp-MimeDocument; Galata 'DEF-NOTES-50 draws no focus ring around the preview when its tab comes back to the front' asserts the viewer node is the active element with computed outline-style none after a tab switch in the dark theme and passes on 0.6.48 (logs/galata-0648-notes-siblings-settings.log: 81 passed, 1 failed, the failure being the siblings forced-render test of DEF-APPLY-37-1, which failed again on 0.6.50 and passed on 0.6.54); the same probe on 0.6.47 without the rule read outline auto 1px on the focused viewer in mouse, keyboard and shortcut switches (tmp/campaign/def50/); accepted cost: a keyboard reader who tabs into the preview has no visible focus indicator on it, as on JupyterLab's own mime documents
  - root-cause: 2026-09-08T11:49:09Z @kj JupyterLab's MarkdownViewer gives its node tabIndex 0 and focuses it on every activate-request; after a tab switch Chromium takes that programmatic focus for keyboard focus (:focus-visible) and draws its default focus ring, outline auto 1px, around the viewer node; JupyterLab suppresses the ring on .jp-MimeDocument but not on .jp-MarkdownViewer, and nothing in the extension's stylesheet did either
  - repro: dark theme, open a Markdown preview and a second document, click the second tab then the preview's tab: a bright ring frames the rendered Markdown
  - test-tags: E2E
  - log: 2026-09-08T11:44:48Z @kj added
  - log: 2026-09-08T11:49:09Z @kj edited title and text and repro (replaced)
  - log: 2026-09-08T12:35:35Z @kj closed
  - log: 2026-09-08T12:35:35Z @kj edited test-tags (replaced)
  - log: 2026-09-08T14:47:49Z @kj edited evidence (replaced)
- [x] `DEF-NOTES-51` **The passage leaves the view when Add note opens the panel beside it** - MAJOR; with the panel hidden, Add note from a passage near the bottom of the view expands the panel; the preview narrows by the panel's width, the text above the passage rewraps onto more lines and the passage moves below the view while the note entry takes the focus; the round-2 change that stopped the reveal on Add note left this case with nothing bringing the passage back
  - evidence: Galata 'DEF-NOTES-51 keeps the passage in view when the panel opens beside it' failed against the 0.6.48 bundle (passage bottom 1502 against a view bottom of 738) and passed on the 0.6.50 bundle (logs/galata-0650-notes-rerun.log, 73 passed); jest 'brings the passage back into the view when the panel opened over it' with stubbed rectangles; mutation passage-not-kept-in-view killed
  - repro: long document with the panel hidden: scroll a passage to the bottom edge of the view, select it, choose Add note; the passage is below the view
  - test-tags: E2E
  - root-cause: 2026-09-08T12:44:39Z @kj selectMark skips _reveal when the entry opens from the passage, and the fit the state change posts runs after selectMark returns, so nothing reads the new layout
  - log: 2026-09-08T12:44:39Z @kj added
  - log: 2026-09-08T13:29:42Z @kj closed
- [x] `DEF-NOTES-52` **Keyboard focus falls to the page body when a panel action takes the focused row away** - MAJOR; Remove pressed on a row, the switch to the minimap while a row is focused, and Hide pressed on the panel's own button all destroy or hide the focused control and nothing takes the focus, so document.activeElement is the body; the DEF-NOTES-47 fix put the focus back only on a row that survives the rebuild; the same refocus also scrolled the panel to the row on a rebuild a change from disk caused
  - evidence: src/notes-panel.ts _render records the focused row's index and focuses the row by id, else the row at that index, else the Hide button, all with preventScroll; _apply moves the focus to the viewer node before hiding the panel; jest 'moves the focus to the row at its place when Remove took the focused row away', '... to the Hide button when Remove took the last row away', '... to the Hide button when the rows become ticks', '... to the viewer when the panel is hidden from its own button', 470/470 via make test (logs/mutation-none-after-round3-fixes.log); mutations no-row-at-the-removed-rows-place and hidden-panel-keeps-the-focus each fail exactly their test (logs/mutation-*.log)
  - repro: panel expanded, one mark: Tab to the row's Remove button, Enter; document.activeElement is the body
  - test-tags: UNIT
  - root-cause: 2026-09-08T12:44:40Z @kj _render looks the focused row up by its mark id after the rebuild and does nothing when no row carries it; _apply hides the panel node with the focus still inside it
  - log: 2026-09-08T12:44:40Z @kj added
  - log: 2026-09-08T12:59:19Z @kj closed
- [x] `DEF-NOTES-53` **The render hook takes the current source for the one the render read** - MINOR; _onRendered sets the rendered source from the document at render completion; a typed change that lands while the viewer's render is in flight is then taken as rendered, a mark inside the next second remembers its write against it, and the render the viewer schedules for the typed change is dropped: the typed change is not on screen until the next change
  - evidence: the render-drop hook takes down the source when it lets the update request through (_requestedSource, gated on context.isReady) and _onRendered sets _renderedSource from that snapshot, falling back to the document only for a render no request preceded; jest 'keeps the source the render read when the document moved during the render' asserts the typed change's render goes through after a mark inside the window; mutation rendered-source-taken-at-completion (this._source ?? this._requestedSource) fails exactly that test (logs/mutation-rendered-source-taken-at-completion.log, re-run against the current source on 2026-09-08)
  - repro: editor beside the preview: an external write forces a render, type during it, mark a passage within a second; the typed text is not shown
  - test-tags: UNIT
  - root-cause: 2026-09-08T12:44:40Z @kj the update-request hook already records the source the render reads; the completion handler overwrote it with a source the render never saw
  - log: 2026-09-08T12:44:40Z @kj added
  - log: 2026-09-08T12:59:19Z @kj closed
  - log: 2026-09-08T15:05:59Z @kj edited evidence (replaced)
- [x] `DEF-NOTES-54` **The focused row and the note field draw the browser's own focus ring** - MINOR; no focus-visible rule exists for the panel row or the textarea, so after a keyboard Save or Cancel the row the panel focuses draws Chromium's unthemed ring, the bright frame DEF-NOTES-50 removed from the viewer, while the Save and Cancel buttons beside it draw JupyterLab's themed ring
  - evidence: style/base.css rule .jp-AdvancedMd-notesRow:focus-visible, .jp-AdvancedMd-notesForm textarea:focus-visible with the theme's --jp-focus-outline-width and --jp-focus-outline-color; Galata 'DEF-NOTES-54 draws the theme focus ring on a row the keyboard reaches' asserts the row holds keyboard focus with a solid outline in the theme's colour and passes on 0.6.50 (logs/galata-0650-notes-siblings-settings.log); the same test read outline auto against the 0.6.48 bundle without the rule
  - repro: dark theme, open a note entry from the keyboard, press Enter on Cancel; the row shows a white ring
  - test-tags: E2E
  - log: 2026-09-08T12:44:40Z @kj added
  - log: 2026-09-08T13:22:41Z @kj closed
- [x] `DEF-NOTES-55` **The note field has no accessible name** - MINOR; the textarea carries no aria-label, placeholder or label element while every button of the panel is named, so a screen reader announces an unnamed multiline field
  - evidence: the textarea carries aria-label Note (src/notes-panel.ts _form); jest 'names the note field for assistive technology'; mutation note-field-unnamed fails exactly that test (logs/mutation-note-field-unnamed.log)
  - repro: open a note entry with a screen reader running; the field is announced without a name
  - test-tags: UNIT
  - log: 2026-09-08T12:44:40Z @kj added
  - log: 2026-09-08T12:59:19Z @kj closed
- [x] `DEF-NOTES-56` **The wiring test's registry stand-in throws on the accessors the Mark submenu render reads** - MINOR; the stand-in answers isEnabled by calling a function the mark command does not declare, and lacks the other accessors Lumino's menu renderer reads; since the Mark submenu is a real Lumino Menu its posted render fails inside Lumino's exception handler and make test logs 32 TypeErrors under a passing suite
  - evidence: the stand-in in `src/__tests__/wiring.spec.ts` answers isEnabled, isToggled, caption, className, dataset, icon, iconClass, iconLabel, mnemonic and keyBindings as Lumino's registry does for a command that declares nothing; make test output holds 0 TypeError lines (logs/mutation-none-after-round3-fixes.log), 32 before (logs/test-submenu.log)
  - repro: make test; the output under PASS `src/__tests__/wiring.spec.ts` holds 32 TypeError lines
  - test-tags: UNIT
  - log: 2026-09-08T12:44:40Z @kj added
  - log: 2026-09-08T12:59:19Z @kj closed
- [x] `DEF-NOTES-57` **A mark lands on the passage selected after asking when the route falls back** - MAJOR; with the write route absent (a 404 on the first write of the session), the write path re-evaluates the edit callback on its fallback to the document path, and the callback reads the controller's selection record again; a reader who selected other words between asking for the mark and the 404 coming back gets the mark on those words
  - evidence: mark() reuses the selection record against the same source and re-reads it only when a refresh moved the source; jest 'keeps a selection the reader made while the write was on its way' put the markers around 'delta.' before the fix and around 'beta gamma' after it, with the later selection kept; Galata 'ACC-NOTES-47' marks three passages in a row on 0.6.54 (94 passed); Galata on the installed 0.6.54 bundle: 94 passed (logs/galata-0654-notes-siblings-settings.log) and 41 passed (logs/galata-0654-live-events-stream-cue.log); jest 488/488 via make test
  - test-tags: UNIT
  - repro: jest notes.spec 'keeps a selection the reader made while the write was on its way': hold the route's answer, mark 'beta gamma', select 'delta' while the route is pending, answer 404 - the markers land around 'delta.'
  - root-cause: 2026-09-08T14:13:20Z @kj NotesController.mark's edit callback reads this._selection on every call, and _write calls it once per loop turn: after a 409 refresh that is right, the refresh re-rendered and carried the record to new offsets, but on the route-absent fallback the source is unchanged and the record read is whatever the reader selected since
  - log: 2026-09-08T14:13:20Z @kj added
  - log: 2026-09-08T14:14:01Z @kj edited test-tags (added)
  - log: 2026-09-08T14:33:42Z @kj closed
- [x] `DEF-NOTES-58` **The Expand and Collapse controls drop the keyboard focus to the page body** - MAJOR; the Expand control on the minimap strip and the Collapse control in the expanded header each hide themselves through the state class once pressed, and a control that is display none loses the browser's focus, so a keyboard reader who presses Enter on either is left with the focus on the page body: a screen reader announces nothing and the next Enter acts on nothing; the focus guarantee DEF-NOTES-52 closed for Remove, the ticks switch and Hide did not cover the two header controls added after it
  - evidence: _apply in src/notes-panel.ts moves a focused control of the panel outside the body to the viewer when hiding, else to the Hide control every shown state keeps; jest 'collapses to the minimap from the control in the expanded header' and 'expands from the control on the minimap strip' assert document.activeElement is the Hide control after the click; mutation focus-guard-hidden-only fails exactly those two (logs/mutation-focus-guard-hidden-only.log); Galata ACC-NOTES-130 and ACC-NOTES-135 press Enter on each control and assert the Hide control is focused; Galata on the installed 0.6.56 bundle: 93 passed and 1 failed (logs/galata-0656-notes-siblings-settings.log; the failure is ACC-NOTES-127 at its 60 s timeout waiting for the Mark entry of the second context menu, rerun three times green in logs/galata-0656-rerun-acc127.log) and 41 passed (logs/galata-0656-live-events-stream-cue.log); jest 486/486 via make test; lint clean with 8 pre-existing warnings
  - repro: expand the panel, Tab to the Collapse control, press Enter: document.activeElement is the body; the mirror on the minimap strip with the Expand control does the same
  - test-tags: UNIT, E2E
  - root-cause: 2026-09-08T14:47:49Z @kj _apply in src/notes-panel.ts moves the focus only for the hidden state, and _render refocuses only a control inside the body; the header controls are siblings of the body and the state class hides them after the click
  - log: 2026-09-08T14:47:49Z @kj added
  - log: 2026-09-08T15:05:59Z @kj closed
- [x] `DEF-NOTES-59` **Both header carets show in every panel state on 0.6.55** - MAJOR; the expanded panel shows a 'Show notes' expand caret beside the collapse caret, and the minimap strip shows the collapse caret, on build 0.6.55; each control is meant for one state alone; found by the Star Colonel on the installed build
  - evidence: style/base.css: the header control rules are ordered so the bare rules for the expand and the collapse control set display none alone and the state-gated rule is the last of the group; jest 'shows the expand and the collapse controls in their own state alone' reads every rule naming a control without a state prefix and fails on the 0.6.55-shaped sheet (logs/mutation-def59-sheet-of-0655.log: 1 failed); Galata ACC-NOTES-130 and ACC-NOTES-135 assert each control is hidden in the other state; Galata on the installed 0.6.56 bundle: 93 passed and 1 failed (logs/galata-0656-notes-siblings-settings.log; the failure is ACC-NOTES-127 at its 60 s timeout waiting for the Mark entry of the second context menu, rerun three times green in logs/galata-0656-rerun-acc127.log) and 41 passed (logs/galata-0656-live-events-stream-cue.log); jest 486/486 via make test; lint clean with 8 pre-existing warnings
  - repro: install 0.6.55, expand the notes panel: the header holds a caret titled Show notes next to the caret titled Show notes minimap
  - test-tags: UNIT, E2E
  - root-cause: 2026-09-08T14:51:10Z @kj the 24 px pointer-target rule added for the round-4 ux finding declared display inline-flex on .jp-AdvancedMd-notesExpand and .jp-AdvancedMd-notesCollapse after their display none rule, at the same specificity, so it won in every state; the state-gated rule is now the last of the group and the bare rules set display none alone
  - log: 2026-09-08T14:51:10Z @kj added
  - log: 2026-09-08T15:05:59Z @kj closed
- [x] `DEF-NOTES-60` **A mark retried after a refusal lands on the words a change put at the closed selection's place** - MAJOR; when an external tool rewrites the selected passage out of the file while the mark's write is on its way and the server refuses the write, the render that completes meanwhile carries the selection record shut but leaves the object the mark holds at its old offsets, so the retry pairs those offsets with the words now on screen at that place and writes the mark around words the reader never selected; found by the round-5 bug-hunter lens with a probe on the notes harness, a regression of the round-4 change that reads the record once
  - evidence: _carrySelection in src/notes.ts writes the moved offsets into the record whether the range stays open or closes and nulls the field only on close; renderedToSource in src/anchor.ts names no words for a closed range; jest 'writes nothing when a change closed the selected words during the write' (409 with the passage rewritten out, the render during the hold, then 404) asserts id null and the source unchanged; mutation carry-leaves-the-closed-record fails exactly that test (logs/mutation-carry-leaves-the-closed-record.log); Galata on the installed 0.6.57 bundle: 93 passed and 1 failed, the failure being the siblings forced-render flake of DEF-APPLY-37-1 (logs/galata-0657-notes-siblings-settings.log), and 41 passed (logs/galata-0657-live-events-stream-cue.log); jest 490/490 via make test; lint clean with 8 pre-existing warnings
  - repro: select beta gamma in 'Alpha beta gamma delta.', mark; while the route is on its way apply 'Alpha delta.' from disk and render it; answer 409 then 404: the file carries the mark around delta.
  - test-tags: UNIT
  - root-cause: 2026-09-08T15:21:56Z @kj _carrySelection in src/notes.ts moved the record in place only while the range stayed open and nulled the field when it closed, so the holder of the object saw the old offsets; renderedToSource then located the words at those offsets in the new source
  - log: 2026-09-08T15:21:56Z @kj added
  - log: 2026-09-08T15:33:26Z @kj closed
- [x] `DEF-NOTES-61` **Focus falls to the page body when Hide or Collapse is clicked while a note is typed, in browsers whose button click leaves the focus** - MINOR; in Safari and in Firefox on macOS a button click does not move the focus, so with the note field focused a click on Hide or Collapse ran the panel's focus guard with the textarea as the active element: _apply left it to _render, and _render either emptied the body for the hidden state before any refocus or looked for a textarea that the ticks do not hold, so the focus fell to the page body; found by the round-5 ux lens by reading, since the Galata run is Chromium
  - evidence: _apply in src/notes-panel.ts lets the hidden state take every focused control of the panel, and _render runs the row chain when no note field was rebuilt; jest 'moves the focus to the viewer when the panel is hidden while a note is typed' and 'moves the focus to the Hide control when the rows become ticks while a note is typed'; mutations hidden-skips-a-focused-note-field and typing-keeps-the-focus-nowhere each fail their test (logs/mutation-hidden-skips-a-focused-note-field.log, logs/mutation-typing-keeps-the-focus-nowhere.log); Galata on the installed 0.6.57 bundle: 93 passed and 1 failed, the failure being the siblings forced-render flake of DEF-APPLY-37-1 (logs/galata-0657-notes-siblings-settings.log), and 41 passed (logs/galata-0657-live-events-stream-cue.log); jest 490/490 via make test; lint clean with 8 pre-existing warnings
  - repro: in Safari, open a row's note field, click Hide (or Collapse): document.activeElement is the body
  - test-tags: UNIT
  - root-cause: 2026-09-08T15:21:56Z @kj _apply skipped every control inside the body and _render's keepFocus branch focused nothing when no textarea was rebuilt
  - log: 2026-09-08T15:21:56Z @kj added
  - log: 2026-09-08T15:33:26Z @kj closed
- [x] `DEF-NOTES-62` **A mark retried after a refusal lands on the words a change moved to the let-go selection's place** - MAJOR; when the reader clicks in the text or selects other words while the mark's write is on its way, the record the mark holds is no longer the one a render carries; a change ahead of the passage that renders before the answer leaves the record's offsets naming other words on screen, and the retry after a refusal writes the mark around them; found by the round-6 bug-hunter lens with a probe on the notes harness, the sibling of DEF-NOTES-60 on the open-record path
  - evidence: the write callback in src/notes.ts uses the record only while it holds the text of the render as it stands, which a carried record always does; jest 'writes nothing when the reader let the selection go and a change moved the words during the write' (click, change ahead of the passage rendered during the hold, 409 then 404) asserts id null and the source unchanged; mutation stale-record-used-on-the-retry fails exactly that test (logs/mutation-stale-record-used-on-the-retry.log); the sibling identity fix in _onSelectionChange keeps the held record for an unchanged selection, jest 'clears the selection when the browser reported its own restore during the write', mutation same-selection-makes-a-new-record; Galata on the installed 0.6.58 bundle: 96 passed and 1 failed (logs/galata-0658-notes-siblings-settings.log; the failure is ACC-NOTES-120 counting nine entries where the new Add document note entry makes ten, the count set to ten and the case rerun green with the three new cases in logs/galata-0658-rerun-menu-count.log) and 41 passed (logs/galata-0658-live-events-stream-cue.log); jest 501/501 via make test; lint clean with 8 pre-existing warnings; the three new Galata cases failed against the installed 0.6.57 first (logs/galata-0657-proof-document-notes.log: 3 failed)
  - repro: select beta gamma in 'Alpha beta gamma delta.', mark; while the route is on its way click in the text, apply 'beta gamma delta.' from disk and render it; answer 409 then 404: the file carries the mark around gamma delta.
  - test-tags: UNIT
  - root-cause: 2026-09-08T15:55:03Z @kj the write callback in src/notes.ts read the record once and used it on every turn, while _carrySelection moves only the record the controller still holds; a let-go record kept the offsets of a render that was gone
  - log: 2026-09-08T15:55:03Z @kj added
  - log: 2026-09-08T16:09:48Z @kj closed
  - log: 2026-09-08T16:40:23Z @kj confirmed on the 0.6.59 build: Galata on the installed 0.6.59 bundle: 97 passed (logs/galata-0659-notes-siblings-settings.log) and 41 passed (logs/galata-0659-live-events-stream-cue.log); jest 506/506; lint 0 errors and 15 warnings (logs/lint-check-round7.log), two of them the new command's describedBy and label
- [x] `DEF-NOTES-63` **The notes badge is offered with the notes setting off, and a press on it writes a settings marker** - MAJOR; with the notes setting off the panel is pinned hidden, and the badge is shown in every hidden state, so the badge stays on the preview and reads the file's marks; a click on it asks for the expanded state, which has no setting guard and writes a settings marker into the file while the panel stays hidden; found by the round-7 bug-hunter lens with a probe on the wiring harness
  - evidence: NotesController.setPanelState in src/notes.ts refuses every state while the notes setting is off, and the plugin's sync in src/index.ts hides panel.badge with the setting; jest wiring 'takes the badge away with the setting, and a press on it writes nothing' starts from a bare source with the setting on, turns it off, presses the badge and asserts no write and a hidden badge; mutations state-written-with-notes-off (logs/mutation-state-written-with-notes-off.log, caught after the test was set to start from the bare source) and badge-shown-with-notes-off (logs/mutation-badge-shown-with-notes-off.log); Galata on the installed 0.6.59 bundle: 97 passed (logs/galata-0659-notes-siblings-settings.log) and 41 passed (logs/galata-0659-live-events-stream-cue.log); jest 506/506; lint 0 errors and 15 warnings (logs/lint-check-round7.log), two of them the new command's describedBy and label
  - repro: set notes off, open a preview: the badge is on it; click it: the file gains a marks:settings marker and nothing shows
  - test-tags: UNIT
  - root-cause: 2026-09-08T16:26:08Z @kj setPanelState in src/notes.ts checked only the state, and the badge's visibility followed the panel state alone
  - log: 2026-09-08T16:26:08Z @kj added
  - log: 2026-09-08T16:40:23Z @kj closed
- [x] `DEF-NOTES-64` **The notes badge drops the keyboard focus to the page body when it opens the panel** - MAJOR; the badge lives in the box beside the preview and not in the panel node, so the panel's focus guard does not see it; when its press opens the panel the badge is hidden while it holds the focus and the browser moves the focus to the page body; found by the round-7 ux and bug-hunter lenses, the DEF-NOTES-61 case on the one control outside the panel
  - evidence: NotesPanel._apply in src/notes-panel.ts hands a focused badge to the Hide control after the panel is shown, since a focus call into a hidden subtree does nothing; jest 'hands the focus to the Hide control when the badge opened the panel'; Galata 'ACC-NOTES-138 shows the notes badge over the preview while the panel is hidden' presses Enter on the focused badge and asserts the Hide control holds the focus, failed against the installed 0.6.58 first (logs/galata-0658-proof-round7.log: 2 failed); mutation badge-keeps-the-focus-it-loses (logs/mutation-badge-keeps-the-focus-it-loses.log); Galata on the installed 0.6.59 bundle: 97 passed (logs/galata-0659-notes-siblings-settings.log) and 41 passed (logs/galata-0659-live-events-stream-cue.log); jest 506/506; lint 0 errors and 15 warnings (logs/lint-check-round7.log), two of them the new command's describedBy and label
  - repro: panel hidden, Tab to the badge, Enter: the panel opens and document.activeElement is the body
  - test-tags: UNIT, E2E
  - root-cause: 2026-09-08T16:26:08Z @kj _apply in src/notes-panel.ts moved the focus only for controls inside the panel node, and the badge is hidden after the panel is shown
  - log: 2026-09-08T16:26:08Z @kj added
  - log: 2026-09-08T16:40:23Z @kj closed
- [x] `DEF-NOTES-65` **Add document note chosen twice writes a second document marker** - MINOR; markDocument wrote a new marker on every call, so the menu entry chosen again gave the file two document markers and the panel two rows reading Document that nothing tells apart; found by the round-7 ux and bug-hunter lenses
  - evidence: NotesController.markDocument in src/notes.ts answers with the id of the open document mark the file already holds and writes nothing; jest 'opens the document mark the file already holds instead of a second'; Galata 'ACC-NOTES-136' chooses the entry a second time and asserts one document marker and one Document row, failed against the installed 0.6.58 first (logs/galata-0658-proof-round7.log: 2 failed); mutation second-document-marker-written (logs/mutation-second-document-marker-written.log); Galata on the installed 0.6.59 bundle: 97 passed (logs/galata-0659-notes-siblings-settings.log) and 41 passed (logs/galata-0659-live-events-stream-cue.log); jest 506/506; lint 0 errors and 15 warnings (logs/lint-check-round7.log), two of them the new command's describedBy and label
  - repro: choose Add document note, save a note, choose Add document note again: two Document rows
  - test-tags: UNIT, E2E
  - root-cause: 2026-09-08T16:26:08Z @kj markDocument did not look for a document mark the file already holds
  - log: 2026-09-08T16:26:08Z @kj added
  - log: 2026-09-08T16:40:23Z @kj closed
  - log: 2026-09-08T17:24:16Z @kj the route is the panel's plus control since the ruling of 2026-09-08 (ACC-NOTES-139); the repro reads press the plus twice. Round-8 ux and bug-hunter lenses: two presses before the first write lands still wrote two markers through the refused-write retry; the existence check now runs inside the edit on the source of every attempt
- [x] `DEF-NOTES-66` **A document note shows a tick on the minimap** - MEDIUM; After Add document note, a tick appears on the minimap. A document note has no place in the text and must have no tick (ACC-NOTES-137). Seen by the Star Colonel on 1.0.5. The 1.0.5 source skips document notes when drawing ticks and the Galata test for ACC-NOTES-137 passes, so the cause is unknown. First step: reproduce in the browser. If it does not reproduce, check for a stale build.
  - evidence: not reproducible from the source: src/notes-panel.ts has one append into the minimap strip and it skips a mark of the document type, and reverting that guard fails jest 'shows no tick for a document note'. The tick the Star Colonel saw belonged to a passage mark marked in the same document. Galata 'DEF-NOTES-66 puts the one tick of the minimap on the passage mark, not on the document note' now names which mark owns the tick rather than counting ticks, so the question cannot come back ambiguous; no source line was changed; green on the 1.0.6 build with jest 562 of 562, pytest 29 of 29, lint 0 errors, and Galata 147 of 147 in two runs, the last case passing on a re-run after the machine was busy
  - related: ACC-NOTES-137
  - repro: On 1.0.5 open a Markdown preview, choose Add document note, then Show notes minimap. A tick is on the strip.
  - test-tags: E2E
  - log: 2026-09-08T20:38:21Z @kj added
  - log: 2026-09-08T20:48:30Z @kj edited title and text and repro (replaced)
  - log: 2026-09-08T22:31:32Z @kj closed: closed not reproducible
- [x] `DEF-NOTES-67` **Marks an agent deleted from the file stay in the panel and the minimap** - MAJOR; When an external rewrite removes a mark's markers from the file, the panel still lists the mark with its passage and notes, labelled unanchored, and the minimap still shows its tick. They stay until the reader presses Remove or reloads. This was the rule of ACC-NOTES-59. It is wrong: a mark that is gone from the file is gone. The new rule is ACC-NOTES-144.
  - evidence: fixed by ACC-NOTES-144: the remembered listing the marks getter used is gone, so a mark the file no longer holds is not listed and has no tick. jest 'drops a mark a rewrite took both markers from, and writes nothing' plus the rest of the 'broken marks' describe, and the mutation that restores the symptom fails six cases; Galata 'ACC-NOTES-144 removes a mark a rewrite took both markers from'; green on the 1.0.6 build with jest 562 of 562, pytest 29 of 29, lint 0 errors, and Galata 147 of 147 in two runs, the last case passing on a re-run after the machine was busy
  - related: ACC-NOTES-59
  - related: ACC-NOTES-137
  - related: ACC-NOTES-144
  - repro: On 1.0.5 mark a passage and write a note. Rewrite the file from outside JupyterLab without the two markers. The panel still lists the mark and the minimap still shows its tick.
  - test-tags: UNIT, E2E
  - root-cause: 2026-09-08T20:48:30Z @kj By design under ACC-NOTES-59. NotesController._read stores every complete mark in _remembered; the marks getter lists every remembered mark the file no longer holds as unanchored; only remove deletes from _remembered. The panel draws a tick for every listed passage mark, unanchored included.
  - log: 2026-09-08T20:39:10Z @kj added
  - log: 2026-09-08T20:48:30Z @kj edited title and text and repro (replaced)
  - log: 2026-09-08T22:31:32Z @kj closed
- [x] `DEF-NOTES-69` **The broken-mark deletion fired on the reader's own editing and on a half-written file** - CRITICAL; The first build of ACC-NOTES-144 called the deletion from the read that follows any content change. The preview and the editor share one model, so the reader selecting a marked passage and pressing Backspace emptied the passage, the mark counted as broken within one frame, and both markers and every note line the reader had written were deleted in a transaction made with undoable false, so Ctrl+Z brought the text back and never the notes. The same trigger destroyed a mark an agent wrote intact: the server coalesces file events and never holds them past 400 ms, so a half-written file reaches the document by design, and a read landing between the opening marker and the closing marker deleted the opening marker and its notes and committed that to disk. Found in review before any release, by all three lenses independently, each with a jest probe against the live source.
  - evidence: fixed by the settling step: src/notes.ts arms BREAK_SETTLE_MS of 750 ms on every read that sees a break and re-arms it on every further change, and the deletion returns at once while the document holds unsaved edits. jest 'deletes nothing when the reader empties a marked passage as they type', 'deletes nothing while the document holds unsaved edits, and deletes once it is clean', 'leaves a mark the rest of a streamed write made whole again' and 'pushes the settle out with every further change'; Galata 'ACC-NOTES-144 keeps the markers and the notes of a passage the reader emptied while the document is unsaved'; each guard proved by a mutation; green on the 1.0.6 build with jest 562 of 562, pytest 29 of 29, lint 0 errors, and Galata 147 of 147 in two runs, the last case passing on a re-run after the machine was busy
  - related: ACC-NOTES-144
  - repro: On the first 1.0.6 build mark a passage, write a note on it, then select the passage in the editor and press Backspace. The markers and the note text leave the document within a frame and Ctrl+Z does not bring them back.
  - test-tags: UNIT, E2E
  - root-cause: 2026-09-08T22:05:36Z @kj NotesController._read ran from _onContentChanged, which fires for the reader's own typing as well as for a change applied from disk, and it called the deletion write on the first frame a mark looked broken, with no settling step and no test of whether the document held unsaved edits
  - log: 2026-09-08T22:05:36Z @kj added
  - log: 2026-09-08T22:31:32Z @kj closed
- [x] `DEF-NOTES-70` **A duplicated marked passage gives one identifier two rows, and a note saved on the second is written into the first** - MINOR; A mark is a pair of HTML comments in the text, so a reader who copies a marked paragraph in the editor copies its markers with it. The parser then pairs markers by identifier and returns two complete marks for that one identifier. The panel draws a row for each and appends the open note entry to every row whose mark carries the entry's identifier, so both rows show a field holding the same draft; typing into the second row's field and letting any rebuild run moves the focus into the first row's field, and pressing Save on the second row writes the note into the first pair's marker. Pre-existing, not introduced by the 1.0.6 work: found by the round-18 bug-hunter lens while checking that the panel's new dependence on an identifier naming at most one row is sound.
  - evidence: parseMarks in `src/marks.ts` names one mark per identifier, the mark its first opening marker begins: jest 'yields one mark for a passage copied with both its markers', 'reads a repeated identifier as the one mark its first marker opened', 'keeps two pairs with different identifiers as two marks', 'reads the pair, not the orphan, when a copied closing marker comes first', 'reads the opening marker when the only closing marker sits above it', 'reads a document marker a stray closing marker sits above' and 'keeps a lone closing marker the mark where nothing opens its identifier' in `src/__tests__/marks.spec.ts`, with 'leaves the markers a copied passage brought with it where they are', 'writes the note into the first of the two pairs a copy left behind', 'lists and paints the pair a stray closing marker sits above' and 'deletes a stray closing marker nothing in the document opens' in `src/__tests__/notes.spec.ts`; each guard proved by a mutation; green on the 1.0.6 build with jest 582 of 582, pytest 29 of 29, lint 0 errors, and Galata 147 of 147 with 1 skipped
  - related: ACC-NOTES-48
  - repro: Mark a paragraph and write a note on it. In the editor, select the whole paragraph including the invisible comment markers, copy it and paste it below. The panel shows two rows; open the entry on the second, type, and press Save. The note lands on the first.
  - test-tags: UNIT
  - root-cause: 2026-09-09T01:15:12Z @kj parseMarks in src/marks.ts pairs an opening marker with a closing marker by identifier and keeps a list per identifier, so a file holding two pairs with one identifier yields two complete marks; NotesPanel appends the entry form to every row whose mark carries the entry's identifier, and the write finds the first pair
  - log: 2026-09-09T01:15:12Z @kj added
  - log: 2026-09-09T01:39:35Z @kj the first fix registered the first marker rather than the first pair, so a lone closing marker before a whole pair claimed the identifier and hid a real mark and its notes until the cleanup removed the stray; found and fixed in the same session, before any build, by computing the paired identifiers before the marks are built
  - log: 2026-09-09T01:39:35Z @kj closed
  - log: 2026-09-09T02:05:45Z @kj edited evidence (replaced)
- [x] `DEF-NOTES-71` **An older panel-state write overrides a newer panel state and takes the keyboard focus off the Hide control** - MEDIUM; When the reader hides the notes panel and opens it again from the badge before the hide's settings marker reaches the document, the controller reads the file, which still says hidden, and applies that over the newer state the reader asked for. The panel hides and then reopens when the second write lands, and the keyboard focus that the badge handed to the Hide control goes to the preview. The same happens for any two panel-state changes made faster than a write lands.
  - evidence: NotesController.setPanelState in src/notes.ts counts panel-state writes that have not returned in _panelWrites, and _read leaves the panel state alone while that count is above 0; jest 'keeps the state the reader asked for while an older state write lands' failed on the unfixed controller, which emitted expanded, hidden, expanded (logs/jest-def-notes-71-repro.log), and passes with the fix; mutation stored-state-read-over-a-pending-write caught (logs/round20-mutations.log); Galata ACC-NOTES-138 30 of 30 on the fixed build (logs/galata-106-repeat-138-round20b.log), where the unfixed build failed 2 of 46; on the round-21 build 1.0.6: jest 588 of 588 (logs/jest-106-round21.log), lint 0 errors and 8 warnings (logs/lint-106-round21.log), Galata 103 passed (logs/galata-106-round21-notes-siblings-settings.log) and 44 passed with 3 skipped (logs/galata-106-round21-live-events-stream-cue.log)
  - root-cause: 2026-09-10T09:17:51Z @kj setPanelState sets the controller's state and emits at once, then awaits the refresh and the settings write, while _read sets the state from the settings marker the file holds on every content change. A hide write that reaches the document after the reader asked for expanded - brought in by the refresh of the next request, then read on a frame while that request's own write is out - makes the read apply hidden. The plugin's sync hides the panel, whose _apply moves the focus from the Hide control to the viewer, and the later write reopens the panel with the focus left on the viewer. jest 'keeps the state the reader asked for while an older state write lands' reproduces it on the unfixed controller, which emits expanded, hidden, expanded (logs/jest-def-notes-71-repro.log).
  - repro: Mark a passage so the panel opens, press its Hide control, focus the badge and press Enter at once: the panel ends expanded but the focus is not on the Hide control. Galata ACC-NOTES-138 fails at its focus assertion in 2 of 46 runs on the 1.0.6 build (logs/galata-106-round20-notes-siblings-settings.log, logs/galata-106-rerun-138.log)
  - test-tags: UNIT, E2E
  - log: 2026-09-10T09:13:11Z @kj added
  - log: 2026-09-10T09:13:37Z @kj edited title
  - log: 2026-09-10T10:00:54Z @kj closed
- [x] `DEF-NOTES-72` **Cancel on a new document note leaves an empty document note in the file and the panel** - MEDIUM; Pressing the plus in the notes panel writes a document marker into the file and opens its note field. Pressing Cancel closes the field and leaves the marker, so the file keeps a document marker with no note and the panel keeps a Document row with no note. Save with an empty field, and opening the note field of another row, leave it the same way. A passage mark that stays after Cancel is intended, because its colour still marks the passage; a document marker holds nothing without a note
  - evidence: NotesPanel._closeEntry in src/notes-panel.ts, called by Cancel, by Save with an empty field and by _openEntry when the field moves to another row, asks for NotesController.removeEmptyDocument when the entry's mark is a document mark with no notes; removeEmptyDocument in src/notes.ts checks inside the edit that the marker still holds no note before it removes it; Galata ACC-NOTES-139 with its Cancel step failed at notes.spec.ts:625 against the installed 1.0.6 without the fix (logs/galata-106-proof-def-notes-72.log) and passes on the round-21 build (logs/galata-106-round21-notes-siblings-settings.log, 103 passed); jest 'removes the document note the plus wrote when its field is cancelled', 'removes the document note left empty by Save and by the field opened on another row', 'keeps a document note that holds a note, and a passage mark, when the field is cancelled', 'removes a document marker that holds no note', 'keeps a document marker a note reached before the removal was written'; 6 of 6 mutations caught (logs/round21-mutations.log); jest 588 of 588 (logs/jest-106-round21.log), lint 0 errors and 8 warnings (logs/lint-106-round21.log)
  - related: ACC-NOTES-139
  - repro: Open a Markdown file that has no document note in the Markdown Preview, expand the notes panel, press the plus, press Cancel: the file starts with <!-- mark:ID document --> and the panel lists a Document row with no note
  - test-tags: UNIT, E2E
  - root-cause: 2026-09-10T09:37:37Z @kj NotesController.markDocument writes the marker when the plus is pressed, before any note exists, as ACC-NOTES-139 requires. The Cancel button and the empty Save in NotesPanel._form only set _entry to null, and NotesPanel._openEntry replaces an empty entry, so no path removes a document marker that never received a note
  - log: 2026-09-10T09:37:37Z @kj added
  - log: 2026-09-10T10:00:54Z @kj closed
- [x] `DEF-NOTES-73` **A refused and retried panel-state write stores the older state over the newer one** - MINOR; when the route refuses the write of one panel state and the write is retried after the write of a newer state has landed, the retry stores the older state in the file; the panel shows expanded while the file says hidden, and the next read of the file hides the panel under the reader
  - evidence: src/notes.ts setPanelState builds the settings marker from this._state on each attempt of the write; jest 'stores the newer state when an older state write is refused and retried after the newer one lands' passes on the round-23 build (logs/jest-106-round23.log, 589 passed) and fails with the captured state put back (logs/round23-mutations.log, 1 of 1 caught); the Galata notes group on the round-23 build passed 103, ACC-NOTES-138 among them, its one failure the known DEF-APPLY-37-1 case (logs/galata-106-round23-notes-siblings-settings.log)
  - related: DEF-NOTES-71
  - repro: with the panel expanded, press Hide while an external tool writes the file between the hide's refresh and its POST, then open the panel from the badge before the hide's retry is sent: the file ends with panel=hidden, and the next external change hides the panel
  - test-tags: UNIT
  - root-cause: 2026-09-10T10:52:03Z @kj setPanelState's edit closure builds the settings marker from the state captured when it was called, so a write that the route refuses and retries after a newer state has landed stores the older state
  - log: 2026-09-10T10:52:03Z @kj added
  - log: 2026-09-10T11:33:29Z @kj closed
- [x] `DEF-NOTES-76` **A click on a note in the notes panel does not select its row** - MEDIUM; only the head of a row in the notes panel listens for a click, so a click on a note shown in the row, the first note of a closed row or any note of an open row, neither selects the row nor opens it; since a closed row carries no triangle (ACC-NOTES-148), the note line that fills most of a closed row does nothing when clicked
  - evidence: Galata 'DEF-NOTES-76 selects and opens a row from a click on its note' failed on the round-28 build (row controls not found) and passes on round 29 (108 passed); jest mutations note-click-selects-nothing and note-click-drops-a-text-selection caught; logs/round29-chain.log
  - related: ACC-NOTES-148
  - repro: mark a passage and write a note on it, close its row; click the note text shown in the closed row: the row stays closed and unselected, and the passage is not brought into view; a click on the row's head opens and selects it
  - test-tags: UNIT, E2E
  - root-cause: 2026-09-10T13:18:30Z @kj NotesPanel._row adds the click listener that runs selectMark to the head element alone; the note entries built by entryRow and appended to the row have no listener
  - log: 2026-09-10T13:18:30Z @kj added
  - log: 2026-09-10T13:40:13Z @kj closed
- [x] `DEF-NOTES-77` **A double click on Add note changes the colour of the mark** - MEDIUM; a row whose note field is open leaves out Add note (ACC-NOTES-147), and Add note was built before the colour dots, so the first click of a double click on Add note opens the field and moves the dots 76 px left under the pointer; the second click lands on a colour dot, sets that colour and writes it to the file
  - evidence: Galata 'DEF-NOTES-77 keeps the colour of a mark when Add note is double-clicked' failed on the round-28 build (swatch pink, expected yellow) and passes on round 29 (108 passed); jest mutation add-note-built-before-the-dots caught; logs/round29-chain.log
  - related: ACC-NOTES-147
  - repro: mark a passage yellow, open its row and double-click Add note: the note field opens and the mark takes another colour in the panel and in the file
  - test-tags: UNIT, E2E
  - root-cause: 2026-09-10T13:21:58Z @kj NotesPanel._controls built Add note before the colour dots, so when ACC-NOTES-147 removes Add note on the first click, the dots move left under the pointer and the second click of a double click sets a colour
  - log: 2026-09-10T13:21:58Z @kj added
  - log: 2026-09-10T13:21:59Z @kj found by review round 4's ux-designer; residue: after the fix the second click of a double click lands on the empty place Add note left in the controls row, and Chromium moves the focus to the row, so the open note field has no caret until the reader clicks into it; nothing is written, and a fix is a new mechanism for a MINOR
  - log: 2026-09-10T13:40:13Z @kj closed
- [x] `DEF-NOTES-78` **A click on the state line of a closed row does not open the row** - MEDIUM; a closed row carries no triangle and opens from a click on it (ACC-NOTES-148), but only its head and its note entries listen for the click, so a click on the line that names an unanchored mark or a mark of an unknown type opens nothing
  - evidence: jest 'shows the triangle on an open row alone, and opens a row from a click or Enter on it' fails with the state-line listener removed (mutation state-line-click-opens-nothing caught, logs/round30-mutations.log) and passes on the round-30 build: jest 594 of 594, Galata 108 and 44 with 2 skipped; logs/round30-chain.log
  - related: ACC-NOTES-148
  - repro: list a mark the panel shows as unanchored or of an unknown type, close its row and click the line that names that state: the row stays closed
  - test-tags: UNIT
  - root-cause: 2026-09-10T14:28:02Z @kj NotesPanel._row attaches the click listener that runs selectMark to the head and the note entries only, so the state line that an unanchored mark or a mark of an unknown type shows opens nothing
  - log: 2026-09-10T14:28:02Z @kj added
  - log: 2026-09-10T14:28:54Z @kj found by review round 6; residue: a click on the row's own padding or on the 4 px gap above a note line does nothing under the pointer cursor, because the row element listens only for Enter; nothing is written and a click on the row's text opens it; a fix is a branch on the click target, a new mechanism for a MEDIUM, priced at one click listener on the row that runs selectMark when the event target is the row itself
  - log: 2026-09-10T14:42:30Z @kj closed
  - log: 2026-09-10T17:07:34Z @kj the residue logged at 2026-09-10T14:28:54Z, a click on the row's padding or on the gap above a note line, is filed as DEF-NOTES-80
  - log: 2026-09-10T20:41:45Z @kj DEF-NOTES-80 closed: the padding and gap residue is fixed by a listener on the row element
- [x] `DEF-NOTES-80` **A click on the padding of a notes-panel row, or on the gap above its note line, does not open the row** - MINOR; a closed row carries no triangle and opens from a click on it (ACC-NOTES-148), and the whole row shows the pointer cursor, but a click on the row's 6 px padding, on the 4 px margin above a note line, on the 6 px margin above the controls or on the 4 px margin above the note field has the row itself as its target, and the row element listens only for Enter, so the click neither opens nor selects the row
  - evidence: NotesPanel._row adds a click listener on the row element that runs selectMark when the click target is the row itself and no text selection is open; Galata 'DEF-NOTES-80 opens a row from a click on its padding or the gap above its note' failed on the installed 07654a2 build (logs/galata-106-proof-def-notes-80.log) and passes on the round-31 build 1.0.6 (logs/galata-106-round31-notes-siblings-settings.log, 112 passed); jest 'selects and opens a row from a click on its padding or the gap above a note (DEF-NOTES-80)', 'selects nothing from a click on the controls row or the note field of an open row', 'leaves a row alone when a click ends a selection of its note text'; 3 mutations caught: padding-click-selects-nothing, padding-click-drops-a-text-selection, padding-click-takes-every-click-in-the-row (logs/round31-mutations.log)
  - related: DEF-NOTES-78
  - related: ACC-NOTES-148
  - repro: list a mark with a note and leave its row closed; click the 4 px gap between the passage line and the note line, or the padding above the passage line: the row stays closed and unselected under the pointer cursor; a click on the passage or on the note text opens it
  - test-tags: UNIT, E2E
  - root-cause: 2026-09-10T16:25:48Z @kj NotesPanel._row adds click listeners that run selectMark to the head, the state line and the note entries, and only a keydown listener to the row element, so a click whose target is the row itself, on its padding or on the margins between its parts, reaches no listener that selects
  - log: 2026-09-10T16:25:48Z @kj added
  - log: 2026-09-10T20:41:44Z @kj closed
- [x] `DEF-NOTES-81` **A closed notes row does not tell a screen reader that it opens** - MEDIUM; since ACC-NOTES-148 a closed row in the expanded notes panel carries no control; the row is a focusable list item that opens on Enter, but nothing tells assistive technology that the row is closed, that Enter opens it, or that the Collapse button closes an open row; the Expand button that said so left the closed row with the triangle
  - evidence: each row carries aria-describedby to a hidden hint element outside the list body, Closed. Press Enter to open. on a closed row and Open. The Collapse button closes it. on an open row (ACC-CUE-149); Galata 'ACC-CUE-149 describes whether a row is open and how to change it' failed on the installed 07654a2 build with an empty description (logs/galata-106-proof-acc-cue-149.log) and passes on the round-31 build 1.0.6 (logs/galata-106-round31-notes-siblings-settings.log, 112 passed); jest 'lists the marks with list roles and marks the selected one current' asserts both descriptions; 5 mutations caught (logs/round31-mutations.log)
  - related: ACC-NOTES-148
  - related: ACC-CUE-149
  - repro: mark a passage and move the focus to its row in the expanded notes panel: a screen reader reads the row content and list item and nothing about the row state or Enter, and Playwright reads the row accessible description as empty
  - test-tags: UNIT, E2E
  - root-cause: 2026-09-10T16:25:48Z @kj NotesPanel._row in src/notes-panel.ts builds the TOGGLE_CLASS button, the only element that carried a row state for assistive technology, on an open row alone, and sets nothing on the listitem that describes its state; aria-expanded is not a supported state of role listitem in WAI-ARIA 1.2
  - log: 2026-09-10T16:25:48Z @kj added
  - log: 2026-09-10T20:41:44Z @kj closed
- [x] `DEF-NOTES-82` **A double click on the notes badge presses the header control under it** - MEDIUM; the notes badge sits 20 to 44 px from the right edge of the box that holds the preview and the panel and 4 to 28 px from its top, and the header of the expanded panel that its first click shows has the plus 30 to 54 px and the Hide control 6 to 30 px from that edge at the same height, so every point of the badge lies over one of the two; the second click of a double click presses that control: over the plus it writes a document marker into the file and opens the Document row with its note field focused, over Hide it hides the panel again and stores the hidden state; a double click on Hide where it overlaps the badge opens the panel again in the same way; measured on 1.0.6: 25 of 25 double clicks at the badge centre wrote the marker
  - evidence: button() in src/notes-panel.ts returns before its handler when event.detail is above 1, so the second click of a double click presses no panel button (the badge, the plus, Hide, Save); Galata 'DEF-NOTES-82 presses no header control with the second click of a double click on the notes badge' failed on the installed 07654a2 build with a document marker written (logs/galata-106-proof-def-notes-82.log) and passes on the round-31 build 1.0.6 (logs/galata-106-round31-notes-siblings-settings.log, 112 passed); jest 'lets the second click of a double click press no control of the panel or the badge', 'acts on a single click and on a key press, which reports no click count'; 4 mutations caught: double-click-guard-removed, single-click-ignored, key-press-ignored, double-click-counted-as-a-press (logs/round31-mutations.log)
  - related: ACC-NOTES-138
  - related: ACC-NOTES-139
  - repro: open a Markdown file with no marks, so the panel is hidden, and double-click the notes badge at its centre: the file gains a document marker and the panel shows a Document row with an open note field; double-click the badge 3 px inside its right edge: the panel opens and hides again
  - test-tags: UNIT, E2E
  - root-cause: 2026-09-10T17:07:33Z @kj button() in src/notes-panel.ts runs the handler of the badge, the plus and Hide on every click, and the badge (style/base.css .jp-AdvancedMd-notesBadge, top 4px, right 20px) overlaps the plus and Hide of the expanded header; the first click shows the panel, Lumino lays it out on the next animation frame, and the second click of the double click, which the browser reports with detail 2, reaches the control now under the pointer
  - log: 2026-09-10T17:07:33Z @kj added
  - log: 2026-09-10T20:41:44Z @kj closed
- [x] `DEF-NOTES-83` **A double click on Save writes the note twice** - MEDIUM; Save in a note field adds the note on every click and the field is rebuilt only after the write returns, so the second click of a double click on Save reaches the same Save button and writes the same note a second time; measured on 1.0.6: 5 of 5 double clicks left the marker holding the note twice
  - evidence: the same guard in button() covers Save; Galata 'DEF-NOTES-83 writes a note once when Save is double-clicked' failed on the installed 07654a2 build with the note held twice (logs/galata-106-proof-def-notes-83.log) and passes on the round-31 build 1.0.6 (logs/galata-106-round31-notes-siblings-settings.log, 112 passed); jest 'writes a note once when Save is double-clicked (DEF-NOTES-83)'; mutations double-click-guard-removed, single-click-ignored and double-click-counted-as-a-press each fail that case (logs/round31-mutations.log)
  - related: DEF-NOTES-82
  - repro: open a note field, type a note and double-click Save: the marker in the file holds the note twice and the row lists it twice
  - test-tags: UNIT, E2E
  - root-cause: 2026-09-10T17:07:33Z @kj the Save handler in NotesPanel._form (src/notes-panel.ts:810-823) calls addNote on every click through button(), which runs its handler with no test of the click count, and the form is rebuilt only after the write returns
  - log: 2026-09-10T17:07:33Z @kj added
  - log: 2026-09-10T20:41:44Z @kj closed
- [x] `DEF-NOTES-84` **A passage that ends at a hard line break cannot be marked** - MAJOR; in a paragraph whose line ends with two spaces, a hard line break, the renderer (marked 17) writes the break as a br element with no newline after it, so the captured rendered text runs the word before the break and the word after it together (rygor:W); a selection of the words before the break snaps outward to that joined word, which no source token matches, and the mark command writes nothing and shows nothing; reported by the Star Colonel on the passage Ostateczny rygor: of the Norlandia argument file, where Add note did nothing
  - evidence: captureText reads a br element as a break of the captured text (src/highlight.ts) and NotesController._listed lists a passage by its source tokens (src/notes.ts); jest 'breaks the text at a hard line break, which the renderer writes without a newline (DEF-NOTES-84)' in src/**tests**/highlight.spec.ts, 'finds the words before a hard line break, which the render shows with no newline (DEF-NOTES-84)' in src/**tests**/anchor.spec.ts and 'lists a passage by its words, without the list number or the emphasis marks around it (DEF-NOTES-84)' in src/**tests**/notes.spec.ts; mutations line-break-ignored and every-element-breaks-the-text (logs/round33-mutations.log) and passage-listed-as-raw-source (logs/round34-mutations.log) each caught; Galata 'DEF-NOTES-84 marks the words before a hard line break' in ui-tests/tests/notes.spec.ts failed on the round-32 build with no mark written and on the round-33 build with the row listing the raw source 3. **Ostateczny rygor:** (logs/galata-106-proof-def-notes-84.log), green on the round-35 build of 1.0.6 with jest 607 of 607, pytest 29 of 29, Galata 113 of 113 and 46 of 46 (logs/round35-chain.log)
  - related: ACC-NOTES-44,ACC-NOTES-101
  - repro: open a file holding a line that ends with two spaces followed by another line of the same paragraph, select the last word before the break in the preview and choose Add note: no marker is written, no row appears
  - test-tags: UNIT, E2E
  - root-cause: 2026-09-10T20:56:32Z @kj captureText in src/highlight.ts walks text nodes only and inserts a break between two blocks, but a br element inside one block leaves no text node, so the text before and after it is concatenated; renderedWords then reads one word across the break and locate finds no such token in the source, where the line break separates the two words
  - log: 2026-09-10T20:56:32Z @kj added
  - log: 2026-09-10T21:03:27Z @kj fix in the tree: captureText reads a br element as a break of the captured text (src/highlight.ts); jest 'breaks the text at a hard line break' and 'finds the words before a hard line break' pass; Galata 'DEF-NOTES-84 marks the words before a hard line break' written; round-33 chain running
  - log: 2026-09-10T21:20:07Z @kj the round-33 build wrote the mark but listed the passage as the raw source between the markers, '3. **Ostateczny rygor:**'; NotesController._listed now lists a passage by its source words, which drops the list number and the emphasis marks; jest 'lists a passage by its words, without the list number or the emphasis marks around it' passes; round-34 chain running
  - log: 2026-09-10T22:07:34Z @kj closed
- [x] `DEF-NOTES-86` **A passage beside an inline br element cannot be marked, and a mark over one loses its highlight** - MAJOR; since the hard line break fix (DEF-NOTES-84) the captured rendered text breaks at every br element, but tokeniseSource skips an HTML tag in the source without ending the word before it, so a br written inline in the source (Alpha<br>Beta, a table cell one<br>two) still joins the words around it into one source token; the render reads two words and the source one, no token matches, and Add note on either word writes nothing; a mark already over such a passage finds no rendered range and draws unanchored; found by the architect and slop-hunter lenses of review round 2 with reproductions in jest
  - evidence: the tag branch of angle() in tokeniseSource calls flush() before pushing the html span of a br tag (src/anchor.ts), so a br written in the source ends a word as captureText reads it; jest 'ends a word at a br element written in the source, as the render does (DEF-NOTES-86)', 'finds the word before a br element written in the source (DEF-NOTES-86)' and 'finds the passage of a mark over a br element written in the source (DEF-NOTES-86)' in `src/__tests__/anchor.spec.ts`, all three failing on the unchanged code first (the selection found nothing, the passage null); mutations br-flush-removed and every-tag-flushes each caught (logs/round37-mutations.log); Galata 'DEF-NOTES-86 marks the word before a br element written in the source' in ui-tests/tests/notes.spec.ts failed on the round-36 build with no note field opened, nothing written (logs/galata-106-proof-def-notes-86.log), and is green on the round-37 build of 1.0.6 with jest 611 of 611, pytest 29 of 29, Galata 115 of 115 and 46 of 46 (logs/round37-chain.log)
  - repro: open a preview of a file holding the line Alpha<br>Beta and more words here.: select Alpha, choose Add note: nothing is written; a file holding a mark whose passage is Alpha<br>Beta shows the mark's row unanchored and no highlight
  - test-tags: UNIT, E2E
  - root-cause: 2026-09-10T22:33:57Z @kj the tag branch of angle() in tokeniseSource (src/anchor.ts) pushes an html span and returns past the tag without calling flush(), so the characters before and after the tag accumulate into one word; captureText (src/highlight.ts) reads the same br as a break since DEF-NOTES-84, so the two readers disagree
  - log: 2026-09-10T22:33:57Z @kj added
  - log: 2026-09-10T22:51:58Z @kj closed
- [x] `DEF-NOTES-87` **A passage that starts mid-line with a marker character loses its first token in the panel row** - MINOR; NotesController._listed lists a passage by tokenising the source slice between its markers as a document of its own, so a slice that starts mid-line with a character tokeniseSource strips at a line start, a bullet dash, a hash, a quote marker or a table pipe, loses that character or its whole first token from the row text; found by the bug-hunter lens of review round 2
  - evidence: NotesController.marks scans the whole source once and _listed lists the scan's tokens that overlap the passage, in the context of their own line, as _paint already read them (src/notes.ts); jest 'lists a passage that starts mid-line with a marker character by its whole text (DEF-NOTES-87)' in `src/__tests__/notes.spec.ts` failed on the unchanged code with the dash and the hash stripped; mutation listed-passage-read-as-a-slice caught (logs/round37-mutations.log); the DEF-NOTES-84 cases unchanged and green; round-37 build of 1.0.6 with jest 611 of 611, Galata 115 of 115 and 46 of 46 (logs/round37-chain.log)
  - repro: mark the words '- and its cost' inside the line 'the plan - and its cost' and open the notes panel: the row reads 'and its cost'
  - test-tags: UNIT
  - root-cause: 2026-09-10T22:33:57Z @kj _listed re-reads the slice out of the context of its line, while _paint two methods below already scans the whole source once and reads a passage as the tokens that overlap it
  - log: 2026-09-10T22:33:57Z @kj added
  - log: 2026-09-10T22:51:58Z @kj closed
- [x] `DEF-NOTES-89` **The second line of a selected or marked two-line heading covers letters of the first line** - MINOR; when a heading wraps to two lines in the preview, a selection made over it, or a mark painted over it, shows the second line's highlight box covering the bottom of the first line's letters; reported by the Star Colonel on 2026-09-11 on the h3 title of the Norlandia argument file; pictured with the extension live and off (tmp/heading-live-selected.png, tmp/heading-off-selected.png, byte-identical) and with a yellow mark over the heading (tmp/heading-live-marked.png), so the browser's own selection shows it without this extension and this extension's marks show it too
  - evidence: style/base.css gives the headings of the preview, .jp-MarkdownViewer .jp-RenderedMarkdown :is(h1, h2, h3, h4, h5, h6), line-height: normal in place of JupyterLab's 1 (review round 4 superseded the earlier 1.25, a value tied to one font: normal takes the font's own ascent plus descent, so the line box holds the glyphs under any theme font; review round 5 scoped the rule to the preview, a notebook cell's rendered Markdown untouched; the computed heading line height in the pictures moved from 20.16 px, equal to the font size, to the font's own glyph box, the computed value reading normal on the live and the off runs, logs/galata-106-round43-heading-shot.log); Galata 'DEF-NOTES-89 keeps the box of its second line clear of the first, so a selection over it covers no letters' in ui-tests/tests/live-view.spec.ts reads the line boxes of a wrapped h3 through Range.getClientRects and failed on a build without the rule with the second box starting 3.3 px above the end of the first (logs/galata-106-proof-def-notes-89.log, built to logs/build-106-round41-prefix.log), and is green on the round-45 build of 1.0.6 with jest 616 of 616, pytest 29 of 29, lint 0 errors, Galata 114 of 115 in the notes group (the one failure the siblings forced-render case of DEF-APPLY-37-1, rejected wontfix, passing 3 of 3 in isolation) and 49 of 49 with 2 skipped (logs/round45-chain.log); the title pictured selected before (tmp/heading-live-selected.png of 01:46, the second line's box cutting the first line's letters, byte-identical with the extension off) and after (the same file of the round-43 run, two separate bands; logs/galata-106-round43-heading-shot.log)
  - repro: open the Norlandia argument file in the preview at a width where the h3 title wraps, drag-select the title: the second line's blue selection box cuts the bottom of the first line's letters; mark the title yellow: the mark on the second line does the same
  - test-tags: E2E
  - root-cause: 2026-09-10T23:48:01Z @kj JupyterLab's rendered headings take line-height from --jp-content-heading-line-height, which the theme sets to 1, while the font's glyph box (ascent plus descent) is taller than the font size, so the inline box of every heading line reaches into the line above; the browser paints a line's inline backgrounds, the selection highlight and any span background such as a mark, before that line's text but after the text of the line above, so the second line's box covers the first line's descenders and baseline strokes; the extension neither styles the selection nor touches heading line-height, and its mark span is an ordinary inline background inside the heading
  - log: 2026-09-10T23:48:01Z @kj added
  - log: 2026-09-11T07:15:22Z @kj the Star Colonel clarified at 01:50 that the report is about the selection, before any mark; the only remedy inside this extension is the heading line height, applied as one rule on headings inside .jp-RenderedMarkdown (line-height 1.25), which also keeps a mark's box to its own line
  - log: 2026-09-11T07:15:22Z @kj closed
  - log: 2026-09-11T08:13:14Z @kj review round 4: the value 1.25 was a guess tied to the container's font, 8 of 36 fonts on this host having a normal line height above it (Segoe UI 1.33 on a Windows host); the rule now reads line-height: normal, so the line box is the font's own glyph box plus its line gap on every host; the Galata case's abutment assertion holds either way (round-43 chain)
  - log: 2026-09-11T08:28:46Z @kj edited evidence (replaced)
  - log: 2026-09-11T08:55:42Z @kj review round 5: the heading rule reached every rendered Markdown in the lab, a notebook cell's included, the one rule in the stylesheet outside the preview; the selector now reads .jp-MarkdownViewer .jp-RenderedMarkdown :is(h1, h2, h3, h4, h5, h6), the preview alone, and outranks rendermime's own heading rule instead of tying with it on load order
  - log: 2026-09-11T09:33:12Z @kj edited evidence (replaced)

## Change detection `DETECT`

Noticing that a file on disk has changed

- [x] `DEF-DETECT-20` **A deletion in the first moments after a preview opens can go unreported** - MINOR; MINOR; a preview opened while the change channel is already connected has no recorded baseline until the next fallback tick, so if the file is deleted inside that window and the file event is also lost, the deletion is taken for the baseline and never reported; not reproduced, found by reading the code
  - evidence: unit test 'reports a file deleted between its registration and the first check' in `src/__tests__/channel.spec.ts`, written failing first with no change reported at all, and the control 'reports a file deleted right after its registration once when its event arrived'; restoring the old baseline fails the first and passes the control, and emitting past the event guard fails the control and passes the first; the fix adds no request, so the one-connection criterion is untouched; jest 417 of 417, Galata 94 of 94 on build 0.6.25
  - repro: open a second preview while the channel is connected, delete its file within the fallback interval and suppress the file event, then watch for the missing marker
  - test-tags: UNIT
  - root-cause: 2026-09-07T03:57:38Z @kj confirmed by test: the fallback took every first reading as the baseline, so a first reading of nothing was recorded as the baseline and no later reading could differ from it; the baseline is now taken only from a first reading that found a file, because a path is registered for a document that has already loaded from it, so the file was there when it was registered
  - root-cause: 2026-09-07T01:32:08Z @kj register on an open socket sends the registration but takes no stat, and the fallback treats a first reading of nothing as the baseline rather than as a change; the watcher's own initial check covers only a file already gone at registration
  - log: 2026-09-07T01:32:08Z @kj added
  - log: 2026-09-07T03:57:38Z @kj closed
- [x] `DEF-DETECT-24` **A document on another drive can show a false file-gone marker** - MEDIUM; MEDIUM; a path carrying a drive prefix, as jupyter-collaboration gives every document it manages, cannot be resolved under the server root, and the batched check answers nothing for it; since a first answer of nothing is now read as a deletion, such a document shows the file-gone cross on its tab although the file is there; introduced by the fix for DEF-DETECT-20 and not reachable on this workstation, where jupyter-collaboration is not installed
  - evidence: unit test 'leaves a path the server cannot serve out of the comparison' in `src/__tests__/channel.spec.ts`, written failing first with the false file-gone report, and the server tests test_stat_leaves_out_a_path_it_cannot_serve and test_drive_path_is_ignored; four separate breaks were run, each turning only its own tests red; jest 420 of 420, pytest 23 of 23, Galata 94 of 94 on build 0.6.26
  - repro: install jupyter-collaboration, open a Markdown document it manages, wait one fallback interval and read the tab
  - test-tags: UNIT
  - root-cause: 2026-09-07T04:19:55Z @kj the batched check answered nothing both for a file that is gone and for a path it could not serve; the answer now leaves out a path it cannot serve altogether, so the client can tell the two apart, and that also covers a path outside the root and a path the operating system cannot express
  - root-cause: 2026-09-07T03:57:38Z @kj the batched check answers the same nothing for a file that is gone and for a path it cannot resolve, so the two cannot be told apart by the client
  - log: 2026-09-07T03:57:38Z @kj added
  - log: 2026-09-07T04:19:55Z @kj closed
- [x] `DEF-DETECT-25` **An event arriving before the first check can swallow the change after it** - MINOR; MINOR; when a file event arrives for a document between its registration and its first fallback check, the record of that event is not cleared by the check, so the next change the fallback finds is taken for the one already reported and is not applied; the change after that is reported normally
  - evidence: unit test 'does not let an event before the first check consume the change after it' in `src/__tests__/channel.spec.ts`, written failing first with the second change swallowed; removing the one line that clears the record turns only that test red
  - repro: register a path on a standing connection, deliver an event for it before the first check, then change the file without an event and wait a fallback interval
  - test-tags: UNIT
  - root-cause: 2026-09-07T03:57:38Z @kj the branch that records the first reading returns without clearing the event record that the ordinary branch below it consumes
  - log: 2026-09-07T03:57:38Z @kj added
  - log: 2026-09-07T04:19:55Z @kj closed
- [x] `DEF-DETECT-26` **The same false file-gone marker remains where the server extension is absent** - MINOR; MINOR; where this extension's server side is not installed the fallback compares through JupyterLab's own contents interface, and a document on another drive is not served there either, so its absence is read as a deletion and the tab shows the file-gone cross although the file is there; the server-side half of this was fixed, this half was not
  - evidence: unit test 'leaves a drive path out of the contents comparison too' in `src/__tests__/channel.spec.ts`, written failing first with the false report standing beside the genuine one; three breaks were run - the guard removed, the guard widened to any colon, and the guard always true - each turning only its own tests red, the third also proving the ordinary fallback is not silently disabled; jest 421 of 421, pytest 23 of 23, Galata 94 of 94 on build 0.6.27
  - repro: run a lab with jupyter-collaboration and this extension's frontend but without its server extension, open a document that collaboration manages, wait one fallback interval and read the tab
  - test-tags: UNIT
  - root-cause: 2026-09-07T04:37:00Z @kj measured against jupyter-server 2.21.0: the contents route has no notion of drives and resolves a drive path as a file name under the server root, answering not-found with the same status and the same message as a missing file, so the two cannot be told apart there; the client-side manager does not separate them either, because an unregistered drive name falls through to the default drive; the drive path is therefore left out of the comparison by the same first-segment rule the server uses, which loses nothing because that path was never being compared correctly
  - root-cause: 2026-09-07T04:20:05Z @kj the contents fallback treats a not-found answer as a deletion without asking whether the path is one it can serve at all
  - log: 2026-09-07T04:20:05Z @kj added
  - log: 2026-09-07T04:37:00Z @kj closed

## Sibling extension compatibility `COMPAT`

Defects in how the viewer coexists with the sibling Markdown extensions

- [x] `DEF-COMPAT-43` **Scroll restore skipped for three seconds after a tab activation** - MAJOR; the viewer's fallback timer treats every render within 3 s of a tab activation as guarded by the switch-tab scrolling fix and skips its own scroll restore; in a lab without that fix, or on a render it does not guard, an image document whose file is rewritten in that window collapses under the reader while its images reload and nothing puts the position back
  - evidence: FOREIGN_SCROLL_GUARD_MS, foreignGuardMarked, noteActivated, _activatedAt and the ILabShell block deleted; _foreignScrollGuard is the attribute test alone; Galata 'DEF-COMPAT-43 restores the scroll of an unguarded image document however recently its tab came to the front' blocks the switch-tab bundle and passes on 0.6.44 and 0.6.46 (logs/galata-064*-notes-siblings-settings.log); bundle mutation making the viewer always yield fails it, 1000 expected 1492 received (logs/def-compat-43.log)
  - test-tags: E2E
  - repro: in a lab without the switch-tab scrolling fix, open a Markdown file with an image in the preview, scroll down, switch to another tab and back, rewrite the file on disk within 3 s; the preview renders, the picture reloads and the scroll position is lost
  - root-cause: 2026-09-08T09:47:53Z @kj _foreignScrollGuard in src/controller.ts answers true while no guard attribute has ever been seen and the activation is under 3 s old, a fallback kept for a sibling release that does not mark its widget; the fallback governs whenever the mark is absent, which on image-less documents is always
  - log: 2026-09-08T09:47:53Z @kj added
  - log: 2026-09-08T09:49:29Z @kj edited test-tags (added)
  - log: 2026-09-08T10:13:11Z @kj edited text and repro (replaced)
  - log: 2026-09-08T11:38:21Z @kj closed

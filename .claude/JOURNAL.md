# Claude Code Journal

This journal tracks substantive work on documents, diagrams, and documentation content.

---

1. **Task - Project creation and Claude Code configuration** (v0.1.0): Created `jupyterlab_advanced_markdown_viewer_extension` as a new JupyterLab extension and initialised its project configuration<br>
   **Result**: Scaffolded from `jupyterlab/extension-template` v4.6.5 as a frontend-and-server extension with settings and tests enabled (`.copier-answers.yml`). Purpose: keep open Markdown documents live when an external agentic tool rewrites the file on disk, with a tab-title cue, fading pale red and green highlights for removed and added text, working in both the rendered view and the editor without garbling concurrent user edits. Source is still the template scaffold (`src/index.ts` hello route call, `routes.py` hello handler, empty `schema/plugin.json`). Wrote `.claude/CLAUDE.md` as a thin overlay importing the user-level and workspace-level CLAUDE.md layers, adding the Makefile-only build lifecycle (`make install`, `make publish`, `make clean`, `make mrproper`), the lockfile-pair commit rule, the Makefile version check against `@utils/jupyterlab-extensions/Makefile` (both at 1.37), the acceptance-criteria and defects mandate, and the required `jupyterlab-extension` and `playwright` skills. Rewrote `README.md` with the seven standard badges, a feature list modelled on the terminal-show-in-file-browser extension, and nothing below the Uninstall section.

2. **Task [Extended] - Acceptance criteria and sibling extension survey** (v0.1.0): Defined the visual and functional paradigm for live Markdown files as 42 acceptance criteria and surveyed the eight sibling Markdown extensions for conflicts<br>
   **Result**: A background agent read the sibling extensions (edit-at-content, export, github-alerts, insert-content, switch-tab-scrolling-fix, syntax-rendering-fix, viewer-toc-fix, paste-as-markdown) plus the refresh-view and colourful-tab extensions and found twelve collision points: three competing scroll authorities (the switch-tab guard's 100 ms interval, the TOC fix scrolling to the hash on every `rendered`, edit-at-content's scroll sync), edit-at-content's invariant that `.jp-RenderedMarkdown` child count equals the `marked` block count, the alerts parser wrapper that only runs through `IMarkdownParser.render`, insert-content's whole-document `setSource` bursts that look like external rewrites, export reading the file from disk, and refresh-view's `context.revert()` discarding unsaved edits. The paradigm therefore applies external content through `sharedModel.updateSource` in one tagged Yjs transaction, three-way merges against the last disk text keeping the user's lines on overlap, highlights with CodeMirror decorations in the editor and inline spans inside existing blocks in the viewer, and signals through `widget.title.className`. Filed through `pm-tools` into `docs/acc-crit-advanced-markdown-viewer.md` under categories DETECT, APPLY, HILITE, CUE, CONFIG and COMPAT; `docs/defects.md` seeded with the author roster; `pm-tools check docs --strict` clean. Four sibling changes recorded as COMPAT criteria: TOC fix hash gating, switch-tab guard signal, refresh-view dirty prompt, export reading the on-screen source.

3. **Task [Extended] - Reconciliation paradigm research** (v0.1.0): Researched how concurrent editing is solved and which paradigm fits an agentic tool rewriting a Markdown file while the user types<br>
   **Result**: Two background agents read primary sources - the operational transformation papers (Ellis and Gibbs 1989, Jupiter 1995, Sun and Ellis 1998), the CRDT literature, Fraser's Differential Synchronization (2009), diff3 (Khanna, Kunal and Pierce) - and the actual source of VS Code, Emacs, Vim, IntelliJ, CodeMirror, `jupyter-collaboration` and `jupyter_ydoc`. Conclusion: operational transformation is a category error here because the disk emits no positioned operations, and the CRDT already in JupyterLab is a category error as the reconciler because a `.md` file carries no character identifiers to converge with - it is the transport only. The fitting paradigm is Differential Synchronization: hold a shadow of the last text agreed with disk, diff shadow against the new snapshot, apply as positioned hunks, update the shadow unconditionally. Fraser's fuzzy patching is replaced with a three-way merge (base = shadow) because a wrong fuzzy match silently corrupts a document the user is reading. The Star Colonel's overlay intuition maps onto the shadow but the geometry differs - a shadow sits beside the view, not underneath, and carries no precedence rule. Survey finding: no mainstream editor merges into a dirty buffer; VS Code refuses with four guards, Emacs and Vim refuse, IntelliJ asks, and JetBrains declares a `ConflictResolution.MERGE` mode documented for AI agents that is not implemented. `jupyter-collaboration` overwrites silently and its tracker carries open data-loss issues; its `YUnicode.set` already diffs but two-way against the live document, which is the defect the shadow fixes. Wrote `docs/research-live-markdown-reconciliation.md`. Corrected four mechanism lines through `pm-tools` (hash-first detection, shadow, three-way merge, the `undoable` flag rather than tracked origins), added `ACC-APPLY-43` for shadow maintenance, and recorded the unresolved constraint that JupyterLab's `Context._contentsModel` is private so a merged document cannot be marked clean without saving, patching a private field, or accepting the File Changed dialog.

4. **Task [Extended] - Live preview implementation, Galata suite and adversarial review loop** (v0.1.6): Implemented the viewer-only live Markdown preview with fading change highlights and tab cue, covered it with 41 unit tests and a 10-test Galata suite, and took it through a five-round three-lens adversarial review that returned SHIP<br>
   **Result**: **Trigger**: the Star Colonel narrowed scope to the rendered viewer, editor deferred, because the viewer removes the merge problem: reload iff clean, block and cue when dirty. `src/watcher.ts` `FileWatcher` polls the contents API hash then `last_modified` against a shadow with a Lumino `Poll` started inside `context.ready` (a poll before load duplicated content over the load in flight), applies disk content through `sharedModel.updateSource` inside one `transact(fn, false, EXTERNAL_ORIGIN)` so the editor undo stack stays untouched, blocks on a dirty model, and carries an `enabled` switch. `src/controller.ts` `LiveViewController` diffs rendered text of `.jp-RenderedMarkdown` by words rather than mapping source, so highlights survive whatever the parser and the alerts sibling produce; `src/highlight.ts` wraps added runs in inline spans and inserts removed runs as inline ghosts, never adds direct children of the render root (edit-at-content counts them), skips whitespace text nodes under the root, and drops ghosts past the diff token bound `MAX_LCS_TOKENS`. Tab cue via `widget.title.className`: updated, blocked, and an active class pulsing the icon while writes continue. Galata caught two defects (`DEF-HILITE-1` root-level whitespace wrapped, `DEF-CONFIG-2` `enabled=false` kept polling), both closed; the suite overrides galata's `waitForApplication` because this lab opens with no Launcher tab, and threads the port through `JUPYTER_TEST_PORT` after 8899 turned out to be held by another session's server. Highlight and fade observed in recorded browser video; ghost lifetime measured at 1300 ms in the browser, so the lingering frames in the video were stale screencast frames, not a defect. Review ran as constructed Workflow `review-live-markdown-preview` encoding the nine loop invariants, lenses architect, ux-designer, bug-hunter; three PLAN rounds applied six closures: diff baseline held while a fade is pending so bursts highlight cumulatively (the first version tested the timer before creating it, a line-order defect the reviewers reproduced with a jest harness that became `controller.spec.ts`), ghost fades opacity to zero before removal so the reflow happens invisibly, ghost bound moved from characters to the diff's own token unit, and decorations re-created during a hold skip their fade-in so a streaming writer produces a hold rather than a pulse. Rounds four and five adjudicated clean. Seven defects remain open in `docs/defects.md`: `DEF-APPLY-3` scroll restore late pass (needs the TOC-fix sibling change), `DEF-APPLY-4` stale contents model raising the File Changed dialog (three options on `ACC-APPLY-9`, the Star Colonel's decision), `DEF-CUE-5` blocked cue cleared on first interaction (decision on when the cue ends), `DEF-HILITE-6` to `9` minor.

5. **Task [Extended] - Root causes and fixes for the seven open defects, adversarial review rounds six to ten** (v0.6.7): Resolved `DEF-APPLY-3`, `DEF-APPLY-4`, `DEF-CUE-5` and `DEF-HILITE-6` to `9` at root cause, took the fixes through five more review rounds to SHIP, grew Galata to 15 tests and unit tests to 62<br>
    **Result**: **Trigger**: Star Colonel's order to resolve every open defect at root cause, with the rule that the File Changed dialog belongs to the editor and never to the live viewer. `DEF-APPLY-4`: `Context._maybeSave` compares the recorded hash against disk before every write, so a save after an applied change raised the dialog itself and the save-after-merge option was not viable; `src/watcher.ts` now reads content with `hash: true` (the server hashes only on request) and hands the applied revision to the Context's private `_updateContentsModel`, the same move the collaborative drive makes through a save event the Context accepts only for collaborative models; a unit test in `watcher.spec.ts` fails should a JupyterLab release drop the method. A save over unsaved edits never reaches that path, so the editor keeps its dialog and both Overwrite and Revert release the blocked marker through a new `unblocked` signal. `DEF-CUE-5`: watcher keeps the pending change until the model is clean, reports it once; controller clears only the updated marker on attention. `DEF-APPLY-3`: own restore recognised by landing position, and during the restore window a scroll more than 500 ms after the last wheel, pointer or key event counts as another extension's, so the late pass overrides the TOC fix's smooth scroll without touching the sibling. `DEF-HILITE-6` gap pseudo-element after a ghost, `7` captureText skips SVG-namespace text, `8` fade timer cleared in `_clearDecorations`, `9` per-span fade-in by diffing the previous render against the current one. Round 8 found `DEF-HILITE-11` (ghost bound tested one side of the diff's two-sided coarse rule, fixed with `pastBound` on either side); round 10 recorded its trade-off as `DEF-HILITE-12`, a short ghost hidden when a 1000-token append follows, deferred to the Star Colonel. **E2E gotcha**: the Galata lab URL carries `?reset`, which JupyterLab rewrites within about 2.5 s dropping the hash, so the TOC fix's render-time anchor scroll never fires there, and assigning `window.location.hash` hangs the router; the test stands the trigger in through the sibling's patched `setFragment` 100 ms after `rendered`. Rounds 9 and 10 adjudicated clean. Eight defects closed through `pm-tools` with evidence, `ACC-HILITE-20` mechanism corrected. Graphify graph built in `tmp/graphify-out` for blast-radius checks going forward.

6. **Task [Extended] - Typed-in change animation with animationSpeed setting, review to SHIP, last defects closed** (v0.6.8): Added text now types in letter by letter and removed text stands red, then is deleted backwards; new setting `animationSpeed`; seven ANIM criteria closed; DEF-HILITE-10 and 12 closed; 132 unit tests, 18 Galata tests<br>
    **Result**: **Trigger**: Star Colonel's requirement that a change is followed as it happens and no text disappears without warning, recorded as ACC-ANIM-73 to 79 and in the README. **Design**: a workflow read the code, produced three designs and judged them; live typing on real text nodes (new `src/animate.ts`, `ChangeAnimator`, one requestAnimationFrame loop per document, per-tick character budget) won over reserved-width reveals because the text must visibly grow and shrink. Added spans start empty and gain characters; a ghost holds 750 ms (`GHOST_HOLD_MS`) and is deleted from its end; `jp-AdvancedMd-typing` pauses the CSS fade-out until a span is complete; headings and speed 0 or reduced motion take the pre-animation path unchanged. **Gotchas**: the workflow's own verify/fix loop ran three rounds and each added reconciliation machinery (ghostParts, freshRemovalOf, coreAt, offset mapping) that a randomised check still broke in 76 and 217 of 1500 cases; the adversarial review (architect, ux-designer, bug-hunter, five rounds, SHIP on rounds 4 and 5) replaced it with ghosts built by construction: fresh removals plus the animator's live carried runs at mapped offsets, so text that left never comes back; nodes of one contiguous added range are chained (negative shown debt) so inline markup types in document order; a render that creates no decorations clears the runs; a removal straddling the typed edge is clipped per character. **Defects**: DEF-HILITE-10 closed by matching a fresh removal by offset and text; DEF-HILITE-12 closed by `MAX_GHOST_TOKENS` (50), a ghost-size line for the coarse diff branch that keeps DEF-HILITE-11 hidden. Galata gained three tests (typing, hold then delete, animation off) with a sampling helper; the hold assertion allows for attach-poll latency. Release 0.6.8 reached npm; PyPI rejected the wheel for empty project URLs, now filled. Workflow scripts persisted under `.claude/workflows/`; graph rebuilt with graphify after every change.

7. **Task - Animation switch, Settings section in README, screenshots of the animation and the tab cue** (v0.6.8): Added `animation` setting (default on) that turns the typing off while live updates, highlight and tab cue stay; documented `enabled` as the whole-plugin switch; captured frames and video under `docs/images/`<br>
    **Result**: **Trigger**: Star Colonel asked for a switch covering the entire plugin, how it looks and how the tab signals arrivals. `LiveViewController._speed()` already merged reduced motion and `animationSpeed` 0 into one path, so `animation` became one more input to it and `updateSettings` re-applies the speed when it flips; `enabled` already stopped the watcher poll, so only its schema description and a README Settings section were needed. Two ad-hoc Galata specs, deleted after the run, captured `animation-*.png` plus `animation.webm` and `tab-*.png`: green dot before the label on the first applied change, icon pulse while writes keep arriving, dot alone after the quiet window, cleared on pointer, key or wheel in the document, red dot while unsaved edits block a change. Verification: 134 jest, Galata 18/18 on installed 0.6.8, `ACC-ANIM-80` and `ACC-CONFIG-33` closed, `pm-tools check docs --strict` clean, `lint:check` exit 0.

8. **Task [Extended] - File events replace polling, marks and notes stored in the Markdown file, five adversarial rounds** (v0.6.27): Rebuilt change detection on operating-system file events, built the marks-and-notes feature, edited three sibling extensions, closed all 103 criteria and 26 defects<br>
    **Result**: **Trigger**: Star Colonel's order to implement every remaining acceptance criterion through dynamic workflows, then a goal adding notes, zero defects at root cause, and survival of a four-lens adversarial review. **Detection**: polling cost one read per document per interval and delayed a change by up to a full interval; `watch.py` `FileWatchRegistry` now holds one watchdog observer per directory keyed by inode, `routes.py` serves status, stat and one `/events` WebSocket, and `channel.ts` shares that socket across every open preview with a batched stat fallback for filesystems raising no events and a contents-API fallback where the server extension is absent. Coalescing is capped at 400 ms because the first implementation was a pure debounce, so a streaming writer - this extension's primary user - delivered nothing until it stopped. **Notes**: `marks.ts` parses paired HTML comments carrying a UUID, unknown types and attributes preserved byte for byte; `anchor.ts` aligns source to rendered text and widens a boundary out of emphasis, links, headings and fences, and never starts an opening marker on a continuation line because CommonMark reads `<!--` at a line start as an HTML block and swallows the paragraph; `notes-panel.ts` reparents the content through a Lumino `BoxPanel` rather than moving DOM nodes. **Siblings**: the switch-tab fix now marks its widget with `data-jp-scroll-guard` so the viewer reads the guard instead of guessing three seconds. **Reviews**: five rounds found what tests could not - a scroll-guard test that passed with the cooperation deleted (position cannot discriminate; scroll writes are now attributed to the bundle that made them), a cue timer that erased the settled turn at shipped defaults, and the notes feature reaching past the `enabled` switch through three doors, closed by moving the test into the single function that reads the file. Every test was broken before it was trusted; every mutation harness carries a negative control. jest 62 to 421, pytest 1 to 23, Galata 18 to 94.

9. **Task [Extended] - Rounds four and five of the improvement campaign: an adjudicator overruled, a scroll guard reverted, two MAJOR defects found in my own fixes** (v0.6.40): Round 4 caught a change made against the adjudicator's deferral, round 5 caught silent note loss and an invisible dark-theme flash; all 116 criteria and 44 defects resolved<br>
    **Result**: **Trigger**: Star Colonel's standing order to improve the extension under `graphify`, fix every defect and survive `/devils-advocate:adversarial-review`. **Round 4**: four lenses raised three MAJOR against one change - a re-armed late pass in `LiveViewController._restoreScroll` the adjudicator had explicitly deferred and I made anyway. Holding the guard 600 ms instead of 150 discards any scroll no input on the widget node precedes, and a table-of-contents click lands in the sidebar while a scrollbar drag emits one `pointerdown` then bare scroll events, so the reader's own navigation was undone. Reverted; `DEF-APPLY-37` reopened as `DEF-APPLY-37-1`, rejected wontfix - the flake is 1 in 113 runs and every remedy costs the reader more. **Lesson**: the adjudicator had named that blast radius before the change; a zero-defects directive is not a licence to overrule a reasoned deferral. **Round 5**, the three lenses the goal names, scoped to the post-round-4 deltas: architect SHIP, bug-hunter and ux-designer DO-NOT-SHIP. `DEF-NOTES-39` (MAJOR): `NotesController._write`'s guard answered `true` for both states reaching it, though only one wrote into the document - on the other (reader typed, or a second external write landed inside the POST) the mark was on disk and absent from the document, and `NotesPanel` clears the note textarea on that answer, so a typed note existed nowhere. Fixed by returning `this._source === applyEdits(source, changes)`, computing the answer instead of assuming it; this overturned my own round-4 decline on `DEF-NOTES-33`, whose server-work premise the probes disproved. `DEF-NOTES-40` (MAJOR): the flash keyframe sets `background-color` on the mark span, so it replaces the mark's colour rather than layering over it and composites over the page; over the dark theme the grey sat 0.0010 from the yellow mark's luminance - invisible outside foveal fixation, the one place a locate signal is not needed. Themed through `--jp-AdvancedMd-mark-flash-bg` for a 7.4 percent step. Also closed `DEF-NOTES-36-1`, `-38`, `-41` and `-42`. **Gotcha**: six mutations, six caught - but one first escaped because my test composited the flash over the mark instead of the page. The test's model of the CSS was wrong, not the CSS. **Final**: jest 451, pytest 29, Galata 112 of 112 in 7.9 min, lint 0 errors and 7 pre-existing warnings, nothing open.

10. **Task [Extended] - Server write route, keyboard marking, plus control, document notes, badge, rounds 6 to 13 to SHIP, release 1.0.5** (v1.0.5): took the campaign from 0.6.41 through nineteen unpublished 0.6 builds to the release 1.0.5 with two consecutive clean adversarial rounds<br>
    **Result**: **Trigger**: the standing directive to improve the extension until every acceptance criterion is closed, every defect fixed and the four lenses return two clean rounds. **Built**: marks and notes on the document as a whole (ACC-NOTES-136 to 139: a `document` marker with no passage written after any YAML front matter, listed first, added only from a plus control in the panel header, the Star Colonel's ruling); the notes badge over the preview while the panel is hidden; the minimap strip with the hide control above the plus and the expand caret (ruling of 2026-09-08); one `_openEntry` rule so a press on Add note never silently blocks on a draft elsewhere, the draft's collapsed row re-opened and focused, whitespace read as no draft as Save reads it; the found path of `markDocument` flushing the deferred read so a marker the press's own refresh brought in is listed before the id is answered; the one-marker guard inside the write callback so two presses before the first write lands write one marker. **Why the rounds ran long**: each confirming round found one class the previous fix had not covered (race, draft discard, silent block, whitespace), and round 11 found my own register error - an evidence rewrite that located the criterion by the nearest backticked id and landed ACC-NOTES-139's text on ACC-NOTES-138; rounds 12 and 13 were then clean. **Verification**: every fix carries a jest case and a mutation the runner proves that case kills (`tmp/campaign/round*-mutations.py`, logs under `logs/mutation-*.log`); new Galata cases first fail against the previous installed bundle. Final on 1.0.4: jest 519/519, lint 0 errors and 8 warnings, 5 of 5 mutations, Galata 98 and 41 (three cases that failed under concurrent sibling builds rerun green), 129 criteria closed and 10 rejected with 0 open, 0 open defects. **Release**: the version was set so each verification build landed on the release number; 1.0.3 and 1.0.4 were published outside this session at 20:31 from an intermediate tree; the reviewed tree shipped as 1.0.5 through `/release-jupyterlab-extension`. README gained a Usage section and four screenshots taken in the JupyterLab Dark theme by `ui-tests/tests/screenshots.spec.ts`. **Siblings**: six Markdown siblings released through a dynamic workflow (template v4.6.5, Makefile 1.38, dependencies, publish, CI green, journals); the forced `npm audit fix` in the canonical Makefile's `upgrade` target downgraded every `@jupyterlab/*` range to 0.x in all six trees, so canonical 1.39 reports advisories instead.

11. **Task [Extended] - Highlight visibility, 25 characters per second, @author, broken marks removed, tab marker freed from reduced motion, rounds 14 to 19** (v1.0.6): implemented five criteria and four defects, then six adversarial rounds that broke the campaign's own fixes twice and answered both by deletion<br>
    **Result**: **Trigger**: the Star Colonel filed eight locked items after using 1.0.5 - a tab marker that never turned, a stray minimap tick, ghost marks an agent had deleted, faint highlights, a slow default animation and a note signature nobody could read - then asked for a dynamic workflow, implementation and adversarial review. **Built**: `highlightVisibility` (ACC-HILITE-140) as a `oneOf` declaration with Low, Medium and High labels, written as `data-jp-advancedmd-visibility` on the viewer widget node so a change recolours live decorations with no rebuild and no sibling constraint touched; the four change-highlight alphas five points up (ACC-HILITE-141); `animationSpeed` default 10 to 25 (ACC-ANIM-143); `@author` as the handle an empty setting writes, deleting the identity lookup, the `User` import and the plugin option (ACC-NOTES-142); and broken marks removed with their leftover markers swept from the file (ACC-NOTES-144), which replaced the unanchored rule of ACC-NOTES-59. **Diagnoses**: DEF-CUE-68 was one cause - `style/base.css` set `animation: none` on all three tab markers under `prefers-reduced-motion`, last in the file at equal specificity, and the shorthand also reset the faster turn; Windows reports that preference whenever its show-animations switch is off, and the same preference silenced the typing animation, so the ruling became that only this extension's own settings stop its motion, rewriting ACC-CUE-72 and ACC-ANIM-77. DEF-NOTES-66 was not reproducible: one append into the minimap strip, guarded, and the tick belonged to a passage mark. **Why six rounds**: round 14 found a critical all three lenses reproduced - the deletion fired on any content change, so a reader's Backspace destroyed a mark and its notes in a non-undoable transaction and a half-written file was committed with the marker stripped. The fix was a 750 ms settling step plus an unsaved-edits guard. Rounds 15 to 19 then attacked the machinery those fixes added: a confirmed set meant to stop the panel outrunning the file was deleted after four proven failures, and a focus record went from boolean to two states to the element itself. Twice the right answer was removal, leaving the extension smaller than before the mechanism existed. **Also**: DEF-NOTES-70, pre-existing, where a duplicated marked paragraph gave one identifier two rows and a note saved on the second landed in the first, fixed by ACC-NOTES-145 - an identifier names one mark. **Runs**: jest 582, pytest 29, Galata 147, lint 0 errors, every guard proved by a mutation (`tmp/campaign/siblings/round19/mutations.md`).

12. **Task [Extended] - Post-1.0.6 fixes: CI lockfile and sibling gate, plus off the minimap strip, DEF-NOTES-71 and DEF-NOTES-72, README images at 2560x1440** (v1.0.6): prepared CI to go green, reversed the minimap plus ruling, fixed two defects and retook every README image<br>
    **Result**: **Trigger**: CI on `6381905` was red; the Star Colonel reversed the 2026-09-08 ruling that put the plus on the minimap strip, reported that Cancel on a new document note left the note behind, and asked for 16:9 2K README images in the Galaxa Steel dark theme. **CI**: two causes - `yarn.lock` descriptor ranges stale since `f58ff53` (four hunks adding `@jupyterlab/ui-components ^4.0.0` and `@lumino/messaging ^2.0.0`, identical to a local yarn resolution), and sibling-dependent Galata cases on a runner that installs this extension alone, now skipped only where `CI` is set (`ui-tests/tests/siblings.spec.ts`; with `CI=1` 8 skipped, 1 passed). **Minimap plus**: `style/base.css` shows the plus only in the expanded state; ACC-NOTES-139 and ACC-NOTES-130 rewritten and closed. **DEF-NOTES-71**: the intermittent ACC-NOTES-138 failure (2 of 46) was a real race - while a panel-state write was on its way, `_read` applied the older state still in the file and hid the panel under the reader. `setPanelState` counts pending writes in `_panelWrites` and `_read` leaves the state alone while the count is above 0; 30 of 30 after. **DEF-NOTES-72**: the plus writes the document marker at the press, as ACC-NOTES-139 requires, and no way out of the field removed it. A passage mark kept after Cancel is intended, since its colour still marks the passage; a document marker without a note holds nothing. `NotesPanel._closeEntry` (Cancel, empty Save, field moved to another row) calls `NotesController.removeEmptyDocument`, which checks inside the edit that the marker still holds no note, so a note another writer added meanwhile keeps it. Removal on close chosen over writing the marker only on Save, which would have reopened the locked criterion. **Images**: whole-window shots from a 1280x720 viewport at `deviceScaleFactor` 2; `animation-01` and `tab-05` gained capture cases in `ui-tests/tests/screenshots.spec.ts`, the tab marker animations paused for a fixed frame. **Proofs**: each Galata change failed on the unfixed build first; 8 mutations caught across rounds 20 and 21. **Final on 1.0.6**: jest 588, pytest 29, lint 0 errors and 8 warnings, Galata 103 and 44 with 3 skipped, 0 open criteria and 0 open defects.

13. **Task [Extended] - Note field font, DEF-NOTES-73, README reduced to two images and seven feature bullets, DEF-CUE-74** (v1.0.6): made the Star Colonel's font and README changes, fixed one review finding and one tooltip defect found while checking the README<br>
    **Result**: **Note field**: Star Colonel asked for a non-monospace font in the note input, then a size smaller than usual, and ruled it a criterion, not a defect. `.jp-AdvancedMd-notesForm textarea` in `style/base.css` gains `font-family: inherit` and `font-size: var(--jp-ui-font-size0)` (ACC-NOTES-146; its Galata case failed first on the old build with family `monospace`). Smaller font made the ACC-NOTES-118 box 58 px against a 60 px check, so that check now fills four lines and asserts no scroll, the criterion's own test line. **DEF-NOTES-73**: found by review round 2's architect - the retried write in `setPanelState` built the settings marker from the state the call captured, so a hide refused with 409 and retried after a newer expand stored `panel=hidden`. Closure now reads `this._state` on each attempt; one jest case, one mutation caught. **README**: at the Star Colonel's request four of six images and their capture code in `ui-tests/tests/screenshots.spec.ts` removed; `animation-01.png` and `notes-01-panel.png` retaken at 2560x1440 below `#jp-top-panel` in locale en-GB. `application:toggle-top` does not exist in JupyterLab 4.6, so the viewport grows by the bar height and the shot is clipped below it. Note dates stay in the browser display language by the Star Colonel's choice. First rewrite merged the features into two prose bullets, rejected as prose; after a marketing and a UX review the list became seven short bullets, each checked against the register, plus a Limitations section, and three false statements were removed. Image links stay relative: absolute links to `main` showed the old committed images. **DEF-CUE-74**: checking the unsaved-edits claim showed the held-change tooltip said save or revert to take the change, but a completed save drops the held change (`FileWatcher._onSaveState` releases it, ACC-APPLY-105). `TAB_CAPTIONS` in `src/controller.ts` now names revert; Galata tab-cue asserts it after failing on the old build. **Also**: all 10 sibling extensions confirmed at their latest PyPI releases. **Runs** (`logs/round24-chain.log`): lint 0 errors and 8 warnings, jest 589, pytest 29, Galata 104 and 44 with 2 skipped; 136 criteria and 74 defects closed, 0 open. Review round 3 launched as run `wf_8f25cf7b-4f8`; resume recipe in `tmp/campaign/siblings/round20/launch.json`.

14. **Task [Extended] - Note field a quarter smaller, Add note left out while a note is written, review round 3 and DEF-CUE-75** (v1.0.6): made two Star Colonel requests and the one change review round 3 ordered<br>
    **Result**: **Font**: Star Colonel asked for the note field font 25% smaller; read literally as a quarter off the size the field had, so the `style/base.css` textarea takes `font-size: calc(var(--jp-ui-font-size0) * 0.75)`, about 8.1 px against the panel's 13 px (the light theme and Galaxa Steel both define `--jp-ui-font-size0` as 0.8333em). ACC-NOTES-146 reopened and reworded; its Galata case resolves `--jp-ui-font-size0` in a probe beside the field, so the assertion does not depend on one theme's numbers; failed on the round-24 build (10.8333 px against 8.125 px). **Add note**: first request hid a row's colours and Add note while its note is written, then narrowed to Add note alone (ACC-NOTES-147). `NotesPanel._row` passes `entry === null` to `_controls(mark, offerNote)`; the accessible-name jest case now takes Add note before the press so its name stays checked. Galata case failed on the round-25 build, mutation caught. Source and spec edits waited for the round-25 chain, because Playwright reloads a spec when a worker restarts. **Review round 3** (`wf_8f25cf7b-4f8`): PLAN, one change. Two reviewers found the tooltip word Revert matches the File menu's Revert to Checkpoint, which runs `restoreCheckpoint` then `revert` and copies the checkpoint over the agent's change on disk; confirmed in the installed `jlab_core` bundle before editing. Reload from Disk, which both reviewers proposed, was rejected: `Context._revert` sets no `dirty=false` and emits no save state, so the red square stays. DEF-CUE-75: the tooltip now names a save, then Revert in the File Changed dialog, and Overwrite to keep the reader's version; README bullet and tab-cue assertion match; proof failed on round 26. Reload from Disk leaving the marker is deferred to the Star Colonel as a new mechanism. **Runs** (`logs/round27-chain.log`): lint 0 errors and 8 warnings, jest 590, pytest 29, Galata 105 and 44 with 2 skipped; 137 criteria and 75 defects closed, 0 open. Review round 4 next, recipe in `tmp/campaign/siblings/round20/launch.json` key `round4`.

15. **Task [Extended] - Row triangle only on an open row, note field at three quarters of the panel text size, DEF-NOTES-76, review round 4 and DEF-NOTES-77** (v1.0.6): made three Star Colonel requests and the one change review round 4 ordered<br>
    **Result**: **Font**: entry 14's quarter-off reading put the note field at 8.1 px, and the Star Colonel then said the field was too small; switched to the other reading, three quarters of the panel's ordinary text size, `calc(var(--jp-ui-font-size1) * 0.75)`, 9.75 px. ACC-NOTES-146 reworded; its Galata probe resolves `--jp-ui-font-size1`; proof failed on the round-27 build (8.12 px against 9.75 px). **Triangle**: Star Colonel asked that a click opens a row with no triangle, and only collapsing needs one (ACC-NOTES-148). `NotesPanel._row` builds the `TOGGLE_CLASS` button on an open row alone; its click calls `stopPropagation`, because the head's own click selects the row and closing is not selecting. Jest helper `expand()` clicks the head; six Galata cases open their row by a click; proof failed on round 27 (triangle count 1 on a closed row), mutation caught, README notes image retaken. **DEF-NOTES-76**: Star Colonel reported that a click on a note selected nothing. Each note entry gets a click listener running `selectMark`, skipped while `window.getSelection().isCollapsed` is false, so a reader copying note text keeps the selection. Jest spies on `window.getSelection`, because the jsdom Range lacks `selectNodeContents`. **Review round 4** (`wf_b462f0a8-dd1`): PLAN, one change, cleanStreak 0. DEF-NOTES-77: Add note sat before the six colour dots, so once the first click of a double click removed it (ACC-NOTES-147) the dots moved under the pointer and the second click set pink. The Add note block moved after the dot loop; proof failed on round 28 (swatch pink, yellow expected). A refuted MINOR on ACC-NOTES-137 was still corrected, because its text was false. The adjudicator cautioned that changes arriving while rounds run add review surface. **Runs** (`logs/round29-chain.log`): lint 0 errors and 8 warnings, jest 594, pytest 29, Galata 108 and 44 with 2 skipped, 3 of 3 mutations caught, notes image 2560x1440; 138 criteria and 77 defects closed, 0 open. Review round 5 next, recipe in `tmp/campaign/siblings/round20/launch.json` key `round5`. Deferred to the Star Colonel: Reload from Disk leaves the red square; the badge double click is not measured.

16. **Task [Extended] - Review rounds 5 and 6, DEF-NOTES-78, SHIP declared at the round limit** (v1.0.6): ran the last two review rounds, applied round 6's one change, and declared SHIP by the Star Colonel's ruling<br>
    **Result**: **Round 5** (`wf_5e90059f-103`, which ran rounds 5 and 6): 4 MINOR, adjudicated clean (1 of 2). Three refuted: a double click on a closed row's right end reaches the new Collapse triangle and closes the row again (nothing written); Add note moved from Tab stop 3 to 9 behind the colour dots, kept because the revert test reopens DEF-NOTES-77; a click on already-selected note text is ignored once, which is the guarantee's own exception. Deferred to the Star Colonel: a closed row no longer tells assistive technology that it opens, since the Expand button left it; `aria-expanded` is not allowed on `role=listitem` in WAI-ARIA 1.2, and the conforming fixes reopen ACC-CUE-115 (the row role) or ACC-NOTES-148 (a control on a closed row). **Round 6**: 2 MEDIUM with one cause, only the head and the note entries listened for the click that opens a row. Plan: one listener on the state line in `NotesPanel._row`, identical to the head's, plus one jest assertion. DEF-NOTES-78 filed, related ACC-NOTES-148, closed on mutation `state-line-click-opens-nothing`. Clicks on the row padding and on the 4 px gap above a note stay a residue on DEF-NOTES-78, because the fix is a branch on the click target, a new mechanism for a MEDIUM. **SHIP**: round 6 is `maxRounds`, so two clean rounds in a row were no longer reachable; the Star Colonel ruled "after all declare SHIP" instead of raising the limit (`launch.json` key `shipRuling`), and SHIP was declared after the round-30 chain. **Runs** (`logs/round30-chain.log`): mutation 1 of 1, lint 0 errors and 8 warnings, jest 594, pytest 29, Galata 108 and 44 with 2 skipped; 138 criteria and 78 defects closed, 0 open. Commit and push wait for approval; CI on `main` has been red since `6381905` for the `yarn.lock` descriptors and the sibling Galata cases this tree fixes.

17. **Task [Extended] - The four known limitations fixed: Reload from Disk, padding click, row description, double click** (v1.0.6): fixed the four limitations recorded at SHIP, each grounded by a workflow agent and proven by a failing Galata case first<br>
    **Result**: **Trigger**: entry 16 left four limitations recorded and not fixed; the Star Colonel asked for them through a dynamic workflow, Reload from Disk discussed first. Ground workflow `wf_ff2a2378-4fa` (Opus 5, one agent per limitation) wrote a spec each to `tmp/campaign/limitations/ground-<key>.json`. **DEF-CUE-79**: `Context._revert` in `@jupyterlab/docregistry` 4.6.3 (`context.js:583-641`) loads the file and records the revision through `_updateContentsModel`, which emits `fileChanged`, but never sets `dirty` false (only `_save` does, line 553), so the held change stayed held, the red square stayed and the next write was held again. Of three options the Star Colonel chose C: `FileWatcher` connects `context.fileChanged` to `_onFileChanged`, which runs `_check` while a change is held, and the equal-text branch of `_read` now releases, records the contents model and sets `model.dirty` false. **DEF-NOTES-80**: a listener on the row element in `NotesPanel._row` runs `selectMark` when the click target is the row itself and no text selection is open. **DEF-NOTES-81** with ACC-CUE-149: `aria-expanded` is not allowed on `role=listitem`, so `hint()` builds two hidden elements outside the list body with page-unique ids from a module counter, and each row carries `aria-describedby` to the closed hint (Closed. Press Enter to open.) or the open hint (Open. The Collapse button closes it.); Playwright's `toHaveAccessibleDescription` reads it. **DEF-NOTES-82**: 25 of 25 double clicks on the badge pressed the plus or Hide under it; the Star Colonel chose one guard in the shared `button()` builder, `event.detail` above 1 returns before the handler, so it covers the badge, the plus, Hide and Save; Save was measured writing a note twice, filed and closed as DEF-NOTES-83. Mechanism records added on ACC-CUE-30 and ACC-NOTES-148. **Runs** (`logs/round31-chain.log`): 5 Galata proofs failed on the installed `07654a2` build, mutations 16 of 16 caught, lint 0 errors and 8 warnings, jest 602, pytest 29, Galata 112 and 45 with 2 skipped; 139 criteria and 83 defects closed, 0 open. Next: adversarial review with the architect, ux-designer, bug-hunter and slop-hunter lenses, then commit and push on approval, then `logs/` and `tmp/` removed once CI is green.
18. **Task [Extended] - Review rounds 1 to 6 of the limitations delta: note field, defaults, DEF-NOTES-84 to 89, DEF-APPLY-90** (v1.0.6): ran the four-lens adversarial review of the four-limitations delta to its cap, applied every plan test-first, and took the Star Colonel's asks as criteria along the way<br>
    **Result**: **Trigger**: entry 17's next step. The review ran through the constructed Workflow `tmp/campaign/limitations/round32-review.js` on Opus 5 (architect, ux-designer, bug-hunter, slop-hunter; an adjudicator plans, the main session applies; cap 6 rounds, 2 clean required). All six rounds returned a plan (findings 7, 8, 12, 5, 6, 7), each verified by chains 33 to 45 (`logs/roundNN-chain.log`). **Asks as criteria**: ACC-NOTES-146 note field at a row's note size, ACC-NOTES-150 a closed row shows no note, ACC-NOTES-151 the note field's look, ACC-ANIM-143 speed default 50, ACC-HILITE-21 highlight default 5 s. **Defects closed**: DEF-NOTES-84 a passage ending at a hard line break; DEF-CUE-85 the hold test in `FileWatcher._read` holds a write only while the document is dirty and its text differs from the held revision and the shadow; DEF-NOTES-86 a `br` in the source ends a word; DEF-NOTES-87 the listed passage from the whole-source scan; DEF-HILITE-88 a middle past the token bound aligned line by line first (`MAX_LCS_LINES` 1600) so two far-apart edits stay two edits; DEF-NOTES-89 preview headings take `line-height: normal`; DEF-APPLY-90 CRITICAL, a preview opened over unsaved edits overwrote them at open because the shadow was set from a dirty model, fixed by setting the shadow at ready only while the model is clean, proven by jest on the unguarded source and by Galata on a build without the guard (round 44's Galata case sat under a `beforeEach` that opened the preview first and proved nothing). **Round 6, the cap**, returned a register-only plan: DEF-APPLY-90's evidence had claimed the next write clears the marker, which the test contradicts (a later write is held; a save, a Reload from Disk with the file quiet, or the document becoming clean clears it); DEF-CUE-85 records the shape-B limit with an intermediate revision, the obvious remedy unsafe since `fileChanged` also fires on rename. Adjudicator: converging, no further round on this code. **Open for the Star Colonel**: the MAJOR note-scent finding (ACC-NOTES-150 locked) and the cap with clean streak 0 of 2. **Chain 45**: lint 0 errors, jest 616, pytest 29, Galata 114 of 115 (DEF-APPLY-37-1, known intermittent, 3 of 3 alone) and 49 of 49 with 2 skipped; 141 criteria and 90 defects closed, 0 open. **Lesson**: a proof runs on a build lacking the fix. Next: commit and push on approval, CI, `logs/` and `tmp/` removed.
